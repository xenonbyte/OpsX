---
phase: 08-stability-json-and-release-coverage
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - lib/migrate.js
  - lib/sync.js
  - scripts/test-workflow-paths.js
autonomous: true
requirements:
  - QUAL-06
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Write-sensitive migration and sync path guards reuse the shared containment helpers instead of maintaining their own copies."
    - "Sync still refuses writes outside `repoRoot/.opsx/specs` and migration still preserves no-partial-write / truthful-abort behavior."
    - "The path topic script covers glob-special fixtures plus write-sensitive guard failures."
  artifacts:
    - path: "lib/migrate.js"
      provides: "Migration planning and status logic refactored onto shared path helpers"
      contains: "getMigrationStatus"
    - path: "lib/sync.js"
      provides: "Canonical spec guard and sync writes refactored onto shared path helpers"
      contains: "resolveCanonicalSpecsDir"
    - path: "scripts/test-workflow-paths.js"
      provides: "Write-sensitive path and glob-special fixture coverage"
      contains: "applySyncPlan rejects targets outside canonical specs without writing files"
  key_links:
    - from: "lib/path-utils.js"
      to: "lib/migrate.js"
      via: "migration base containment checks reuse the shared helper"
      pattern: "ensureWithinBase"
    - from: "lib/path-utils.js"
      to: "lib/sync.js"
      via: "sync canonical-spec guards reuse the shared helper"
      pattern: "ensureWithinBase"
    - from: "lib/sync.js"
      to: "scripts/test-workflow-paths.js"
      via: "path topic tests lock canonical-spec write rejection and no-partial-write behavior"
      pattern: "applySyncPlan"
---

<objective>
Migrate the write-sensitive path guards onto the shared utility layer and extend the path topic script with guard fixtures.

Purpose: finish D-07 and D-08 for the write paths after the read-only utility layer lands, without weakening the existing migration or sync safety guarantees from Phase 7.
Output: shared-helper-backed migrate/sync guards and expanded path-topic coverage for canonical-spec, migration, and glob-special edge cases.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
@.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
@.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
@.planning/phases/08-stability-json-and-release-coverage/08-PATTERNS.md
@lib/migrate.js
@lib/sync.js
@scripts/test-workflow-paths.js

<interfaces>
From `lib/path-utils.js` after 08-03:
```javascript
const {
  toPosixPath,
  normalizeRelativePath,
  relativeToBase,
  isWithinBase,
  ensureWithinBase
} = require('./path-utils');
```

From `lib/glob-utils.js` after 08-03:
```javascript
const {
  escapeGlobLiteral,
  buildLiteralPattern,
  createMatcher,
  matchNormalizedPaths,
  parseGlobArtifactOutput
} = require('./glob-utils');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor migration and sync guards onto the shared path utility layer</name>
  <files>
lib/migrate.js
lib/sync.js
  </files>
  <behavior>
    - Migration and sync use one shared containment policy for base-relative checks.
    - Sync continues to reject any supplied canonical spec directory that does not equal `repoRoot/.opsx/specs`.
    - No-partial-write and truthful abort behavior remain unchanged.
  </behavior>
  <read_first>
lib/migrate.js
lib/sync.js
lib/path-utils.js
lib/glob-utils.js
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
  </read_first>
  <action>Per D-07, D-08, D-17, and the locked "no new dependency" decision, replace the write-sensitive local containment helpers in `lib/migrate.js` and `lib/sync.js` with the shared exports from `lib/path-utils.js`. `lib/migrate.js` must keep truthful migration diagnostics, no-partial-write execution, and existing abort semantics while dropping duplicated `toUnixPath` / `ensureWithinBase` logic. `lib/sync.js` must keep the exact canonical-spec restriction that rejects any supplied `canonicalSpecsDir` outside `repoRoot/.opsx/specs`, must keep rejecting individual write targets outside that directory, and must keep the no-partial-write behavior from Phase 7. Do not broaden sync to arbitrary roots, do not add a new filesystem dependency, and do not reintroduce caller-controlled escape hatches.</action>
  <acceptance_criteria>
- `lib/migrate.js` and `lib/sync.js` import `./path-utils`.
- `lib/sync.js` still contains the exact guard message `Sync plan canonicalSpecsDir must match repoRoot/.opsx/specs.` or an intentionally updated equivalent assertion target.
- Existing migration and sync guard tests continue to pass under `npm run test:workflow-runtime`.
- No new package dependency is added.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-paths.js</automated>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "require\\('./path-utils'\\)|canonicalSpecsDir must match repoRoot/\\.opsx/specs|ensureWithinBase" lib/migrate.js lib/sync.js</automated>
  </verify>
  <done>
- QUAL-06 now covers the write-sensitive migrate/sync guards as well as the read-only surfaces.
- Phase 7 safety guarantees are preserved while duplicated path logic is removed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the path topic script with glob-special and write-guard fixtures</name>
  <files>
scripts/test-workflow-paths.js
  </files>
  <behavior>
    - Path-topic coverage includes canonical-spec write rejection, caller-supplied canonical root rejection, and glob-special artifact/path fixtures.
    - The topic script proves literal glob characters are escaped before matching and parsing.
    - TEST-03 and TEST-04 stay green during the guard refactor.
  </behavior>
  <read_first>
scripts/test-workflow-paths.js
lib/migrate.js
lib/sync.js
lib/path-utils.js
lib/glob-utils.js
.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
  </read_first>
  <action>Move the existing sync path-guard assertions into `scripts/test-workflow-paths.js` and expand them for Phase 8. Preserve the exact existing tests `applySyncPlan rejects targets outside canonical specs without writing files` and `applySyncPlan rejects caller supplied canonical spec roots outside repo .opsx specs`. Add new path-topic assertions that cover glob-special literal path fixtures and `parseGlobArtifactOutput` behavior so `[` / `]` / `*` / `?` characters are treated as literals when required. If migration guard assertions are currently only implicit, add one explicit regression that proves migration still refuses paths outside the canonical repo/home bases. Do not move verify/sync/archive gate semantics here; keep this file narrowly focused on path behavior and write guards.</action>
  <acceptance_criteria>
- `scripts/test-workflow-paths.js` contains the exact two existing sync guard test names from the action.
- `scripts/test-workflow-paths.js` contains at least one new test that exercises a glob-special literal path or artifact name.
- `node scripts/test-workflow-paths.js`, `npm run test:workflow-runtime`, and `npm test` all pass.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-paths.js</automated>
    <automated>npm run test:workflow-runtime</automated>
    <automated>npm test</automated>
    <automated>rg -n "applySyncPlan rejects targets outside canonical specs without writing files|applySyncPlan rejects caller supplied canonical spec roots outside repo \\.opsx specs|glob-special|parseGlobArtifactOutput" scripts/test-workflow-paths.js</automated>
  </verify>
  <done>
- The path topic script covers the full Phase 8 path/glob hardening surface, including write-sensitive guards.
- TEST-03 and TEST-04 remain protected while path utilities replace duplicated guard code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-/plan-derived target paths -> migrate/sync writes | path traversal or forged canonical roots can redirect writes outside `.opsx/**` |
| glob-special fixture names -> literal parsing helpers | special characters can widen matching unexpectedly if the new helpers treat them as raw patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | `lib/sync.js` | mitigate | Keep the exact `repoRoot/.opsx/specs` canonical-dir guard and no-partial-write behavior while swapping only the containment helper implementation. |
| T-08-08 | Elevation of Privilege | `lib/migrate.js` / `lib/path-utils.js` | mitigate | Reuse one shared `ensureWithinBase()` policy so migration writes cannot escape canonical repo or home roots. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-paths.js`.
- Run `npm run test:workflow-runtime`.
- Run `npm test`.
- Grep `lib/migrate.js`, `lib/sync.js`, and `scripts/test-workflow-paths.js` for the shared-helper imports and exact guard test names.
</verification>

<success_criteria>
- Shared path/glob utilities now protect both read-only and write-sensitive surfaces.
- Sync and migration path guards stay hardened while their duplicated logic is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stability-json-and-release-coverage/08-04-SUMMARY.md`
</output>
