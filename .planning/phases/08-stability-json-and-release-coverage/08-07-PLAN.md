---
phase: 08-stability-json-and-release-coverage
plan: 07
type: execute
wave: 7
depends_on:
  - 08-06
files_modified:
  - docs/release-checklist.md
  - README.md
  - README-zh.md
  - docs/commands.md
  - docs/runtime-guidance.md
  - CHANGELOG.md
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Release-facing docs describe `opsx status --json`, the path/glob hardening boundary, and `npm test` as the total preflight entrypoint."
    - "`docs/release-checklist.md` contains the exact final release gate commands, including the local npm cache override, `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8`."
    - "The final plan verifies the documented release gate, including schema drift and code review, against the now-split suite before publication."
  artifacts:
    - path: "docs/release-checklist.md"
      provides: "Canonical v3.0 prepublish checklist with exact commands and pass criteria, including schema drift, code review, and final phase verification"
      contains: "npm_config_cache=.npm-cache npm pack --dry-run --json"
    - path: "docs/commands.md"
      provides: "Documented `status --json` contract and explicit public-route constraints"
      contains: "status --json"
    - path: "docs/runtime-guidance.md"
      provides: "Documented runtime/path-glob behavior and release preflight commands"
      contains: "npm test"
  key_links:
    - from: "docs/release-checklist.md"
      to: "scripts/test-workflow-package.js"
      via: "the checklist references the same package/tarball release gate commands"
      pattern: "npm pack --dry-run --json"
    - from: "docs/commands.md"
      to: "lib/cli.js"
      via: "the documented JSON status contract mirrors the shipped CLI behavior"
      pattern: "status --json"
    - from: "docs/runtime-guidance.md"
      to: "lib/path-utils.js"
      via: "the documented path/glob stability notes match the shared utility layer"
      pattern: "path"
---

<objective>
Publish the Phase 8 operator-facing contract and close the phase with a documented release checklist.

Purpose: implement D-15 and make the final release verification reproducible by documenting the exact shipped behavior and the exact commands that must pass before publication.
Output: release checklist, refreshed README/docs/changelog text, and final documented verification commands bound to the Phase 8 automation.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
@.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
@README.md
@README-zh.md
@docs/commands.md
@docs/runtime-guidance.md
@CHANGELOG.md

<interfaces>
From the shipped release gate after 08-06:
```text
npm test
node bin/opsx.js --help
node bin/opsx.js --version
node bin/opsx.js check
node bin/opsx.js doc
node bin/opsx.js status
node bin/opsx.js status --json
npm_config_cache=.npm-cache npm pack --dry-run --json
node scripts/check-phase1-legacy-allowlist.js
gsd-sdk query verify.schema-drift 08
$gsd-code-review 8
$gsd-verify-work 8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README and command/runtime docs to match the shipped Phase 8 contract</name>
  <files>
README.md
README-zh.md
docs/commands.md
docs/runtime-guidance.md
CHANGELOG.md
  </files>
  <read_first>
README.md
README-zh.md
docs/commands.md
docs/runtime-guidance.md
CHANGELOG.md
package.json
lib/cli.js
lib/path-utils.js
lib/glob-utils.js
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
  </read_first>
  <action>Per D-15, D-16, and the locked release-checklist location, update the release-facing docs to match the shipped Phase 8 behavior. `README.md` and `README-zh.md` must document `npm test` as the total preflight entrypoint and preserve the explicit `$opsx-*` / `/opsx-*` public-route contract. `docs/commands.md` must document `opsx status --json` as a machine-readable envelope with the locked top-level keys, the locked `ok: true` transport-success semantics, and expected exit-0 semantics for missing workspace/active change and migration candidates. The docs must make clear that workflow readiness problems live in `workspace`, `migration`, `changeStatus`, `warnings`, and `errors`. `docs/runtime-guidance.md` must explain the path/glob stabilization boundary at a high level, including repo-local `lib/path-utils.js` / `lib/glob-utils.js` and the fact that literal glob-special artifact names are escaped predictably. Update `CHANGELOG.md` with concise v3.0 Phase 8 release-hardening notes; do not revive legacy public entrypoints or standalone `$opsx` wording anywhere.</action>
  <acceptance_criteria>
- `README.md` and `README-zh.md` mention `npm test`.
- `docs/commands.md` mentions `status --json`, the JSON-envelope behavior, and the locked `ok: true` transport semantics.
- `docs/runtime-guidance.md` mentions the shared path/glob utility layer.
- `CHANGELOG.md` contains a Phase 8 / release-hardening note for v3.0.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "npm test|status --json|ok: true|workspace|migration|changeStatus|warnings|errors|path-utils|glob-utils|\\$opsx-|/opsx-" README.md README-zh.md docs/commands.md docs/runtime-guidance.md CHANGELOG.md</automated>
  </verify>
  <done>
- The operator-facing docs now match the Phase 8 runtime, test, and release contracts.
- No release-facing documentation reintroduces banned public routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the canonical release checklist and verify the documented final gate</name>
  <files>
docs/release-checklist.md
CHANGELOG.md
  </files>
  <read_first>
docs/release-checklist.md
README.md
README-zh.md
docs/commands.md
docs/runtime-guidance.md
scripts/test-workflow-package.js
scripts/test-workflow-generation.js
scripts/test-workflow-state.js
scripts/test-workflow-paths.js
scripts/test-workflow-gates.js
  </read_first>
  <action>Create `docs/release-checklist.md` as the locked Phase 8 release checklist location. Write the checklist as executable prepublish steps with exact commands and expected outcomes, including `npm test`, `node bin/opsx.js --help`, `node bin/opsx.js --version`, `node bin/opsx.js check`, `node bin/opsx.js doc`, `node bin/opsx.js status`, `node bin/opsx.js status --json`, `npm_config_cache=.npm-cache npm pack --dry-run --json`, `node scripts/check-phase1-legacy-allowlist.js`, `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8`. Include short pass/fail expectations for each step and a reminder that true exceptional JSON failures may use stderr/non-zero exit while expected workflow states must not, while expected workflow or readiness issues remain encoded in the JSON envelope instead of flipping `ok` to false. If `CHANGELOG.md` needs a release-callout bullet or checklist link, add it here rather than in a separate plan.</action>
  <acceptance_criteria>
- `docs/release-checklist.md` exists and contains the exact command string `npm_config_cache=.npm-cache npm pack --dry-run --json`.
- The checklist includes `opsx status --json`, `node scripts/check-phase1-legacy-allowlist.js`, `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8`.
- The final documented gate runs cleanly under the verify commands below.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <automated>node bin/opsx.js --help</automated>
    <automated>node bin/opsx.js --version</automated>
    <automated>node bin/opsx.js check</automated>
    <automated>node bin/opsx.js doc</automated>
    <automated>node bin/opsx.js status</automated>
    <automated>node bin/opsx.js status --json</automated>
    <automated>npm_config_cache=.npm-cache npm pack --dry-run --json</automated>
    <automated>node scripts/check-phase1-legacy-allowlist.js</automated>
    <automated>gsd-sdk query verify.schema-drift 08</automated>
    <automated>$gsd-code-review 8</automated>
    <automated>$gsd-verify-work 8</automated>
    <automated>rg -n "npm_config_cache=.npm-cache npm pack --dry-run --json|status --json|check-phase1-legacy-allowlist|verify\\.schema-drift 08|\\$gsd-code-review 8|\\$gsd-verify-work 8" docs/release-checklist.md CHANGELOG.md</automated>
  </verify>
  <done>
- The v3.0 release gate is documented, reproducible, and verified against the shipped Phase 8 automation, including schema drift, code review, and final phase verification.
- Phase 8 closes with one canonical checklist instead of scattered manual notes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release documentation -> operator action | stale or incomplete docs can cause publication without the full Phase 8 gate actually running |
| documented JSON contract -> automation users | incorrect docs can make operators or scripts expect stderr/exit semantics that the CLI does not ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Repudiation | `docs/release-checklist.md` | mitigate | Write one canonical checklist with exact commands and verify those same commands in the plan before calling the phase ready for release. |
| T-08-14 | Information Integrity | README/docs/changelog | mitigate | Update the bilingual docs and runtime guidance directly from the shipped Phase 8 behavior so operator instructions match the actual CLI/test surface. |
</threat_model>

<verification>
- Run the full documented release gate.
- Grep the refreshed docs and checklist for `npm test`, `status --json`, the locked `ok: true` semantics, the shared path/glob layer, the exact local npm-cache pack command, and the schema-drift/code-review/final-verification commands.
</verification>

<success_criteria>
- All release-facing docs match the shipped Phase 8 behavior.
- The project has one canonical `docs/release-checklist.md` with the exact verified release gate commands, including schema drift, code review, and final phase verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stability-json-and-release-coverage/08-07-SUMMARY.md`
</output>
