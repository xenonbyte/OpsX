---
phase: 08-stability-json-and-release-coverage
plan: 06
type: execute
wave: 6
depends_on:
  - 08-05
files_modified:
  - scripts/test-workflow-package.js
  - scripts/test-workflow-generation.js
autonomous: true
requirements:
  - QUAL-05
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "The release gate asserts tarball contents and metadata through `npm_config_cache=.npm-cache npm pack --dry-run --json` instead of a custom pack simulator."
    - "CLI help/version/check/doc/status/status-json smoke coverage is part of the package topic script."
    - "Generated parity and legacy public-surface gates remain part of the generation topic script and block stale entrypoints."
    - "The Phase 8 release gate explicitly carries forward `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8` after the split-script checks."
  artifacts:
    - path: "scripts/test-workflow-package.js"
      provides: "Release package/tarball/CLI smoke assertions"
      contains: "npm_config_cache=.npm-cache npm pack --dry-run --json"
    - path: "scripts/test-workflow-generation.js"
      provides: "Release parity and legacy public-surface gate assertions"
      contains: "BANNED_PUBLIC_ROUTE_STRINGS"
  key_links:
    - from: "scripts/test-workflow-package.js"
      to: "package.json"
      via: "pack dry-run assertions validate the shipped tarball surface against package metadata"
      pattern: "npm pack --dry-run --json"
    - from: "scripts/test-workflow-package.js"
      to: "bin/opsx.js"
      via: "CLI smoke assertions prove the shipped binary still honors help/version/check/doc/status/status-json flows"
      pattern: "status --json"
    - from: "scripts/test-workflow-generation.js"
      to: "commands/**"
      via: "generation assertions keep the public entrypoint contract free of legacy or banned routes"
      pattern: "BANNED_PUBLIC_ROUTE_STRINGS"
---

<objective>
Lock the Phase 8 release gate into the split topic scripts before documentation is refreshed.

Purpose: implement D-13 and D-14 directly in automated tests so the v3.0 release boundary is checkable before publication, while explicitly preserving the schema-drift, code-review, and final verification steps that complete the release gate.
Output: package/tarball/CLI smoke release assertions in the package topic script and final public-surface/parity release assertions in the generation topic script.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
@.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
@.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
@package.json
@scripts/test-workflow-package.js
@scripts/test-workflow-generation.js
@scripts/check-phase1-legacy-allowlist.js

<interfaces>
From `scripts/test-workflow-package.js` after 08-01:
```javascript
function registerTests(test, helpers) {}
```

From `scripts/test-workflow-generation.js` after 08-01:
```javascript
function registerTests(test, helpers) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tarball and CLI smoke release assertions to the package topic script</name>
  <files>
scripts/test-workflow-package.js
  </files>
  <read_first>
scripts/test-workflow-package.js
package.json
bin/opsx.js
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
  </read_first>
  <action>Per D-13 and D-14, extend `scripts/test-workflow-package.js` so the package topic script becomes the release package/smoke gate. Add an assertion that runs `npm_config_cache=.npm-cache npm pack --dry-run --json` and verifies the JSON payload covers the expected package metadata, `@xenonbyte/opsx` name, `opsx` binary mapping, `bin/`, `lib/`, `scripts/`, `commands/`, `skills/`, `schemas/`, `templates/`, `config/`, `docs/`, and `README.md`/`CHANGELOG.md` surface without relying on the default global npm cache. Add CLI smoke assertions for `node bin/opsx.js --help`, `--version`, `check`, `doc`, `status`, and `status --json`, make the status-JSON smoke parse stdout rather than string-matching human text, and treat `ok: true` as transport success rather than workspace readiness per the locked Phase 8 contract. Keep these as package-topic assertions instead of creating a sixth topic file.</action>
  <acceptance_criteria>
- `scripts/test-workflow-package.js` contains the exact command string `npm_config_cache=.npm-cache npm pack --dry-run --json`.
- `scripts/test-workflow-package.js` covers `--help`, `--version`, `check`, `doc`, `status`, and `status --json`.
- The package topic script treats `ok: true` as transport success and validates readiness/diagnostic fields separately inside the JSON envelope.
- `node scripts/test-workflow-package.js`, `npm test`, and `npm_config_cache=.npm-cache npm pack --dry-run --json` all pass.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-package.js</automated>
    <automated>npm test</automated>
    <automated>npm_config_cache=.npm-cache npm pack --dry-run --json</automated>
    <automated>rg -n "npm_config_cache=.npm-cache npm pack --dry-run --json|--help|--version|status --json|opsx check|opsx doc" scripts/test-workflow-package.js</automated>
  </verify>
  <done>
- TEST-01 is enforced through the actual packed tarball and shipped CLI surface.
- The Phase 8 release gate no longer depends on manual tarball inspection or the host global npm cache.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add final parity and legacy public-surface release gates to the generation topic script</name>
  <files>
scripts/test-workflow-generation.js
  </files>
  <read_first>
scripts/test-workflow-generation.js
scripts/check-phase1-legacy-allowlist.js
lib/generator.js
commands/codex/prompts/opsx.md
.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
  </read_first>
  <action>Per D-13, D-16, and D-19, extend `scripts/test-workflow-generation.js` so the generation topic script becomes the release public-surface gate. Keep generated-bundle and checked-in parity assertions active across Claude/Codex/Gemini, then add a release-level legacy public-surface assertion that reuses `scripts/check-phase1-legacy-allowlist.js` or equivalent grep-based logic to prove no public `/prompts:*`, `/opsx:*`, standalone `$opsx`, `$opsx <request>`, or stale OpenSpec surfaces reappear in shipped docs/commands/help output. Keep this plan explicitly aligned to the full D-13 release gate by preserving the post-script checks `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8` for the final release checklist. Do not hand-edit generated files as part of this plan; keep the source-of-truth path in `lib/generator.js` and bundle parity assertions.</action>
  <acceptance_criteria>
- `scripts/test-workflow-generation.js` still contains `runtime suite locks renamed skill targets, generated bundles, and checked-in command entries`.
- The generation topic script now runs or reuses the legacy public-surface allowlist gate.
- The plan explicitly preserves `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8` as required final release-gate steps after the split-script assertions.
- `node scripts/test-workflow-generation.js` and `npm test` pass with the release public-surface checks enabled.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-generation.js</automated>
    <automated>npm test</automated>
    <automated>node scripts/check-phase1-legacy-allowlist.js</automated>
    <automated>gsd-sdk query verify.schema-drift 08</automated>
    <automated>$gsd-code-review 8</automated>
    <automated>$gsd-verify-work 8</automated>
    <automated>rg -n "runtime suite locks renamed skill targets, generated bundles, and checked-in command entries|BANNED_PUBLIC_ROUTE_STRINGS|legacy allowlist|openspec|\\$opsx <request>|/opsx:\\*" scripts/test-workflow-generation.js</automated>
  </verify>
  <done>
- TEST-02 remains a hard release gate instead of a best-effort regression check.
- The hard clean break stays enforced across the shipped command surface before docs are refreshed, and the remaining schema-drift, code-review, and final verification steps stay explicit in the Phase 8 release gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged tarball -> published release | tarball contents can drift from repo expectations if the release gate does not inspect the actual npm pack surface |
| generated command surface -> public docs/help | legacy or banned public entrypoints can reappear even when local runtime tests still pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Integrity | `scripts/test-workflow-package.js` | mitigate | Use `npm_config_cache=.npm-cache npm pack --dry-run --json` as the authoritative packed-surface check and smoke the shipped CLI commands directly. |
| T-08-12 | Spoofing | `scripts/test-workflow-generation.js` / shipped commands | mitigate | Re-run parity plus legacy public-surface gates so banned entrypoints cannot masquerade as valid public routes at release time. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-package.js`.
- Run `node scripts/test-workflow-generation.js`.
- Run `npm test`.
- Run `npm_config_cache=.npm-cache npm pack --dry-run --json`.
- Run `node scripts/check-phase1-legacy-allowlist.js`.
- Run `gsd-sdk query verify.schema-drift 08`.
- Run `$gsd-code-review 8`.
- Run `$gsd-verify-work 8`.
</verification>

<success_criteria>
- The split topic scripts cover the tarball, CLI smoke, parity, and legacy public-surface portions of the Phase 8 release gate.
- The full Phase 8 release gate explicitly includes `gsd-sdk query verify.schema-drift 08`, `$gsd-code-review 8`, and `$gsd-verify-work 8` before documentation is called release-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stability-json-and-release-coverage/08-06-SUMMARY.md`
</output>
