---
phase: 08-stability-json-and-release-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/test-workflow-runtime.js
  - scripts/test-workflow-shared.js
  - scripts/test-workflow-package.js
  - scripts/test-workflow-generation.js
  - scripts/test-workflow-state.js
  - scripts/test-workflow-paths.js
  - scripts/test-workflow-gates.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "`npm test` becomes the total release/preflight entrypoint while `npm run test:workflow-runtime` stays as the compatibility aggregate entrypoint."
    - "Package/bin metadata assertions and public-route generation assertions live in dedicated topic scripts instead of only inside the monolith."
    - "The split contract is explicit and deterministic: shared helpers are centralized once, and every topic script exposes the same registration API for later plans."
  artifacts:
    - path: "package.json"
      provides: "Release-total `npm test` entrypoint while preserving `test:workflow-runtime`"
      contains: "\"test\": \"node scripts/test-workflow-runtime.js\""
    - path: "scripts/test-workflow-shared.js"
      provides: "Shared fixture, CLI, parity, and hash helpers for the split runtime suite"
      contains: "module.exports = {"
    - path: "scripts/test-workflow-package.js"
      provides: "TEST-01 topic assertions for package/bin/runtime metadata"
      contains: "function registerTests"
    - path: "scripts/test-workflow-generation.js"
      provides: "TEST-02 topic assertions for generated command parity and banned public surfaces"
      contains: "function registerTests"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Compatibility aggregate runner that invokes topic registration in stable order"
      contains: "registerTests"
  key_links:
    - from: "package.json"
      to: "scripts/test-workflow-runtime.js"
      via: "`npm test` and `npm run test:workflow-runtime` both dispatch through the same aggregate entrypoint"
      pattern: "test:workflow-runtime|\"test\""
    - from: "scripts/test-workflow-runtime.js"
      to: "scripts/test-workflow-package.js"
      via: "aggregate runner calls the package topic registration function"
      pattern: "test-workflow-package"
    - from: "scripts/test-workflow-runtime.js"
      to: "scripts/test-workflow-generation.js"
      via: "aggregate runner calls the generation topic registration function"
      pattern: "test-workflow-generation"
---

<objective>
Create the reviewable test-split seam and move TEST-01 / TEST-02 coverage out of the monolithic runtime suite.

Purpose: satisfy D-09 through D-12 without a one-shot risky rewrite by first centralizing shared helpers, then moving package and command-surface assertions into bounded topic scripts.
Output: shared test harness, topic-script registration contract, package/generation topic modules, placeholder topic files for later plans, and a stable `npm test` aggregate entrypoint.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
@.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
@.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
@.planning/phases/08-stability-json-and-release-coverage/08-PATTERNS.md
@package.json
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
const { buildPlatformBundle } = require('./generator');
```

From `scripts/test-workflow-runtime.js` before the split:
```javascript
function collectBundleParity(platform, generatedBundle) {}
function runOpsxCli(args, options = {}) {}
function createFixtureRepo() {}
function createLegacyMigrationRepoFixture(options = {}) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared workflow-test harness and create deterministic topic entrypoints</name>
  <files>
scripts/test-workflow-shared.js
scripts/test-workflow-runtime.js
scripts/test-workflow-state.js
scripts/test-workflow-paths.js
scripts/test-workflow-gates.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
package.json
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
.planning/phases/08-stability-json-and-release-coverage/08-PATTERNS.md
  </read_first>
  <action>Per D-09, D-10, and D-11, extract the shared helpers used by the current 109-test monolith into `scripts/test-workflow-shared.js`. The shared module must keep CommonJS and Node `>=14.14.0` compatibility and centralize fixture creation, CLI spawning, parity inspection, file-list helpers, and hash utilities that more than one topic will reuse. Refactor `scripts/test-workflow-runtime.js` into the compatibility aggregate entrypoint, but do not remove it. Give every topic file the same explicit API: export `registerTests(test, helpers)` and make the aggregate runner invoke them in a fixed order. Create `scripts/test-workflow-state.js`, `scripts/test-workflow-paths.js`, and `scripts/test-workflow-gates.js` as real CommonJS modules with empty `registerTests()` placeholders so later plans can populate them without inventing a second contract. Do not add a new test framework, dynamic glob loader, or missing-file skip path.</action>
  <acceptance_criteria>
- `scripts/test-workflow-shared.js` exists and exports named shared helpers through `module.exports`.
- `scripts/test-workflow-runtime.js` imports the shared module and the topic modules through explicit `require(...)` calls.
- `scripts/test-workflow-state.js`, `scripts/test-workflow-paths.js`, and `scripts/test-workflow-gates.js` each export `registerTests`.
- `npm run test:workflow-runtime` still passes after the harness extraction.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "module\\.exports = \\{|registerTests|test-workflow-shared|test-workflow-state|test-workflow-paths|test-workflow-gates" scripts/test-workflow-runtime.js scripts/test-workflow-shared.js scripts/test-workflow-state.js scripts/test-workflow-paths.js scripts/test-workflow-gates.js</automated>
  </verify>
  <done>
- The split suite has one shared helper layer and one stable topic registration contract.
- Future topic migrations no longer need to copy fixture and CLI helper code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move package and command-surface assertions into dedicated topic scripts and wire `npm test`</name>
  <files>
package.json
scripts/test-workflow-runtime.js
scripts/test-workflow-package.js
scripts/test-workflow-generation.js
  </files>
  <read_first>
package.json
scripts/test-workflow-runtime.js
scripts/check-phase1-legacy-allowlist.js
lib/generator.js
.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-RESEARCH.md
  </read_first>
  <action>Per D-10, D-12, D-13, D-16, and D-19, create `scripts/test-workflow-package.js` and `scripts/test-workflow-generation.js` and move the existing TEST-01 / TEST-02 assertions out of `scripts/test-workflow-runtime.js` into those files. `scripts/test-workflow-package.js` must own the package/bin metadata, Node-14/CommonJS surface checks, and shipped CLI/package assertions that belong to the published package contract. `scripts/test-workflow-generation.js` must own generated bundle parity, checked-in command parity, banned public-entrypoint strings, and explicit `$opsx-*` / `/opsx-*` public-surface assertions. Update `scripts/test-workflow-runtime.js` to delegate those assertions to the two topic modules and keep later state/path/gate coverage in place until later plans move them. Add `"test": "node scripts/test-workflow-runtime.js"` to `package.json` while preserving the existing `"test:workflow-runtime"` command as the same aggregate entrypoint. Do not add Jest, Vitest, Tap, or any other dependency.</action>
  <acceptance_criteria>
- `package.json` contains both `"test"` and `"test:workflow-runtime"` and both point at `node scripts/test-workflow-runtime.js`.
- `scripts/test-workflow-package.js` contains the exact test name `declared Node 14 engine floor uses compatible CommonJS builtin imports`.
- `scripts/test-workflow-generation.js` contains the exact test name `runtime suite locks renamed skill targets, generated bundles, and checked-in command entries`.
- `npm test`, `node scripts/test-workflow-package.js`, and `node scripts/test-workflow-generation.js` pass.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-package.js</automated>
    <automated>node scripts/test-workflow-generation.js</automated>
    <automated>npm test</automated>
    <automated>rg -n "\"test\": \"node scripts/test-workflow-runtime\\.js\"|\"test:workflow-runtime\": \"node scripts/test-workflow-runtime\\.js\"|declared Node 14 engine floor uses compatible CommonJS builtin imports|runtime suite locks renamed skill targets, generated bundles, and checked-in command entries" package.json scripts/test-workflow-package.js scripts/test-workflow-generation.js</automated>
  </verify>
  <done>
- TEST-01 and TEST-02 are enforced through dedicated topic scripts instead of only through the monolith.
- The repo now has a release-total `npm test` entrypoint without breaking existing `npm run test:workflow-runtime` usage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published package surface -> split test suite | TEST-01 / TEST-02 coverage can silently regress if package/bin and public-route assertions are lost during the split |
| aggregate runner -> topic modules | inconsistent topic APIs or missing registration calls can hide whole assertion groups while `npm test` still exits zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `package.json` / topic scripts | mitigate | Keep `npm test` and `npm run test:workflow-runtime` pointed at the same aggregate runner and move TEST-01 / TEST-02 assertions into explicit topic files with exact test-name greps. |
| T-08-02 | Information Integrity | `scripts/test-workflow-runtime.js` | mitigate | Freeze one explicit `registerTests(test, helpers)` contract and require the aggregate runner to import topic files in a deterministic order instead of using dynamic discovery or silent skipping. |
</threat_model>

<verification>
- Run `npm test`.
- Run `node scripts/test-workflow-package.js`.
- Run `node scripts/test-workflow-generation.js`.
- Grep `package.json` and the new topic files for the exact entrypoint strings and preserved test names.
</verification>

<success_criteria>
- The Phase 8 test split starts with a shared harness and explicit topic boundaries.
- Package/bin metadata and command-surface coverage are no longer trapped inside the monolithic runtime script.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stability-json-and-release-coverage/08-01-SUMMARY.md`
</output>
