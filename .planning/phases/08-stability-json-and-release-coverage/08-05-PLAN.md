---
phase: 08-stability-json-and-release-coverage
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - scripts/test-workflow-runtime.js
  - scripts/test-workflow-gates.js
autonomous: true
requirements:
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Spec-review, hidden-requirement, TDD-light, verify, sync, archive, and batch-gate coverage lives in the dedicated gates topic script."
    - "`scripts/test-workflow-runtime.js` finishes the split as a compatibility aggregate runner rather than a second monolith."
    - "The split preserves gate coverage and total-suite pass/fail reporting instead of losing assertions during file moves."
  artifacts:
    - path: "scripts/test-workflow-gates.js"
      provides: "TEST-04 topic assertions for spec review, TDD, verify/sync/archive, and batch gates"
      contains: "verify gate blocks forbidden paths unresolved drift and incomplete task groups"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Pure aggregate runner for package, generation, state, paths, and gates topics"
      contains: "test-workflow-gates"
  key_links:
    - from: "scripts/test-workflow-gates.js"
      to: "lib/verify.js"
      via: "gate-topic assertions lock verify findings and accepted-state behavior"
      pattern: "verify gate"
    - from: "scripts/test-workflow-gates.js"
      to: "lib/sync.js"
      via: "gate-topic assertions keep sync conflict and no-partial-write coverage"
      pattern: "sync plan"
    - from: "scripts/test-workflow-runtime.js"
      to: "scripts/test-workflow-gates.js"
      via: "aggregate runner delegates the remaining gate coverage to the topic module"
      pattern: "test-workflow-gates"
---

<objective>
Drain the remaining gate coverage out of the monolith and finish the topic-script split.

Purpose: complete D-09 through D-12 cleanly by moving the remaining Phase 5-7 gate assertions into one bounded file and leaving `scripts/test-workflow-runtime.js` as a compatibility/aggregate runner only.
Output: dedicated gates topic script plus a slim aggregate entrypoint that still powers both `npm test` and `npm run test:workflow-runtime`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
@.planning/phases/08-stability-json-and-release-coverage/08-CONTEXT.md
@scripts/test-workflow-runtime.js
@scripts/test-workflow-gates.js

<interfaces>
From `lib/verify.js`:
```javascript
const { evaluateVerifyGate, acceptVerifyGate } = require('./verify');
```

From `lib/sync.js`:
```javascript
const { planSync, applySyncPlan, acceptSyncPlan } = require('./sync');
```

From `lib/archive.js` and batch helpers already covered by the monolith:
```javascript
// Preserve the existing exported gate and batch entrypoints already exercised by Phase 7 tests.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move spec-review, TDD-light, verify, sync, archive, and batch assertions into `test-workflow-gates.js`</name>
  <files>
scripts/test-workflow-runtime.js
scripts/test-workflow-gates.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
scripts/test-workflow-gates.js
.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/08-stability-json-and-release-coverage/08-VALIDATION.md
  </read_first>
  <action>Move the remaining gate-oriented assertions out of `scripts/test-workflow-runtime.js` into `scripts/test-workflow-gates.js`. Preserve the exact existing test names for the critical Phase 5-7 behaviors, including `verify gate blocks forbidden paths unresolved drift and incomplete task groups`, `sync plan blocks duplicate requirement ids and writes nothing`, `archiveChange moves a fully synced change into .opsx archive using the exact change name`, and `execution checkpoint records completed tdd steps diff summary and drift status`. Keep spec-review hidden-requirement coverage, TDD warning/block coverage, verify/sync/archive gate coverage, and batch isolation coverage together in this file; do not move path-only fixtures back out of `scripts/test-workflow-paths.js`.</action>
  <acceptance_criteria>
- `scripts/test-workflow-gates.js` contains the exact four critical test names listed in the action.
- The corresponding assertions are removed from `scripts/test-workflow-runtime.js` so the topic script becomes the source of truth.
- `node scripts/test-workflow-gates.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/test-workflow-gates.js</automated>
    <automated>rg -n "verify gate blocks forbidden paths unresolved drift and incomplete task groups|sync plan blocks duplicate requirement ids and writes nothing|archiveChange moves a fully synced change into \\.opsx archive using the exact change name|execution checkpoint records completed tdd steps diff summary and drift status" scripts/test-workflow-gates.js</automated>
  </verify>
  <done>
- TEST-04 coverage is isolated in its own topic file.
- No critical Phase 5-7 gate assertion remains trapped only inside the compatibility runner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finish `test-workflow-runtime.js` as a pure compatibility aggregate runner</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
scripts/test-workflow-package.js
scripts/test-workflow-generation.js
scripts/test-workflow-state.js
scripts/test-workflow-paths.js
scripts/test-workflow-gates.js
  </read_first>
  <action>After the gate assertions are moved, remove the leftover domain-specific test bodies from `scripts/test-workflow-runtime.js` and leave it as the compatibility/aggregate runner only. It must still call the package, generation, state, paths, and gates topic registrations in stable order; it must still report one aggregate total/failure count for `npm test` and `npm run test:workflow-runtime`; and it must not reintroduce a second copy of any moved assertion. Keep the aggregate entrypoint narrow and CommonJS, not a new framework or orchestrator.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` contains the topic-module imports and aggregate harness, but no longer contains the moved gate-domain test names.
- `npm run test:workflow-runtime` and `npm test` both pass with the aggregate runner driving the five topic files.
- The aggregate runner still prints one final total pass/fail summary.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>npm test</automated>
    <automated>rg -n "test-workflow-package|test-workflow-generation|test-workflow-state|test-workflow-paths|test-workflow-gates" scripts/test-workflow-runtime.js</automated>
    <automated>rg -n "verify gate blocks forbidden paths unresolved drift and incomplete task groups|sync plan blocks duplicate requirement ids and writes nothing|archiveChange moves a fully synced change into \\.opsx archive using the exact change name" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The monolith split is complete and reviewable.
- `scripts/test-workflow-runtime.js` remains only as the compatibility/aggregate entrypoint required by the locked planning decision.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| split test files -> aggregate release gate | moved assertions can disappear or be duplicated if the aggregate runner and topic scripts drift during the final drain |
| gate coverage -> future release verification | losing verify/sync/archive/TDD/spec-review coverage would make later release checks falsely look complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | `scripts/test-workflow-gates.js` | mitigate | Preserve exact critical gate test names while moving them, so grep and topic execution can prove the assertions survived the split intact. |
| T-08-10 | Information Integrity | `scripts/test-workflow-runtime.js` | mitigate | Strip moved assertions from the aggregate runner and keep it responsible only for ordered topic registration and one final pass/fail summary. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-gates.js`.
- Run `npm run test:workflow-runtime`.
- Run `npm test`.
- Grep the gates topic file for the preserved critical test names and grep the aggregate runner to confirm those names were removed there.
</verification>

<success_criteria>
- TEST-03 / TEST-04 gate coverage survives the split into a dedicated topic script.
- The compatibility runner is now a true aggregate entrypoint instead of a shadow monolith.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stability-json-and-release-coverage/08-05-SUMMARY.md`
</output>
