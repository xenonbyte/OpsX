---
phase: 04-change-state-machine-and-drift-control
plan: 09
type: execute
wave: 6
depends_on:
  - 04-06
  - 04-07
  - 04-08
files_modified:
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-07
  - STATE-08
must_haves:
  truths:
    - "The final runtime gate enforces checked-in parity again after all Phase 4 stateful refresh slices land."
    - "Generated stateful files across Claude, Codex, and Gemini now stay locked to the same Phase 4 durable-state contract."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Final repo-wide parity and stateful-surface assertions for Phase 4"
      contains: "checkedInEntries"
  key_links:
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/**"
      via: "full parity gate restored after all three stateful refresh slices land"
      pattern: "buildPlatformBundle|checkedInEntries"
---

<objective>
Restore the final generated-bundle parity gate after the bounded Phase 4 refresh slices are complete.

Purpose: close the phase with the same strict checked-in parity guarantee that Phase 3 activated, now extended to the Phase 4 stateful command copy.
Output: full parity restored in `scripts/test-workflow-runtime.js` and a green final runtime suite.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-change-state-machine-and-drift-control/04-05-PLAN.md
@.planning/phases/04-change-state-machine-and-drift-control/04-06-PLAN.md
@.planning/phases/04-change-state-machine-and-drift-control/04-07-PLAN.md
@.planning/phases/04-change-state-machine-and-drift-control/04-08-PLAN.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the repo-wide checked-in parity gate for Phase 4 stateful command files</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
.planning/phases/04-change-state-machine-and-drift-control/04-05-PLAN.md
.planning/phases/04-change-state-machine-and-drift-control/04-06-PLAN.md
.planning/phases/04-change-state-machine-and-drift-control/04-07-PLAN.md
.planning/phases/04-change-state-machine-and-drift-control/04-08-PLAN.md
  </read_first>
  <action>Remove the temporary Phase 4 stateful-action checked-in parity exemption that 04-05 introduced and restore the final gate to strict repo-wide parity. `scripts/test-workflow-runtime.js` must again compare every checked-in `commands/**` file to `buildPlatformBundle()` output, including the 24 stateful action files refreshed in 04-06 through 04-08. Keep the Phase 4 behavioral assertions for `do not refresh stored hashes from read-only routes` and `Execute exactly one top-level task group by default` active. Do not weaken other Phase 3 public-surface or parity assertions while reactivating the final gate.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` no longer exempts the Phase 4 stateful action files from checked-in parity.
- `scripts/test-workflow-runtime.js` still asserts the exact phrases `do not refresh stored hashes from read-only routes` and `Execute exactly one top-level task group by default`.
- `npm run test:workflow-runtime` passes with full checked-in parity re-enabled.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "checkedInEntries|do not refresh stored hashes from read-only routes|Execute exactly one top-level task group by default" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The final Phase 4 runtime gate is strict again.
- Generated command copy and source-of-truth metadata are fully synchronized across all platforms.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime suite -> shipped generated files | leaving the temporary parity exception in place would allow silent drift to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | final parity gate | mitigate | Remove the temporary Phase 4 exception and require full `buildPlatformBundle()` parity again. |
| T-04-07 | Tampering | multi-platform `apply` guidance | mitigate | Keep explicit one-group apply assertions active while restoring repo-wide parity. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the runtime suite for the restored parity gate and the exact Phase 4 behavior strings.
</verification>

<success_criteria>
- Full generated parity is re-enabled after the Phase 4 stateful refresh slices land.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-09-SUMMARY.md`
</output>
