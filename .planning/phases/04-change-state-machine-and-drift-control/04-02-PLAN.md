---
phase: 04-change-state-machine-and-drift-control
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - lib/workspace.js
  - lib/change-store.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - STATE-01
  - STATE-04
must_haves:
  truths:
    - "`opsx-new` creates the complete per-change skeleton and selects it as the active change."
    - "New change scaffolds start at `INIT` even though placeholder planning files already exist."
    - "`state.yaml`, `context.md`, and `drift.md` are created with the Phase 4 schema rather than the Phase 2 placeholder shape."
  artifacts:
    - path: "lib/workspace.js"
      provides: "Narrow new-change skeleton creator"
      contains: "function createChangeSkeleton"
    - path: "lib/change-store.js"
      provides: "State and active-pointer writes for new changes"
      contains: "writeActiveChangePointer"
    - path: "scripts/test-workflow-runtime.js"
      provides: "New-change skeleton regression coverage"
      contains: "opsx-new skeleton creates placeholder files, active pointer, and INIT stage"
  key_links:
    - from: "lib/workspace.js"
      to: "lib/change-store.js"
      via: "skeleton creation writes change files and then persists active/state artifacts"
      pattern: "createChangeSkeleton|writeActiveChangePointer|writeChangeState"
---

<objective>
Ship the narrow `opsx-new` file-system behavior required by Phase 4 without turning the CLI into a broad workflow engine.

Purpose: satisfy STATE-01, make new changes resumable from disk immediately, and keep placeholder artifacts honest by pairing them with `INIT` stage state.
Output: `createChangeSkeleton()` plus green tests covering active-change selection and new-change scaffolds.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
@.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
@.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
@.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-01-PLAN.md

<interfaces>
From `lib/workspace.js`:
```javascript
function getCanonicalChangesDir(repoRoot) {}
function getCanonicalActivePath(repoRoot) {}
function writeActiveStateIfMissing(repoRoot, activeChange = '') {}
function writeChangeScaffoldsIfMissing(changeDir, changeName) {}
```

From `templates/project/change-metadata.yaml.tmpl`:
```yaml
name: <change-name>
schema: spec-driven
createdAt: <ISO-8601>
securitySensitive: false
securityWaiver:
  approved: false
  reason: ""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the narrow `opsx-new` skeleton writer and active-change selection path</name>
  <files>
lib/workspace.js
lib/change-store.js
  </files>
  <behavior>
    - A new change directory contains `change.yaml`, `proposal.md`, `design.md`, `tasks.md`, `specs/`, `state.yaml`, `context.md`, and `drift.md`.
    - `.opsx/active.yaml` points at the new change after skeleton creation.
    - Placeholder artifacts do not advance lifecycle state beyond `INIT`.
  </behavior>
  <read_first>
lib/workspace.js
lib/change-store.js
templates/project/change-metadata.yaml.tmpl
skills/opsx/references/artifact-templates.md
.planning/REQUIREMENTS.md
.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
  </read_first>
  <action>Implement D-01, D-03, D-04, and D-17. In `lib/workspace.js`, add `createChangeSkeleton({ repoRoot, changeName, schemaName = 'spec-driven', createdAt, securitySensitive = false })`. Reuse the canonical path helpers already in this file. `createChangeSkeleton()` must create `.opsx/changes/<changeName>/change.yaml` with the exact fields `name`, `schema`, `createdAt`, `securitySensitive`, and `securityWaiver` matching `templates/project/change-metadata.yaml.tmpl`. It must also create these placeholder files with concrete starter content: `proposal.md` containing `# Proposal`, `## Why`, `## What Changes`, and `## Impact`; `design.md` containing `# Design`, `## Context`, `## Approach`, and `## Risks`; `tasks.md` containing `# Tasks`, `## 1. Planning`, and a single unchecked placeholder item `- [ ] 1.1 Replace placeholders with real task groups after planning checkpoints.`; and `specs/README.md` containing `Create capability specs as specs/<capability>/spec.md.`. After creating those artifacts, call the new state-store helpers so `state.yaml`, `context.md`, and `drift.md` use the Phase 4 schema, and update `.opsx/active.yaml` so `activeChange` equals `<changeName>`. Keep the persisted state at `stage: INIT` and `nextAction: Create proposal.md for this change.` even though the placeholder files already exist.</action>
  <acceptance_criteria>
- `lib/workspace.js` exports `createChangeSkeleton`.
- `createChangeSkeleton()` writes `change.yaml`, `proposal.md`, `design.md`, `tasks.md`, `specs/README.md`, `state.yaml`, `context.md`, and `drift.md` under `.opsx/changes/<changeName>/`.
- Placeholder `tasks.md` contains the exact unchecked line `- [ ] 1.1 Replace placeholders with real task groups after planning checkpoints.`.
- The persisted `state.yaml` created for a new change keeps `stage: INIT`.
- `.opsx/active.yaml` stores the exact `activeChange` value for the new change.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "createChangeSkeleton|# Proposal|# Design|# Tasks|Replace placeholders with real task groups after planning checkpoints|Create capability specs as specs/<capability>/spec.md" lib/workspace.js .opsx/changes 2>/dev/null</automated>
  </verify>
  <done>
- New changes become durable, resumable disk artifacts immediately.
- Placeholder planning files no longer depend on Phase 2 scaffold behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime tests that prove the new-change skeleton is complete and still starts at INIT</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - New-change tests assert the full file set exists.
    - The active pointer updates to the new change.
    - The initial state remains `INIT` despite placeholder artifact files.
  </behavior>
  <read_first>
scripts/test-workflow-runtime.js
lib/workspace.js
lib/change-store.js
.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
.planning/phases/04-change-state-machine-and-drift-control/04-01-PLAN.md
  </read_first>
  <action>Extend `scripts/test-workflow-runtime.js` with a test named exactly `opsx-new skeleton creates placeholder files, active pointer, and INIT stage`. The fixture must call the new skeleton helper, then assert that `change.yaml`, `proposal.md`, `design.md`, `tasks.md`, `specs/README.md`, `state.yaml`, `context.md`, and `drift.md` exist. It must also assert `.opsx/active.yaml` points to the created change and that the normalized state still reports `stage === 'INIT'` and `nextAction === 'Create proposal.md for this change.'`. Add a second assertion block that verifies placeholder `design.md` and `tasks.md` files do not count as accepted lifecycle progress on their own.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` contains the exact test name `opsx-new skeleton creates placeholder files, active pointer, and INIT stage`.
- The test asserts the existence of `specs/README.md`, not `spec.md`, for the initial skeleton.
- The test asserts `state.stage === 'INIT'` after the skeleton is created.
- `npm run test:workflow-runtime` passes with the new skeleton coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "opsx-new skeleton creates placeholder files, active pointer, and INIT stage|specs/README.md|Create proposal.md for this change\\." scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- STATE-01 has direct regression coverage instead of relying on migration-only scaffolds.
- New changes can be created repeatedly without inventing accepted planning state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new-change scaffold -> lifecycle state | placeholder files can accidentally look like approved planning output |
| active pointer -> future command routing | a missing or stale `.opsx/active.yaml` can send later actions to the wrong change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `createChangeSkeleton()` outputs | mitigate | Persist `stage: INIT` explicitly and cover placeholder files with direct tests. |
| T-04-03 | Denial of Service / Tampering | `.opsx/active.yaml` updates | mitigate | Update the active pointer with the new state-store helper and assert the exact `activeChange` value in tests. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep for the exact placeholder file content and the new skeleton helper.
</verification>

<success_criteria>
- `opsx-new` can produce the full Phase 4 disk skeleton.
- New changes become the active change without implying accepted planning progress.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-02-SUMMARY.md`
</output>
