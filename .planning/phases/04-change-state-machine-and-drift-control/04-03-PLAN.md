---
phase: 04-change-state-machine-and-drift-control
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - lib/change-state.js
  - lib/runtime-guidance.js
  - lib/cli.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - STATE-02
  - STATE-07
must_haves:
  truths:
    - "Mutation actions advance only through explicit allowed stage/event pairs."
    - "Read-only status and resume work from empty or partial state without implicitly creating files or refreshing hashes."
    - "`opsx-continue` routes to the next valid action from persisted state instead of re-planning unrelated work."
  artifacts:
    - path: "lib/change-state.js"
      provides: "Strict local transition table and continue router"
      contains: "function applyMutationEvent"
    - path: "lib/runtime-guidance.js"
      provides: "Read-only state selectors and continue/resume builders"
      contains: "function buildContinueInstructions"
    - path: "lib/cli.js"
      provides: "State-aware status rendering without a broad CLI workflow engine"
      contains: "function showStatus"
  key_links:
    - from: "lib/change-state.js"
      to: "lib/runtime-guidance.js"
      via: "selectors and continue routing consume the same stage vocabulary"
      pattern: "INIT|TASKS_READY|APPLYING_GROUP|resolveContinueAction"
    - from: "lib/runtime-guidance.js"
      to: "lib/cli.js"
      via: "CLI status delegates to the read-only runtime guidance layer"
      pattern: "buildStatus|buildStatusText"
---

<objective>
Introduce deterministic lifecycle transitions and read-only selectors without building a second workflow engine into the Node CLI.

Purpose: satisfy strict mutation validation, make `status` / `resume` truthful from disk state, and give `continue` an explicit next-action router.
Output: new `lib/change-state.js`, state-aware runtime guidance, updated CLI status text, and green regression tests.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
@.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
@.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
@.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-01-PLAN.md
@.planning/phases/04-change-state-machine-and-drift-control/04-02-PLAN.md

<interfaces>
From `lib/runtime-guidance.js`:
```javascript
function buildStatus(options = {}) {}
function buildApplyInstructions(options = {}) {}
```

From `lib/cli.js`:
```javascript
function showStatus(options = {}) {}
async function runCli(argv) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the strict local transition table and next-action router</name>
  <files>
lib/change-state.js
  </files>
  <behavior>
    - Valid mutation events return a deterministic next state.
    - Invalid mutation events return a blocking result instead of silently advancing the workflow.
    - Continue routing is stage-aware and respects an active task group when one is already in progress.
  </behavior>
  <read_first>
lib/change-store.js
.planning/REQUIREMENTS.md
.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
  </read_first>
  <action>Implement D-05, D-08, and D-16 in a local library module. Create `lib/change-state.js` with these exact exports: `STAGE_VALUES`, `MUTATION_EVENTS`, `applyMutationEvent(state, event)`, `resolveContinueAction(state)`, and `buildLifecycleBlockResult(code, message, patchTargets, nextStep)`. Use the exact stage vocabulary from D-08: `INIT`, `PROPOSAL_READY`, `SPECS_READY`, `SPEC_SPLIT_REVIEWED`, `DESIGN_READY`, `SECURITY_REVIEW_REQUIRED`, `SECURITY_REVIEWED`, `SPEC_REVIEWED`, `TASKS_READY`, `APPLYING_GROUP`, `GROUP_VERIFIED`, `IMPLEMENTED`, `VERIFIED`, `SYNCED`, `ARCHIVED`, `BLOCKED`. Define explicit mutation events for `NEW_SKELETON_CREATED`, `PROPOSAL_ACCEPTED`, `SPECS_ACCEPTED`, `SPEC_SPLIT_ACCEPTED`, `DESIGN_ACCEPTED`, `SECURITY_REVIEW_MARKED_REQUIRED`, `SECURITY_REVIEW_ACCEPTED`, `TASKS_ACCEPTED`, `START_TASK_GROUP`, `COMPLETE_TASK_GROUP`, `VERIFY_ACCEPTED`, `SYNC_ACCEPTED`, `ARCHIVE_ACCEPTED`, and `BLOCK`. `applyMutationEvent()` must return a blocking object for invalid pairs with `status: 'BLOCK'`, `code: 'invalid-transition'`, and a `patchTargets` array. `resolveContinueAction()` must return `apply` when `stage === 'APPLYING_GROUP'`, `tasks` when `stage === 'DESIGN_READY'`, `design` when `stage === 'SPECS_READY'`, `specs` when `stage === 'PROPOSAL_READY'`, `proposal` when `stage === 'INIT'`, and `verify` / `sync` / `archive` for `IMPLEMENTED` / `VERIFIED` / `SYNCED` respectively.</action>
  <acceptance_criteria>
- `lib/change-state.js` exports `STAGE_VALUES`, `MUTATION_EVENTS`, `applyMutationEvent`, `resolveContinueAction`, and `buildLifecycleBlockResult`.
- `lib/change-state.js` contains the exact stage strings `INIT`, `TASKS_READY`, `APPLYING_GROUP`, `IMPLEMENTED`, `VERIFIED`, `SYNCED`, and `ARCHIVED`.
- Invalid transition results use the exact `code` value `invalid-transition`.
- `resolveContinueAction()` includes explicit handling for `INIT`, `PROPOSAL_READY`, `SPECS_READY`, `DESIGN_READY`, `APPLYING_GROUP`, `IMPLEMENTED`, `VERIFIED`, and `SYNCED`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "applyMutationEvent|resolveContinueAction|invalid-transition|APPLYING_GROUP|SYNCED|ARCHIVED" lib/change-state.js</automated>
  </verify>
  <done>
- Mutation validation is explicit and deterministic.
- Later plans can consume a stable local state machine without introducing an external actor library.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate read-only selectors for status, resume, and continue without mutating disk state</name>
  <files>
lib/runtime-guidance.js
lib/cli.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Status and resume read normalized disk state and return concrete next steps even when state is partial.
    - Continue uses persisted stage and active task-group data instead of inferring from file presence alone.
    - CLI status becomes Phase 4-aware but the CLI still does not implement propose/apply/verify/archive execution.
  </behavior>
  <read_first>
lib/runtime-guidance.js
lib/cli.js
scripts/test-workflow-runtime.js
lib/change-store.js
lib/change-state.js
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
  </read_first>
  <action>Implement D-02, D-03, D-06, D-07, and D-16. In `lib/runtime-guidance.js`, load normalized state through `lib/change-store.js` and add exact new exports `buildStatusText(options)`, `buildResumeInstructions(options)`, and `buildContinueInstructions(options)`. `buildStatus()` must remain object-shaped, but it must now include persisted `stage`, `nextAction`, `active.taskGroup`, `warnings`, and `blockers` from `state.yaml` when present. `buildResumeInstructions()` must never write files; it should summarize current stage, unresolved blockers, and the next exact route. `buildContinueInstructions()` must call `resolveContinueAction()` and, when `stage === 'APPLYING_GROUP'`, carry forward the persisted `active.taskGroup` instead of picking a new group. In `lib/cli.js`, replace the Phase 2 placeholder `showStatus()` text with `buildStatusText()` when `.opsx/config.yaml` exists; keep the migration/onboard truthfulness for missing workspace and do not add CLI handlers for `continue`, `resume`, `apply`, `propose`, `ff`, `verify`, or `archive`. Do not add `status --json` in this phase; Phase 8 owns the clean JSON hardening contract. In `scripts/test-workflow-runtime.js`, add tests named exactly `status and resume read partial state without auto-creating files` and `continue routes from persisted stage and active task group`.</action>
  <acceptance_criteria>
- `lib/runtime-guidance.js` exports `buildStatusText`, `buildResumeInstructions`, and `buildContinueInstructions`.
- `buildStatus()` payload includes persisted `stage`, `nextAction`, and `active.taskGroup`.
- `lib/cli.js` no longer contains the literal string `Current phase: Phase 2 (.opsx/ Workspace and Migration)`.
- `lib/cli.js` still does not add `case 'continue'`, `case 'resume'`, `case 'apply'`, `case 'propose'`, `case 'ff'`, `case 'verify'`, or `case 'archive'`.
- `scripts/test-workflow-runtime.js` contains the exact test names `status and resume read partial state without auto-creating files` and `continue routes from persisted stage and active task group`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "buildStatusText|buildResumeInstructions|buildContinueInstructions|Current phase: Phase 2 \\(\\.opsx/ Workspace and Migration\\)|case 'continue'|status and resume read partial state without auto-creating files|continue routes from persisted stage and active task group" lib/runtime-guidance.js lib/cli.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Read-only commands trust disk state and remain non-mutating.
- Continue routing is explicit and no longer inferred only from artifact presence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation events -> persisted stage | invalid transitions can silently advance workflow state |
| read-only routes -> disk state | status/resume can accidentally write or auto-create files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service / Tampering | `buildStatus()` / `buildResumeInstructions()` | mitigate | Normalize partial state, keep selectors read-only, and cover missing-active / partial-state fixtures. |
| T-04-04 | Tampering | `applyMutationEvent()` | mitigate | Return blocking `invalid-transition` results for invalid mutation actions instead of silently continuing. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep for the exact stage names, selector exports, and CLI command exclusions.
</verification>

<success_criteria>
- Mutation transitions are explicit and blocking on invalid input.
- Status, resume, and continue consume persisted state truthfully without mutating missing/partial state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-03-SUMMARY.md`
</output>
