---
phase: 04-change-state-machine-and-drift-control
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - lib/fs-utils.js
  - lib/change-store.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - STATE-01
  - STATE-03
  - STATE-04
must_haves:
  truths:
    - "Sparse Phase 2 `state.yaml` data loads into a stable Phase 4 shape instead of crashing or mutating read-only flows."
    - "Placeholder planning artifacts no longer imply accepted lifecycle stages by file presence alone."
    - "Phase 4 state arrays and nested verification records serialize losslessly on disk."
  artifacts:
    - path: "package.json"
      provides: "Exact serializer dependency choice for Phase 4"
      contains: "\"yaml\": \"2.8.3\""
    - path: "lib/change-store.js"
      provides: "Persisted state load/save/normalize contract"
      contains: "function normalizeChangeState"
    - path: "lib/fs-utils.js"
      provides: "Atomic text-write helper for accepted state updates"
      contains: "function writeTextAtomic"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Wave 0 state-machine contract coverage"
      contains: "placeholder artifacts do not imply accepted planning stages"
  key_links:
    - from: "package.json"
      to: "lib/change-store.js"
      via: "state serialization uses the pinned yaml dependency and no xstate dependency"
      pattern: "yaml"
    - from: "lib/change-store.js"
      to: "scripts/test-workflow-runtime.js"
      via: "normalizer and hash-state contract tests"
      pattern: "normalizeChangeState|loadChangeState|writeChangeState"
---

<objective>
Lock the Phase 4 persistence contract before broader runtime integration starts.

Purpose: satisfy the Wave 0 validation gate first, make the serializer choice explicit, and establish a stable state-store API that later plans can consume without re-litigating shape or dependency choices.
Output: `yaml@2.8.3` pin, atomic write helper, new `lib/change-store.js`, and green Wave 0 state-contract tests.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
@.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
@.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
@.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
@.planning/phases/02-opsx-workspace-and-migration/02-02-SUMMARY.md
@.planning/phases/02-opsx-workspace-and-migration/02-03-SUMMARY.md

<interfaces>
From `lib/workspace.js`:
```javascript
function writeActiveStateIfMissing(repoRoot, activeChange = '') {}
function writeChangeScaffoldsIfMissing(changeDir, changeName) {}
```

From `lib/fs-utils.js`:
```javascript
function writeText(filePath, content) {}
function readText(filePath) {}
```

From `package.json`:
```json
{
  "scripts": {
    "test:workflow-runtime": "node scripts/test-workflow-runtime.js"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the Phase 4 serializer and define the persisted state-store contract</name>
  <files>
package.json
lib/fs-utils.js
lib/change-store.js
  </files>
  <behavior>
    - State files persist arrays and nested verification objects without collapsing them into strings.
    - Sparse Phase 2 scaffolds normalize into a valid Phase 4 state object with array defaults and explicit checkpoint slots.
    - The Phase 4 dependency choice is unambiguous: add `yaml@2.8.3`, do not add `xstate`, and implement the mutation table locally in a later plan.
  </behavior>
  <read_first>
package.json
lib/fs-utils.js
lib/yaml.js
lib/workspace.js
.planning/REQUIREMENTS.md
.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
  </read_first>
  <action>Implement D-01, D-07, D-08, D-17, and the explicit dependency choice for this phase. In `package.json`, add exactly one new runtime dependency: `"yaml": "2.8.3"`. Do not add `xstate`; Phase 4 will use a local transition-table module in `lib/change-state.js` because this phase needs deterministic stage/event validation, not a live actor runtime. In `lib/fs-utils.js`, add `writeTextAtomic(filePath, content)` that writes a same-directory temp file and then renames it into place so accepted checkpoint/state writes do not leave partial YAML on disk. Create `lib/change-store.js` exporting these exact functions: `buildChangeStateSkeleton(changeName, changeDir)`, `normalizeChangeState(raw, options = {})`, `loadActiveChangePointer(repoRoot)`, `writeActiveChangePointer(repoRoot, activeChange)`, `loadChangeState(changeDir)`, and `writeChangeState(changeDir, state)`. Use the `yaml` package for `active.yaml` and `state.yaml`; do not route those files through `lib/yaml.js`. Make `normalizeChangeState()` output this exact top-level shape: `version`, `change`, `stage`, `nextAction`, `artifacts`, `hashes`, `checkpoints`, `active`, `verificationLog`, `blockers`, `warnings`, `allowedPaths`, `forbiddenPaths`, `updatedAt`. Normalize legacy string fields from Phase 2 so `blockers`, `warnings`, `allowedPaths`, `forbiddenPaths`, and `verificationLog` are always arrays. Default `stage` to `INIT`, default `nextAction` to `Create proposal.md for this change.`, default `checkpoints` keys to `spec`, `task`, and `execution`, and default `active.taskGroup` / `active.nextTaskGroup` to `null`.</action>
  <acceptance_criteria>
- `package.json` contains the exact string `"yaml": "2.8.3"`.
- `package.json` does not contain the string `"xstate"`.
- `lib/fs-utils.js` exports `writeTextAtomic`.
- `lib/change-store.js` exports `buildChangeStateSkeleton`, `normalizeChangeState`, `loadActiveChangePointer`, `writeActiveChangePointer`, `loadChangeState`, and `writeChangeState`.
- `normalizeChangeState()` always returns array values for `verificationLog`, `blockers`, `warnings`, `allowedPaths`, and `forbiddenPaths`.
- `normalizeChangeState()` defaults `stage` to `INIT` and `checkpoints` keys to `spec`, `task`, and `execution`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "\"yaml\": \"2\\.8\\.3\"|\"xstate\"|writeTextAtomic|buildChangeStateSkeleton|normalizeChangeState|loadActiveChangePointer|writeActiveChangePointer|loadChangeState|writeChangeState" package.json lib/fs-utils.js lib/change-store.js</automated>
  </verify>
  <done>
- The repository has a single explicit serializer dependency for Phase 4 state files.
- Later plans can read and write a stable Phase 4 state shape without touching the legacy YAML helper.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Wave 0 runtime tests for placeholder, sparse-state, and read-only drift contracts</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Placeholder `proposal.md`, `design.md`, `tasks.md`, and `specs/` scaffolds do not mark planning stages accepted.
    - Sparse Phase 2 state fixtures normalize safely into the Phase 4 state contract.
    - Read-only drift detection warns and reloads without refreshing stored hashes.
  </behavior>
  <read_first>
scripts/test-workflow-runtime.js
lib/change-store.js
lib/workspace.js
.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
  </read_first>
  <action>Implement the Wave 0 contract cases called out by 04-VALIDATION and D-10/D-11. In `scripts/test-workflow-runtime.js`, add fixtures and tests with these exact names: `placeholder artifacts do not imply accepted planning stages`, `sparse Phase 2 state normalizes to Phase 4 arrays and checkpoint slots`, and `read-only drift detection warns without refreshing stored hashes`. The placeholder case must create `proposal.md`, `design.md`, `tasks.md`, and `specs/README.md` but still assert the normalized state remains `INIT` until later accepted writes occur. The sparse-state case must load a legacy fixture where `warnings` and `blockers` are strings and assert `normalizeChangeState()` returns arrays plus `active.taskGroup === null`. The drift case must seed stored hashes in `state.yaml`, change `proposal.md`, run the read-only inspection path, assert a warning is surfaced, and assert the stored hash entry in `state.yaml` is unchanged after the read-only call. Keep `npm run test:workflow-runtime` green at the end of this plan.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` contains the exact test names `placeholder artifacts do not imply accepted planning stages`, `sparse Phase 2 state normalizes to Phase 4 arrays and checkpoint slots`, and `read-only drift detection warns without refreshing stored hashes`.
- The placeholder test creates `specs/README.md` rather than a `spec.md` file, so placeholder specs do not count as completed specs.
- The drift test asserts the stored hash value in `state.yaml` stays unchanged after the read-only inspection path runs.
- `npm run test:workflow-runtime` passes after the new tests are added.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "placeholder artifacts do not imply accepted planning stages|sparse Phase 2 state normalizes to Phase 4 arrays and checkpoint slots|read-only drift detection warns without refreshing stored hashes|specs/README.md" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Wave 0 validation exists for the biggest Phase 4 semantic risks before broader runtime integration starts.
- Later plans can rely on stable regression tests instead of ad hoc manual fixtures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted YAML -> runtime readers | malformed or sparse `state.yaml` can poison selectors and mutation decisions |
| placeholder artifacts -> lifecycle state | file presence can be mistaken for accepted progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | placeholder planning files | mitigate | Keep accepted stage in normalized `state.yaml`; add Wave 0 tests proving placeholder artifacts keep `stage: INIT`. |
| T-04-02 | Tampering | stored artifact hashes | mitigate | Separate read-only drift detection from accepted writes and add a regression asserting hashes stay unchanged after read-only inspection. |
| T-04-03 | Denial of Service / Tampering | `lib/change-store.js` | mitigate | Normalize sparse Phase 2 data into the full Phase 4 shape and keep array defaults explicit. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep for the exact dependency choice and exported state-store symbols.
</verification>

<success_criteria>
- The serializer choice is explicit and reviewable.
- The Phase 4 state-store contract exists and Wave 0 regression coverage is green.
- Placeholder files and sparse legacy state are no longer implicit sources of truth.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-01-SUMMARY.md`
</output>
