---
phase: 04-change-state-machine-and-drift-control
plan: 08
type: execute
wave: 5
depends_on:
  - 04-05
files_modified:
  - commands/gemini/opsx/apply.toml
  - commands/gemini/opsx/continue.toml
  - commands/gemini/opsx/ff.toml
  - commands/gemini/opsx/new.toml
  - commands/gemini/opsx/onboard.toml
  - commands/gemini/opsx/propose.toml
  - commands/gemini/opsx/resume.toml
  - commands/gemini/opsx/status.toml
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-07
  - STATE-08
must_haves:
  truths:
    - "Gemini stateful action files match the Phase 4 source-of-truth guidance exactly."
    - "Gemini `status`, `resume`, `continue`, and `apply` prompts mention drift warnings and one-group apply behavior."
  artifacts:
    - path: "commands/gemini/opsx/apply.toml"
      provides: "Gemini apply route updated for one-group execution"
      contains: "Execute exactly one top-level task group by default"
    - path: "commands/gemini/opsx/status.toml"
      provides: "Gemini status route updated for read-only drift warnings"
      contains: "do not refresh stored hashes from read-only routes"
  key_links:
    - from: "lib/workflow.js"
      to: "commands/gemini/opsx/*.toml"
      via: "buildPlatformBundle('gemini')"
      pattern: "buildPlatformBundle\\('gemini'\\)"
---

<objective>
Refresh the Gemini checked-in stateful command slice from the updated Phase 4 source of truth.

Purpose: keep the checked-in Gemini bundle aligned with the new durable-state semantics without touching unrelated generated files.
Output: eight refreshed Gemini action files for `apply`, `continue`, `ff`, `new`, `onboard`, `propose`, `resume`, and `status`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-change-state-machine-and-drift-control/04-05-PLAN.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-07-SUMMARY.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-08-SUMMARY.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the bounded Gemini stateful-action slice from `buildPlatformBundle('gemini')`</name>
  <files>
commands/gemini/opsx/apply.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
commands/gemini/opsx/new.toml
commands/gemini/opsx/onboard.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/resume.toml
commands/gemini/opsx/status.toml
  </files>
  <read_first>
lib/workflow.js
lib/generator.js
commands/gemini/opsx/apply.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
commands/gemini/opsx/new.toml
commands/gemini/opsx/onboard.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/resume.toml
commands/gemini/opsx/status.toml
.planning/phases/04-change-state-machine-and-drift-control/04-05-PLAN.md
  </read_first>
  <action>Refresh exactly these eight files from `buildPlatformBundle('gemini')`: `commands/gemini/opsx/apply.toml`, `continue.toml`, `ff.toml`, `new.toml`, `onboard.toml`, `propose.toml`, `resume.toml`, and `status.toml`. Do not hand-edit the generated TOML or prompt bodies. Do not change `commands/gemini/opsx.toml` or any non-listed action. The refreshed files must pick up the Phase 4 source-of-truth copy for persisted stage, read-only hash-drift warnings, active task-group resume behavior, and one-group apply execution.</action>
  <acceptance_criteria>
- Only the eight plan-listed Gemini files are modified.
- `commands/gemini/opsx/apply.toml` contains `Execute exactly one top-level task group by default`.
- `commands/gemini/opsx/status.toml` and `commands/gemini/opsx/resume.toml` contain `do not refresh stored hashes from read-only routes`.
- The refreshed Gemini files match `buildPlatformBundle('gemini')` byte-for-byte for the listed paths.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const {buildPlatformBundle}=require('./lib/generator');const bundle=buildPlatformBundle('gemini');const files=['opsx/apply.toml','opsx/continue.toml','opsx/ff.toml','opsx/new.toml','opsx/onboard.toml','opsx/propose.toml','opsx/resume.toml','opsx/status.toml'];for(const file of files){const actual=fs.readFileSync(path.join('commands','gemini',file),'utf8');if(actual!==bundle[file]){throw new Error(file);}}"</automated>
  </verify>
  <done>
- The bounded Gemini slice is back in parity with the Phase 4 source of truth.
- Gemini users see the new durable-state behavior in the checked-in command files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth metadata -> checked-in Gemini files | stale generated files can mislead Gemini users even when library code is correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | Gemini `status` / `resume` files | mitigate | Refresh the exact listed files from `buildPlatformBundle('gemini')` and assert read-only drift wording exists. |
| T-04-07 | Tampering | Gemini `apply` file | mitigate | Refresh `apply.toml` from source of truth and verify the exact one-group apply string. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run the exact Gemini parity command for the listed files.
</verification>

<success_criteria>
- The Gemini generated stateful slice matches the Phase 4 source of truth exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-08-SUMMARY.md`
</output>
