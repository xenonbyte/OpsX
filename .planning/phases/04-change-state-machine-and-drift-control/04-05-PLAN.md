---
phase: 04-change-state-machine-and-drift-control
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified:
  - lib/workflow.js
  - skills/opsx/SKILL.md
  - skills/opsx/references/action-playbooks.md
  - skills/opsx/references/action-playbooks-zh.md
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-07
  - STATE-08
must_haves:
  truths:
    - "The source-of-truth prompt metadata and distributed skill guidance describe persisted stage, drift warnings, and one-group apply semantics."
    - "Stateful generated actions can be refreshed from updated source metadata without immediately failing on stale checked-in files."
    - "The bilingual skill playbooks stay aligned with the same state-machine contract."
  artifacts:
    - path: "lib/workflow.js"
      provides: "Phase 4 fallback/source-output guidance for stateful actions"
      contains: "ACTION_FALLBACK_LINES"
    - path: "skills/opsx/SKILL.md"
      provides: "Distributed skill contract updated for durable state behavior"
      contains: "state.yaml"
    - path: "skills/opsx/references/action-playbooks-zh.md"
      provides: "Chinese action semantics aligned to one-group apply and read-only drift warnings"
      contains: "execution checkpoint"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Temporary source-output gate for the Phase 4 generated refresh wave"
      contains: "buildPlatformBundle"
  key_links:
    - from: "lib/workflow.js"
      to: "commands/**"
      via: "buildPlatformBundle() renders fallback and state-behavior lines from workflow metadata"
      pattern: "ACTION_FALLBACK_LINES"
    - from: "skills/opsx/SKILL.md"
      to: "skills/opsx/references/action-playbooks*.md"
      via: "same preflight/state-machine behavior described in distributed skill and route playbooks"
      pattern: "state.yaml|context.md|drift.md"
---

<objective>
Update the source-of-truth guidance layer for Phase 4 and stage a bounded generated-command refresh wave.

Purpose: teach the public OpsX surfaces about persisted stage, drift warnings, and one-group apply behavior while keeping the runtime suite green until the checked-in generated files are refreshed.
Output: updated `lib/workflow.js`, aligned bilingual skill playbooks, and a temporary source-output-only gate for the affected generated action files.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
@.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
@.planning/phases/04-change-state-machine-and-drift-control/04-VALIDATION.md
@.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-02-SUMMARY.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-11-SUMMARY.md

<interfaces>
From `lib/workflow.js`:
```javascript
const ACTION_FALLBACK_LINES = Object.freeze({
  status: Object.freeze([/* state-aware fallback copy */])
});
```

From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Phase 4 source-of-truth action guidance and stage a temporary source-output gate</name>
  <files>
lib/workflow.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Stateful actions describe persisted stage, read-only drift warnings, and one-group apply semantics.
    - Common preflight lines stay unchanged so unaffected generated files do not churn.
    - The runtime suite stays green while checked-in generated files are refreshed in later plans.
  </behavior>
  <read_first>
lib/workflow.js
lib/generator.js
scripts/test-workflow-runtime.js
commands/claude/opsx/apply.md
commands/codex/prompts/opsx-apply.md
commands/gemini/opsx/apply.toml
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
.planning/phases/04-change-state-machine-and-drift-control/04-PATTERNS.md
.planning/phases/03-skill-and-command-surface-rewrite/03-02-SUMMARY.md
  </read_first>
  <action>Implement D-02, D-09, D-10, D-11, D-14, D-15, and D-16 in the source-of-truth guidance layer. In `lib/workflow.js`, keep `PHASE_THREE_PREFLIGHT_LINES` unchanged, but extend `ACTION_FALLBACK_LINES` with exact Phase 4 behavior for `new`, `propose`, `ff`, `continue`, `apply`, `status`, `resume`, and `onboard`. Add these exact semantics: `new` must create `change.yaml`, placeholder planning files, `state.yaml`, `context.md`, and `drift.md`, then leave `stage: INIT`; `status` and `resume` must warn on artifact hash drift, reload from disk, and explicitly say they do not refresh stored hashes from read-only routes; `continue` must resume `active.taskGroup` when `stage === APPLYING_GROUP`; `apply` must execute exactly one top-level task group, then record one execution checkpoint and stop; `propose` and `ff` must update hashes only after accepted checkpoint/state writes. In `scripts/test-workflow-runtime.js`, temporarily narrow the generated-surface parity gate so these exact 24 files are exempt from checked-in parity until refresh plans land: the Claude, Codex, and Gemini `apply`, `continue`, `ff`, `new`, `onboard`, `propose`, `resume`, and `status` action files. Keep source-output assertions active by checking `buildPlatformBundle()` content for the new strings, and keep checked-in parity active for every untouched generated file.</action>
  <acceptance_criteria>
- `lib/workflow.js` adds explicit `ACTION_FALLBACK_LINES` entries for `new`, `propose`, `ff`, `continue`, and `apply` in addition to the existing `status`, `resume`, and `onboard`.
- `lib/workflow.js` still leaves `PHASE_THREE_PREFLIGHT_LINES` unchanged.
- `scripts/test-workflow-runtime.js` names or lists the exact 24 temporary parity-exempt files covering Claude/Codex/Gemini `apply`, `continue`, `ff`, `new`, `onboard`, `propose`, `resume`, and `status`.
- `scripts/test-workflow-runtime.js` asserts source-output content for the exact phrases `do not refresh stored hashes from read-only routes` and `Execute exactly one top-level task group by default`.
- `npm run test:workflow-runtime` passes before any checked-in generated file is refreshed.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "ACTION_FALLBACK_LINES|do not refresh stored hashes from read-only routes|Execute exactly one top-level task group by default|opsx/(apply|continue|ff|new|onboard|propose|resume|status)|opsx-(apply|continue|ff|new|onboard|propose|resume|status)" lib/workflow.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The generator source of truth now knows about Phase 4 durable-state behavior.
- The runtime suite is ready for bounded generated refresh slices instead of forcing a single all-platform rewrite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align distributed skill guidance in both languages to the persisted-state contract</name>
  <files>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
  </files>
  <read_first>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
lib/workflow.js
.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
.planning/phases/04-change-state-machine-and-drift-control/04-RESEARCH.md
  </read_first>
  <action>Implement D-06, D-10, D-14, D-15, D-17, D-18, and D-19 in the distributed skill docs. In `skills/opsx/SKILL.md`, keep the existing config precedence and preflight path order, but update the execution loop and guardrails so status/resume are explicitly read-only, hash drift must warn and reload before further action, and `apply` completes exactly one top-level task group before writing an execution checkpoint and refreshed `context.md` / `drift.md`. In `skills/opsx/references/action-playbooks.md` and `skills/opsx/references/action-playbooks-zh.md`, update the `new`, `continue`, `resume`, `apply`, and `status` sections with concrete Phase 4 semantics: `new` creates the full skeleton and sets the active change; `continue` follows persisted stage and `active.taskGroup`; `resume` reports `stage`, `nextAction`, `warnings`, and `blockers` without mutating files; `apply` executes one top-level group, records verification command/result plus changed files, and stops; `status` surfaces drift warnings and `allowedPaths` / `forbiddenPaths` as warnings only in Phase 4. Keep archive/verify hard-block rules deferred to Phase 7 and say so explicitly.</action>
  <acceptance_criteria>
- `skills/opsx/SKILL.md` contains `hash drift` or `drift` guidance tied to `state.yaml`, `context.md`, or `drift.md`.
- Both `action-playbooks.md` and `action-playbooks-zh.md` mention one-group apply behavior and read-only status/resume behavior.
- Both playbooks explicitly say `allowedPaths` / `forbiddenPaths` are warnings in Phase 4, not hard blocks.
- Neither playbook claims `archive` or `verify` hard gates already ship in Phase 4.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "active change|stage|nextAction|allowedPaths|forbiddenPaths|one top-level task group|一级任务组|只执行一个|read-only|只读|Phase 7" skills/opsx/SKILL.md skills/opsx/references/action-playbooks.md skills/opsx/references/action-playbooks-zh.md</automated>
  </verify>
  <done>
- Distributed skill behavior matches the same Phase 4 rules as the generator source of truth.
- English and Chinese playbooks stay aligned for status/resume/apply drift semantics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow metadata -> generated command output | stale source-of-truth copy can misdescribe stateful behavior across all platforms |
| distributed skill docs -> operator behavior | docs can silently diverge from persisted-state rules and cause invalid manual updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | `lib/workflow.js` stateful fallback lines | mitigate | Add explicit read-only drift wording and one-group apply text to stateful actions only, then lock source-output assertions before refreshing checked-in files. |
| T-04-06 | Tampering | distributed skill playbooks | mitigate | Rewrite both locales to describe drift warnings, allowed/forbidden path warnings, and Phase 7 deferrals explicitly. |
| T-04-07 | Tampering | `apply` source-of-truth guidance | mitigate | Add exact one-group apply wording to action fallbacks and skill playbooks. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep workflow metadata and both playbook locales for the exact Phase 4 strings.
</verification>

<success_criteria>
- Source-of-truth prompt guidance and distributed skills describe the same durable-state rules.
- The repo is ready for bounded generated refresh plans without breaking the runtime suite.
</success_criteria>

<output>
After completion, create `.planning/phases/04-change-state-machine-and-drift-control/04-05-SUMMARY.md`
</output>
