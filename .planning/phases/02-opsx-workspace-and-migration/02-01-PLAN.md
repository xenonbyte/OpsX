---
phase: 02-opsx-workspace-and-migration
plan: 01
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - .gitignore
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - DIR-03
  - DIR-04
  - DIR-05
  - DIR-06
  - DIR-07
must_haves:
  truths:
    - "`npm run test:workflow-runtime` exercises zero-write dry-run behavior, execute-mode move/rename/scaffold behavior, legacy shared-home migration, and default-abort conflicts after the migration runtime is wired."
    - "Real `.gitignore` rules preserve tracked `.opsx` artifacts and ignore `.opsx/cache`, `.opsx/tmp`, and `.opsx/logs`, so final verification checks actual git behavior instead of a fake fixture."
    - "Phase 2 has one fast regression entrypoint that proves dry-run and execute stay aligned."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Wave 0 migration and gitignore regression harness"
      contains: "opsx migrate --dry-run"
    - path: ".gitignore"
      provides: "Canonical `.opsx` tracking and ignore policy"
      contains: "!.opsx/changes/**"
  key_links:
    - from: "scripts/test-workflow-runtime.js"
      to: "bin/opsx.js"
      via: "spawnSync CLI integration"
      pattern: "bin/opsx\\.js"
    - from: "scripts/test-workflow-runtime.js"
      to: ".gitignore"
      via: "git check-ignore fixture assertions"
      pattern: "check-ignore"
    - from: ".gitignore"
      to: "future `.opsx/` workspace files"
      via: "ordered include/ignore rules"
      pattern: "\\.opsx"
---

<objective>
Establish migration regression coverage and the canonical `.opsx` git policy after migration logic is wired.

Purpose: satisfy the Nyquist contract in `02-VALIDATION.md` by adding deterministic migration fixtures and `.gitignore` rules against the real Phase 2 migration runtime, so final verification can use one fast suite instead of manual repo/home mutation.
Output: expanded `scripts/test-workflow-runtime.js` coverage for dry-run, execute, abort, and shared-home cases plus real `.gitignore` rules for tracked versus ignored `.opsx` paths.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
@.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
@.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
@.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
@.planning/phases/02-opsx-workspace-and-migration/02-02-SUMMARY.md
@.planning/phases/02-opsx-workspace-and-migration/02-03-SUMMARY.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-01-SUMMARY.md

<interfaces>
From `scripts/test-workflow-runtime.js`:
```javascript
function createFixtureRepo() { /* copies schemas/skills and creates openspec/config.yaml */ }
function createChange(fixtureRoot, changeName, files = {}) { /* writes .openspec.yaml by default */ }
function runOpsxCli(args, options = {}) { /* spawnSync(process.execPath, [bin/opsx.js, ...args]) */ }
```

From `lib/fs-utils.js`:
```javascript
module.exports = {
  ensureDir,
  writeText,
  readText,
  removePath,
  copyFile,
  copyDir,
  listFiles
};
```

From `lib/install.js`:
```javascript
function runCheck(options = {}) { /* uses cwd/homeDir injection */ }
module.exports = { install, uninstall, runCheck, showDoc, setLanguage };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legacy repo/home migration fixtures and executable CLI assertions</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
lib/cli.js
lib/install.js
lib/runtime-guidance.js
lib/fs-utils.js
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
  </read_first>
  <action>Extend `scripts/test-workflow-runtime.js` with explicit Wave 0 helpers that never touch the real user home: add a repo fixture builder that creates `openspec/config.yaml`, `openspec/changes/<change>/.openspec.yaml`, optional `openspec/specs`, and `openspec/archive`; add a shared-home fixture builder under a temp `HOME` that creates `~/.openspec/.opsx-config.yaml`, `~/.openspec/manifests/*.manifest`, `~/.openspec/skills/openspec/**`, and `~/.openspec/commands/openspec.md`; and add concrete tests named for the target behaviors. The dry-run test must run `opsx migrate --dry-run` and assert stdout contains exact mapping lines such as `MOVE openspec/config.yaml -> .opsx/config.yaml`, `MOVE openspec/changes/demo/.openspec.yaml -> .opsx/changes/demo/change.yaml`, `MOVE ~/.openspec/.opsx-config.yaml -> ~/.opsx/config.yaml`, `MOVE ~/.openspec/skills/openspec -> ~/.opsx/skills/opsx`, `MOVE ~/.openspec/commands/openspec.md -> ~/.opsx/commands/opsx.md`, and `CREATE .opsx/active.yaml`, while also asserting `.opsx/` and `~/.opsx/` do not exist afterward. The execute test must run `opsx migrate`, then assert `.opsx/config.yaml`, `.opsx/changes/<change>/change.yaml`, `.opsx/changes/<change>/state.yaml`, `.opsx/changes/<change>/context.md`, `.opsx/changes/<change>/drift.md`, `.opsx/active.yaml`, `~/.opsx/config.yaml`, `~/.opsx/manifests/<platform>.manifest`, `~/.opsx/skills/opsx/SKILL.md`, and `~/.opsx/commands/opsx.md` exist while the old `.openspec.yaml` path is gone. Add a default-abort test that pre-creates `.opsx/config.yaml`, runs `opsx migrate`, expects a non-zero exit plus an error containing `already exists`, and proves the legacy `openspec/` tree stayed untouched. Keep all assertions string-based and deterministic; do not add snapshot output, watch mode, or any dependency beyond built-in `assert`, `fs`, `path`, `os`, and existing helpers.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` contains helper functions for both a legacy repo fixture and a legacy shared-home fixture rooted at `~/.openspec/commands/openspec.md` and `~/.openspec/skills/openspec/**`.
- The file contains test names or assertion messages covering `dry-run`, execute-mode migration, default abort, and shared-home migration.
- The dry-run assertions include the exact strings `MOVE openspec/config.yaml -> .opsx/config.yaml`, `MOVE ~/.openspec/.opsx-config.yaml -> ~/.opsx/config.yaml`, `MOVE ~/.openspec/skills/openspec -> ~/.opsx/skills/opsx`, `MOVE ~/.openspec/commands/openspec.md -> ~/.opsx/commands/opsx.md`, and `CREATE .opsx/active.yaml`.
- The execute assertions check for `change.yaml`, `state.yaml`, `context.md`, and `drift.md` under `.opsx/changes/<change>/`.
- `npm run test:workflow-runtime` passes after the new cases are added.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`rg -n "MOVE openspec/config.yaml -> \\.opsx/config\\.yaml|MOVE ~\\/\\.openspec/\\.opsx-config\\.yaml -> ~\\/\\.opsx/config\\.yaml|MOVE ~\\/\\.openspec/skills/openspec -> ~\\/\\.opsx/skills/opsx|MOVE ~\\/\\.openspec/commands/openspec\\.md -> ~\\/\\.opsx/commands/opsx\\.md|CREATE \\.opsx/active\\.yaml|already exists" scripts/test-workflow-runtime.js`
  </verify>
  <done>
- Migration regression cases cover dry-run, execute, conflict abort, and shared-home state using temp fixtures only.
- Dry-run tests prove zero writes by asserting `.opsx/` and `~/.opsx/` are still absent after the command returns.
- Execute tests prove repo/home moves, metadata rename, and scaffold creation all happen through the CLI surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace legacy keep-track rules with canonical `.opsx` gitignore policy and verify it through git</name>
  <files>
.gitignore
scripts/test-workflow-runtime.js
  </files>
  <read_first>
.gitignore
scripts/test-workflow-runtime.js
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
  </read_first>
  <action>Replace the current `openspec/changes/**` keep-track rules in `.gitignore` with ordered `.opsx` rules that preserve canonical workflow artifacts and ignore runtime scratch data. Add explicit tracked entries for `!.opsx/`, `!.opsx/config.yaml`, `!.opsx/active.yaml`, `!.opsx/changes/`, `!.opsx/changes/**`, `!.opsx/specs/`, `!.opsx/specs/**`, `!.opsx/archive/`, and `!.opsx/archive/**`. Add explicit ignored entries for `.opsx/cache/`, `.opsx/cache/**`, `.opsx/tmp/`, `.opsx/tmp/**`, `.opsx/logs/`, and `.opsx/logs/**`. Do not add a broad `.opsx/*` ignore that would hide re-included children. Extend `scripts/test-workflow-runtime.js` with a real git-policy test that initializes a temp git repo, copies the repository `.gitignore`, creates sample `.opsx` files, and runs `git check-ignore -v` so `.opsx/config.yaml`, `.opsx/active.yaml`, `.opsx/changes/demo/proposal.md`, `.opsx/specs/demo/spec.md`, and `.opsx/archive/demo.md` are reported as not ignored, while `.opsx/cache/demo.tmp`, `.opsx/tmp/demo.tmp`, and `.opsx/logs/demo.log` are reported as ignored by the exact `.gitignore` rules.</action>
  <acceptance_criteria>
- `.gitignore` contains `!.opsx/config.yaml`, `!.opsx/active.yaml`, `!.opsx/changes/**`, `!.opsx/specs/**`, and `!.opsx/archive/**`.
- `.gitignore` contains `.opsx/cache/**`, `.opsx/tmp/**`, and `.opsx/logs/**`.
- `.gitignore` no longer depends on `!openspec/changes/**` as the canonical tracking rule for workflow artifacts.
- `scripts/test-workflow-runtime.js` contains a `git check-ignore` based assertion for tracked `.opsx` files and ignored `.opsx/cache|tmp|logs` files.
- `npm run test:workflow-runtime` passes with the gitignore policy case enabled.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`rg -n "!\\.opsx/config\\.yaml|!\\.opsx/active\\.yaml|!\\.opsx/changes/\\*\\*|!\\.opsx/specs/\\*\\*|!\\.opsx/archive/\\*\\*|\\.opsx/cache/\\*\\*|\\.opsx/tmp/\\*\\*|\\.opsx/logs/\\*\\*" .gitignore`
  </verify>
  <done>
- The repository `.gitignore` already matches Phase 2 tracked versus ignored `.opsx` policy before runtime migration code is implemented.
- The runtime suite exercises real git ignore behavior instead of string-only matching.
- Later Phase 2 plans can rely on `.gitignore` assertions remaining in the fast regression suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temp fixture repo/home -> `bin/opsx.js` | Untrusted path/layout input is fed into migration CLI behavior during regression tests |
| repository `.gitignore` -> git index semantics | Incorrect include/ignore ordering can silently hide tracked workflow files or expose scratch state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | `scripts/test-workflow-runtime.js` fixtures | mitigate | Add temp-fixture cases that include tricky repo/home layouts and assert the CLI refuses to escape canonical repo/home roots. |
| T-02-02 | Tampering | `.gitignore`, migration abort tests | mitigate | Add explicit abort coverage for pre-existing `.opsx/` content and canonical ignore rules that do not hide tracked workflow files. |
| T-02-03 | Denial of Service / Tampering | execute-mode fixture assertions | mitigate | Prove the CLI either migrates the full repo/home tree plus scaffolds or leaves the legacy tree untouched on abort. |
| T-02-04 | Repudiation | dry-run fixture assertions | mitigate | Assert the exact `MOVE` and `CREATE` lines in dry-run output and separately prove no filesystem writes occurred. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep `.gitignore` for the canonical `.opsx` track/ignore rules.
</verification>

<success_criteria>
- Wave 0 regression coverage exists for dry-run, execute, shared-home legacy state, conflict abort, and gitignore policy.
- The repository `.gitignore` is already canonical for `.opsx/` before migration implementation starts.
- All Wave 0 checks stay inside the existing fast `npm run test:workflow-runtime` loop.
</success_criteria>

<output>
After completion, create `.planning/phases/02-opsx-workspace-and-migration/02-01-SUMMARY.md`
</output>
