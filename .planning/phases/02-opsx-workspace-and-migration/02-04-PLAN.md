---
phase: 02-opsx-workspace-and-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
files_modified:
  - README.md
  - README-zh.md
  - docs/customization.md
  - docs/runtime-guidance.md
  - templates/project/config.yaml.tmpl
  - templates/project/change-metadata.yaml.tmpl
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-07
must_haves:
  truths:
    - "Public docs describe `.opsx/` and `~/.opsx/` as the current canonical workspace locations, not future intent."
    - "Tracked versus ignored `.opsx` paths are documented consistently with the repository `.gitignore` rules."
    - "Template scaffolds tell operators exactly where `config.yaml` and `change.yaml` belong without mentioning legacy filenames."
  artifacts:
    - path: "README.md"
      provides: "Public migration and canonical workspace guidance"
      contains: "opsx migrate --dry-run"
    - path: "docs/customization.md"
      provides: "Canonical config precedence and shared-home semantics"
      contains: "~/.opsx/config.yaml"
    - path: "templates/project/change-metadata.yaml.tmpl"
      provides: "Canonical saved-path hint for `change.yaml`"
      contains: "# Save as .opsx/changes/<change-name>/change.yaml"
  key_links:
    - from: "README.md"
      to: ".gitignore"
      via: "tracked vs ignored `.opsx` path guidance"
      pattern: "\\.opsx/cache"
    - from: "docs/runtime-guidance.md"
      to: "lib/runtime-guidance.js"
      via: "canonical runtime artifact paths"
      pattern: "change.yaml"
    - from: "templates/project/config.yaml.tmpl"
      to: ".opsx/config.yaml"
      via: "explicit save-path comment"
      pattern: "# Save as \\.opsx/config\\.yaml"
---

<objective>
Finish the public/operator-facing side of Phase 2 so the shipped docs and templates match the implemented `.opsx` migration behavior.

Purpose: satisfy D-15 through D-17 by making the canonical workspace layout, migration commands, and tracked-vs-ignored rules obvious in both English/Chinese docs and project templates.
Output: updated README/docs text plus explicit save-path comments in the project config and change-metadata templates.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
@.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
@.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
@.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
@.planning/phases/02-opsx-workspace-and-migration/02-03-PLAN.md
@.gitignore

<interfaces>
Canonical precedence already established in docs/runtime code:
```text
1. .opsx/changes/<name>/change.yaml
2. .opsx/config.yaml
3. ~/.opsx/config.yaml
4. package defaults
```

Required tracked/ignored policy:
```text
Tracked: .opsx/config.yaml, .opsx/active.yaml, .opsx/changes/**, .opsx/specs/**, .opsx/archive/**
Ignored: .opsx/cache/**, .opsx/tmp/**, .opsx/logs/**
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite public docs from future-tense migration intent to shipped `.opsx` behavior</name>
  <files>
README.md
README-zh.md
docs/customization.md
docs/runtime-guidance.md
  </files>
  <read_first>
README.md
README-zh.md
docs/customization.md
docs/runtime-guidance.md
.gitignore
lib/cli.js
lib/install.js
lib/runtime-guidance.js
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
  </read_first>
  <action>Update English and Chinese docs so Phase 2 is described as shipped behavior rather than future intent. In `README.md` and `README-zh.md`, replace the current Phase 1/Phase 2 future-tense project-config paragraph with direct instructions that `opsx migrate --dry-run` prints the exact `MOVE`/`CREATE` mapping and performs zero writes, while `opsx migrate` aborts by default if `.opsx/` already exists. Add one concrete tracked-versus-ignored list using the exact paths `.opsx/config.yaml`, `.opsx/active.yaml`, `.opsx/changes/**`, `.opsx/specs/**`, `.opsx/archive/**`, `.opsx/cache/**`, `.opsx/tmp/**`, and `.opsx/logs/**`. In `docs/customization.md`, keep the precedence list canonical and present `~/.opsx/config.yaml` plus `~/.opsx/manifests/*.manifest` as current shared-home semantics, not target semantics. In `docs/runtime-guidance.md`, add or update a short compatibility/migration section that states runtime artifact resolution now uses `.opsx/config.yaml` and `.opsx/changes/<name>/change.yaml`, while legacy `openspec/` and `~/.openspec/` appear only as migration candidates. Keep the two README files semantically aligned and keep legacy names restricted to migration-history paragraphs only.</action>
  <acceptance_criteria>
- `README.md` and `README-zh.md` both mention `opsx migrate --dry-run` and default-abort behavior for existing `.opsx/`.
- The docs contain the exact tracked paths `.opsx/config.yaml`, `.opsx/active.yaml`, `.opsx/changes/**`, `.opsx/specs/**`, and `.opsx/archive/**`.
- The docs contain the exact ignored paths `.opsx/cache/**`, `.opsx/tmp/**`, and `.opsx/logs/**`.
- `docs/customization.md` describes `~/.opsx/config.yaml` as current shared-home config, not a future target.
- `docs/runtime-guidance.md` references `.opsx/config.yaml` and `change.yaml` as current runtime artifact paths.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`rg -n "opsx migrate --dry-run|default abort|\\.opsx/config\\.yaml|\\.opsx/active\\.yaml|\\.opsx/changes/\\*\\*|\\.opsx/specs/\\*\\*|\\.opsx/archive/\\*\\*|\\.opsx/cache/\\*\\*|\\.opsx/tmp/\\*\\*|\\.opsx/logs/\\*\\*|change\\.yaml|~\\/\\.opsx/config\\.yaml" README.md README-zh.md docs/customization.md docs/runtime-guidance.md`
  </verify>
  <done>
- Public docs match the shipped `.opsx` runtime and migration semantics.
- The tracked versus ignored `.opsx` policy is documented exactly once per language set and matches `.gitignore`.
- Legacy names remain only where they explain migration from old layouts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit canonical save-path hints to project templates</name>
  <files>
templates/project/config.yaml.tmpl
templates/project/change-metadata.yaml.tmpl
  </files>
  <read_first>
templates/project/config.yaml.tmpl
templates/project/change-metadata.yaml.tmpl
README.md
docs/customization.md
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
  </read_first>
  <action>Prepend explicit canonical save-path comments to the two project templates without changing their schema/content shape. Add `# Save as .opsx/config.yaml` as the first line of `templates/project/config.yaml.tmpl`. Add `# Save as .opsx/changes/<change-name>/change.yaml` as the first line of `templates/project/change-metadata.yaml.tmpl`. Leave the rest of the YAML keys and values untouched, and do not introduce any `.openspec.yaml`, `openspec/config.yaml`, or `.opsx-config.yaml` references into template content.</action>
  <acceptance_criteria>
- `templates/project/config.yaml.tmpl` starts with `# Save as .opsx/config.yaml`.
- `templates/project/change-metadata.yaml.tmpl` starts with `# Save as .opsx/changes/<change-name>/change.yaml`.
- The existing YAML keys under both templates remain unchanged after the new comment line.
- No legacy path strings appear in either template.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`rg -n "^# Save as \\.opsx/config\\.yaml$|^# Save as \\.opsx/changes/<change-name>/change\\.yaml$|\\.openspec\\.yaml|openspec/config\\.yaml|\\.opsx-config\\.yaml" templates/project/config.yaml.tmpl templates/project/change-metadata.yaml.tmpl`
  </verify>
  <done>
- Operators can see the canonical `.opsx` save locations directly in template files.
- Template semantics stay stable while legacy naming is removed from scaffold guidance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs/templates -> operator behavior | Inaccurate migration or path guidance can cause users to overwrite state or keep editing legacy locations |
| docs policy -> repository tracking rules | If documented tracked/ignored paths diverge from `.gitignore`, users will mis-handle workflow artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | docs/templates path guidance | mitigate | Document only canonical `.opsx` and `~/.opsx` save paths so operators do not manually write outside the approved workspace roots. |
| T-02-02 | Tampering | README/docs migration guidance | mitigate | State the default-abort behavior explicitly so operators expect conflicts instead of assuming silent merges or overwrites are safe. |
| T-02-03 | Denial of Service / Tampering | docs vs `.gitignore` policy | mitigate | Mirror the exact tracked and ignored `.opsx` paths from `.gitignore` so repo state does not split between tracked and scratch directories. |
| T-02-04 | Repudiation | migration docs and template hints | mitigate | Keep docs aligned with the same dry-run/execute semantics and canonical filenames that the runtime now uses, avoiding misleading future-tense copy. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep docs and templates for canonical `.opsx` paths, `change.yaml`, and tracked/ignored policy strings.
</verification>

<success_criteria>
- README/docs stop describing `.opsx` as future work and instead explain current behavior.
- Template files explicitly tell users where to save `config.yaml` and `change.yaml`.
- Docs, templates, and `.gitignore` all agree on the canonical tracked versus ignored workspace paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-opsx-workspace-and-migration/02-04-SUMMARY.md`
</output>
