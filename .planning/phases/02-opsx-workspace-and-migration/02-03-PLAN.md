---
phase: 02-opsx-workspace-and-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - lib/constants.js
  - lib/config.js
  - lib/install.js
  - lib/runtime-guidance.js
  - lib/cli.js
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
must_haves:
  truths:
    - "Canonical runtime path resolution uses `.opsx/` and `~/.opsx/config.yaml` everywhere instead of hidden `openspec` compatibility fallbacks."
    - "`opsx migrate --dry-run` and `opsx migrate` now dispatch the real migration service through the public CLI."
    - "`opsx check` and minimal `opsx status` tell the truth about canonical paths and legacy migration candidates without pretending Phase 4 state-machine behavior already exists."
  artifacts:
    - path: "lib/constants.js"
      provides: "Canonical shared-home and global config constants"
      contains: "const SHARED_HOME_NAME = '.opsx';"
    - path: "lib/cli.js"
      provides: "Real `migrate` dispatch and truthful Phase 2 status"
      contains: "runMigration"
    - path: "lib/runtime-guidance.js"
      provides: "Canonical `.opsx` runtime artifact resolution"
      contains: "change.yaml"
  key_links:
    - from: "lib/cli.js"
      to: "lib/migrate.js"
      via: "migrate/status dispatch"
      pattern: "require\\('\\./migrate'\\)"
    - from: "lib/install.js"
      to: "lib/config.js"
      via: "canonical shared-home paths"
      pattern: "getGlobalConfigPath"
    - from: "lib/runtime-guidance.js"
      to: "lib/config.js"
      via: "merged global/project/change config"
      pattern: "loadGlobalConfig"
---

<objective>
Switch the runtime to the canonical `.opsx/` and `~/.opsx/` paths and wire the public CLI to the real migration service.

Purpose: satisfy D-01 through D-04, D-18 through D-20, and the Phase 2 success criteria that canonical paths become the only runtime truth while legacy paths remain visible only as migration candidates.
Output: updated constants/config/install/runtime-guidance path resolution plus real `opsx migrate --dry-run`, `opsx migrate`, `opsx check`, and minimal `opsx status` behavior.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
@.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
@.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
@.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
@.planning/phases/02-opsx-workspace-and-migration/02-02-PLAN.md

<interfaces>
Existing exports from `lib/config.js`:
```javascript
function getSharedHome(homeDir = process.env.HOME) { /* canonical shared home */ }
function getGlobalConfigPath(homeDir = process.env.HOME) { /* canonical global config path */ }
function loadGlobalConfig(homeDir = process.env.HOME) { /* normalized global config */ }
function writeGlobalConfig(config, homeDir = process.env.HOME) { /* writes YAML config */ }
```

Expected exports from `lib/migrate.js` after Plan 02-02:
```javascript
function createMigrationPlan(options = {}) {}
function formatMigrationPlan(plan, options = {}) {}
function runMigration(options = {}) {}
function getMigrationStatus(options = {}) {}

module.exports = {
  createMigrationPlan,
  formatMigrationPlan,
  runMigration,
  getMigrationStatus
};
```

Current runtime guidance entrypoint:
```javascript
function resolveRuntimeConfig(options = {}) { /* merges global/project/change config */ }
module.exports = { buildRuntimeKernel, buildStatus, buildArtifactInstructions, buildApplyInstructions };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize shared-home and workspace config paths in constants, config, and install/check</name>
  <files>
lib/constants.js
lib/config.js
lib/install.js
  </files>
  <read_first>
lib/constants.js
lib/config.js
lib/install.js
lib/migrate.js
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-RESEARCH.md
.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
  </read_first>
  <action>Apply D-01, D-02, D-03, D-04, and D-20 directly in runtime constants and install/check code. In `lib/constants.js`, set `SHARED_HOME_NAME = '.opsx'` and `GLOBAL_CONFIG_NAME = 'config.yaml'` without renaming the exported identifiers. In `lib/config.js`, keep `getSharedHome()` and `getGlobalConfigPath()` as canonical `.opsx` helpers, add explicit legacy detectors `getLegacySharedHome(homeDir = process.env.HOME)` and `getLegacyGlobalConfigPath(homeDir = process.env.HOME)`, and export them so migration/check code can report old locations without treating them as canonical fallbacks. In `lib/install.js`, let install/uninstall/manifests naturally flow through the new canonical helpers, and rewrite `runCheck()` so the primary paths are `~/.opsx/config.yaml` and `.opsx/config.yaml`. `runCheck()` must print legacy candidates only as diagnostics using exact phrases `legacy shared-home candidate` and `legacy project config candidate`, and if legacy state exists it must recommend `opsx migrate --dry-run` rather than silently accepting the old location as current truth. Keep `homeDir` and `cwd` injection intact for the Wave 0 fixture suite.</action>
  <acceptance_criteria>
- `lib/constants.js` contains `const SHARED_HOME_NAME = '.opsx';` and `const GLOBAL_CONFIG_NAME = 'config.yaml';`.
- `lib/config.js` exports `getLegacySharedHome` and `getLegacyGlobalConfigPath` in addition to canonical helpers.
- `lib/install.js` `runCheck()` reports `.opsx/config.yaml` and `~/.opsx/config.yaml` as the primary workspace/shared-home paths.
- `runCheck()` uses the exact phrases `legacy shared-home candidate` and `legacy project config candidate` when old paths exist.
- No code path in these files treats `openspec/` or `~/.openspec/` as canonical success locations after the change.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`node bin/opsx.js check`
`rg -n "SHARED_HOME_NAME = '\\.opsx'|GLOBAL_CONFIG_NAME = 'config\\.yaml'|getLegacySharedHome|getLegacyGlobalConfigPath|legacy shared-home candidate|legacy project config candidate|\\.opsx/config\\.yaml|~\\/\\.opsx/config\\.yaml" lib/constants.js lib/config.js lib/install.js`
  </verify>
  <done>
- Canonical repo/home config paths are the only runtime truth in constants and loaders.
- `opsx check` stays honest about legacy state without turning old paths into hidden compatibility fallbacks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire real `opsx migrate` and canonical runtime guidance into the CLI</name>
  <files>
lib/runtime-guidance.js
lib/cli.js
  </files>
  <read_first>
lib/runtime-guidance.js
lib/cli.js
lib/migrate.js
lib/workspace.js
lib/install.js
.planning/phases/02-opsx-workspace-and-migration/02-CONTEXT.md
.planning/phases/02-opsx-workspace-and-migration/02-VALIDATION.md
.planning/phases/02-opsx-workspace-and-migration/02-PATTERNS.md
  </read_first>
  <action>Replace Phase 1 placeholder behavior with the real Phase 2 runtime wiring. In `lib/runtime-guidance.js`, rename the internal project root variable from the legacy `openspec` directory to `.opsx`, change per-change metadata loading from `.openspec.yaml` to `change.yaml`, and keep the existing safe change/capability guards plus schema-driven status logic unchanged beyond path updates. In `lib/cli.js`, import `runMigration` and `getMigrationStatus` from `./migrate`, remove `showMigratePlaceholder()`, and route `case 'migrate'` through `runMigration({ cwd: process.cwd(), homeDir: process.env.HOME, dryRun: options['dry-run'] === true })`. Update help text so the public syntax is `opsx migrate --dry-run` and `opsx migrate`. Replace `showStatusPlaceholder()` with a minimal truthful Phase 2 status renderer backed by `getMigrationStatus({ cwd: process.cwd(), homeDir: process.env.HOME })`; the output must still include `Current phase: Phase 2 (.opsx/ Workspace and Migration)`, must never claim durable change-state support, and when canonical `.opsx/config.yaml` is absent but legacy `openspec/config.yaml` exists it must include the exact sentence `Run \`opsx migrate --dry-run\` to preview migration.` Do not add any silent `openspec` fallback path or reintroduce Phase 1 placeholder wording.</action>
  <acceptance_criteria>
- `lib/runtime-guidance.js` resolves `.opsx/config.yaml` and `.opsx/changes/<change>/change.yaml` instead of `openspec/config.yaml` and `.openspec.yaml`.
- `lib/cli.js` imports `runMigration` and `getMigrationStatus`.
- `opsx --help` shows `opsx migrate --dry-run` and `opsx migrate` as real commands.
- `opsx status` includes `Current phase: Phase 2 (.opsx/ Workspace and Migration)` and does not claim durable Phase 4 state-machine behavior exists.
- When only legacy project state exists, status or check output includes `Run \`opsx migrate --dry-run\` to preview migration.`.
  </acceptance_criteria>
  <verify>
`npm run test:workflow-runtime`
`node bin/opsx.js --help`
`node bin/opsx.js migrate --dry-run`
`node bin/opsx.js status`
`rg -n "runMigration|getMigrationStatus|\\.opsx/config\\.yaml|change\\.yaml|Current phase: Phase 2 \\(\\.opsx/ Workspace and Migration\\)|Run \`opsx migrate --dry-run\` to preview migration\\." lib/runtime-guidance.js lib/cli.js`
  </verify>
  <done>
- The public CLI now routes to the real migration service.
- Runtime guidance reads canonical `.opsx` paths without hidden compatibility behavior.
- `opsx status` is minimally useful for migration verification while still deferring Phase 4 state-machine semantics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args and environment -> migration/runtime dispatch | `cwd`, `HOME`, and `--dry-run` determine whether file writes occur and which roots are inspected |
| runtime guidance -> workspace artifacts | Wrong canonical paths would cause the runtime to read stale legacy files or fabricated metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | `lib/cli.js`, `lib/runtime-guidance.js` | mitigate | Route migration/status through the preflight-guarded service and keep runtime path validation anchored to safe repo/change resolution. |
| T-02-02 | Tampering | `lib/install.js`, `lib/cli.js` | mitigate | Report legacy candidates only as diagnostics and keep execute-mode overwrites blocked in `lib/migrate.js`; no hidden compatibility fallback is allowed. |
| T-02-03 | Denial of Service / Tampering | `runCheck()`, `getMigrationStatus()` | mitigate | Make canonical-vs-legacy state visible so users do not operate against split repo/home layouts unknowingly. |
| T-02-04 | Repudiation | `opsx migrate --dry-run` dispatch | mitigate | Wire CLI dry-run directly to the shared migration plan service instead of formatting its own independent path list. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run `node bin/opsx.js --help`.
- Run `node bin/opsx.js migrate --dry-run`.
- Run `node bin/opsx.js status`.
</verification>

<success_criteria>
- Canonical `.opsx/` and `~/.opsx/` paths are the only runtime truth in constants, loaders, and guidance.
- `opsx migrate` is a real CLI command, not a placeholder.
- `opsx check` and minimal `opsx status` help users see legacy candidates and next migration action honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-opsx-workspace-and-migration/02-03-SUMMARY.md`
</output>
