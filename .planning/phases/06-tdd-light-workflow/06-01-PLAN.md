---
phase: 06-tdd-light-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config.js
  - templates/project/config.yaml.tmpl
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - TDD-01
must_haves:
  truths:
    - "`rules.tdd.mode` defaults to `strict` and normalizes invalid input without breaking runtime config loading."
    - "`rules.tdd.requireFor` and `rules.tdd.exempt` have stable list semantics that later checkpoint logic can trust."
    - "The shipped project config template and runtime normalization expose the same TDD-light defaults."
  artifacts:
    - path: "lib/config.js"
      provides: "Canonical `rules.tdd` normalization helpers and defaults"
      contains: "DEFAULT_TDD_CONFIG"
    - path: "templates/project/config.yaml.tmpl"
      provides: "Strict-mode TDD config seeds for new projects"
      contains: "tdd:"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Regression coverage for missing, invalid, and overridden `rules.tdd` values"
      contains: "normalizeConfig defaults rules.tdd to strict mode for behavior-change and bugfix"
  key_links:
    - from: "templates/project/config.yaml.tmpl"
      to: "lib/config.js"
      via: "template defaults and runtime normalization use the same `mode`, `requireFor`, and `exempt` values"
      pattern: "strict|behavior-change|bugfix|docs-only|copy-only|config-only"
    - from: "lib/config.js"
      to: "scripts/test-workflow-runtime.js"
      via: "normalizeConfig() regression tests lock invalid-mode fallback and merged list behavior"
      pattern: "normalizeConfig|rules\\.tdd"
---

<objective>
Lock the Phase 6 config contract before checkpoint parsing or guidance changes depend on it.

Purpose: implement D-01, D-02, and D-08 first so every later plan can rely on one canonical `rules.tdd` shape.
Output: normalized runtime defaults, seeded project config, and green regression coverage for TDD config semantics.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
@.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
@.planning/phases/06-tdd-light-workflow/06-PATTERNS.md

<interfaces>
From `lib/config.js`:
```javascript
function normalizeConfig(config) {
  const normalized = deepMerge({
    version: PACKAGE_VERSION,
    schema: DEFAULT_SCHEMA,
    language: DEFAULT_LANGUAGE,
    context: '',
    rules: {},
    securityReview: DEFAULT_SECURITY_REVIEW_CONFIG
  }, config || {});
}
```

From `templates/project/config.yaml.tmpl`:
```yaml
rules:
  proposal: "Call out rollout, migration, and compatibility impact."
  design: "Describe checkpoint and security-review behavior with rollback notes."
  tasks: "Keep top-level milestones dependency-ordered and include explicit verification steps."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize strict `rules.tdd` defaults and lock them with runtime regression coverage</name>
  <files>
lib/config.js
templates/project/config.yaml.tmpl
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Missing `rules.tdd` config falls back to `mode: strict`, `requireFor: [behavior-change, bugfix]`, and `exempt: [docs-only, copy-only, config-only]`.
    - Invalid `mode` values fall back to `strict` instead of producing undefined checkpoint behavior.
    - Custom classification lists remain additive so later plans can honor `migration-only` or `generated-refresh-only` when explicitly configured.
  </behavior>
  <read_first>
lib/config.js
templates/project/config.yaml.tmpl
scripts/test-workflow-runtime.js
.planning/REQUIREMENTS.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
  </read_first>
  <action>Implement D-01, D-02, and D-08 exactly. In `lib/config.js`, add `DEFAULT_TDD_CONFIG` with `mode: 'strict'`, `requireFor: ['behavior-change', 'bugfix']`, and `exempt: ['docs-only', 'copy-only', 'config-only']`; add a `normalizeTddConfig()` helper that accepts only `off`, `light`, or `strict`, de-duplicates non-empty list entries, and merges caller-provided list values without discarding explicit extras such as `migration-only` or `generated-refresh-only`. Wire `normalizeConfig()` so `normalized.rules.tdd` is always present after `normalized.rules` is validated as an object. In `templates/project/config.yaml.tmpl`, add a `rules.tdd` block with the same strict defaults and list contents. In `scripts/test-workflow-runtime.js`, add regression tests named `normalizeConfig defaults rules.tdd to strict mode for behavior-change and bugfix`, `normalizeConfig repairs invalid rules.tdd values and merges custom classification lists`, and `project config template seeds rules.tdd strict defaults`. Do not add a new config file, new dependency, or compatibility alias such as `tddLight`.</action>
  <acceptance_criteria>
- `lib/config.js` contains `DEFAULT_TDD_CONFIG`, `normalizeTddConfig`, and the exact strings `'strict'`, `'behavior-change'`, `'bugfix'`, `'docs-only'`, `'copy-only'`, and `'config-only'`.
- `templates/project/config.yaml.tmpl` contains a `tdd:` block with `mode: "strict"` and both `requireFor` / `exempt` lists.
- `scripts/test-workflow-runtime.js` contains the exact three Phase 6 config test names listed in the action.
- `npm run test:workflow-runtime` passes with the new config normalization tests enabled.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "DEFAULT_TDD_CONFIG|normalizeTddConfig|mode: \\\"strict\\\"|behavior-change|bugfix|docs-only|copy-only|config-only|normalizeConfig defaults rules.tdd to strict mode for behavior-change and bugfix|normalizeConfig repairs invalid rules.tdd values and merges custom classification lists|project config template seeds rules.tdd strict defaults" lib/config.js templates/project/config.yaml.tmpl scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- TDD-01 is implemented in both runtime normalization and shipped project defaults.
- Later Phase 6 plans can consume `config.rules.tdd` without guessing or backfilling missing keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/project/change config -> `normalizeConfig()` | invalid or partial `rules.tdd` values can silently alter checkpoint enforcement |
| template defaults -> runtime config | mismatched defaults can make generated guidance disagree with checkpoint behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `lib/config.js` | mitigate | Normalize `mode`, `requireFor`, and `exempt` through one `normalizeTddConfig()` helper and test invalid-mode fallback explicitly. |
| T-06-02 | Information Integrity | `templates/project/config.yaml.tmpl` | mitigate | Keep template defaults byte-aligned with `DEFAULT_TDD_CONFIG` and lock them with a template assertion in the runtime suite. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the config runtime, template, and test file for the exact `rules.tdd` defaults and Phase 6 test names.
</verification>

<success_criteria>
- TDD-01 is satisfied with one canonical strict-by-default config contract.
- Invalid `rules.tdd.mode` values cannot silently bypass later checkpoint logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-01-SUMMARY.md`
</output>
