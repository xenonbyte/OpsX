---
phase: 06-tdd-light-workflow
plan: 09
type: execute
wave: 6
depends_on:
  - 06-06
  - 06-07
  - 06-08
files_modified:
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - TDD-04
must_haves:
  truths:
    - "Temporary Phase 6 parity drift is removed once all 12 prompts are refreshed."
    - "The runtime suite restores strict repo-wide checked-in parity while preserving explicit TDD-light prompt assertions."
    - "Generated and checked-in TDD-light prompt surfaces are verified over the same 12 route files."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Final Phase 6 parity closure and checked-in prompt assertions"
      contains: "rules.tdd.mode"
  key_links:
    - from: "lib/generator.js"
      to: "commands/**"
      via: "full bundle parity is restored after the 12-file Phase 6 refresh wave"
      pattern: "buildPlatformBundle"
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/**"
      via: "the same 12 prompts are checked for both generated output and checked-in content"
      pattern: "PHASE6_TDD_PROMPT_PATHS"
---

<objective>
Remove the temporary Phase 6 prompt allowlist and close the phase with one strict regression gate.

Purpose: ensure the shipped TDD-light prompt wording is verified across source output and checked-in files with no lingering parity escape hatch.
Output: a single runtime suite file with strict parity restored and explicit checked-in assertions for the 12 Phase 6 prompts.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
@scripts/test-workflow-runtime.js
@lib/generator.js

<interfaces>
From `scripts/test-workflow-runtime.js`:
```javascript
function collectBundleParity(platform, generatedBundle) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the temporary Phase 6 parity allowlist and re-lock full regression coverage</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
lib/generator.js
commands/claude/opsx/apply.md
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
commands/codex/prompts/opsx-apply.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
commands/gemini/opsx/apply.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
  </read_first>
  <action>Remove the temporary Phase 6 prompt allowlist introduced in 06-05 and restore strict repo-wide checked-in parity against `buildPlatformBundle()` output. Keep the source-output assertions for the same 12 Phase 6 prompt files, and add explicit checked-in assertions that those 12 files contain `rules.tdd.mode`, `TDD Exemption:`, and, for the three `apply` prompts, `completed TDD steps` plus `diff summary`. After this plan, `PHASE6_TDD_PROMPT_PATHS` must no longer act as a mismatched-file escape hatch; every platform bundle must require `parity.mismatched` to be empty again. Do not broaden the checked-in assertion scope beyond the 12 prompts refreshed in 06-06 through 06-08.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` no longer uses `PHASE6_TDD_PROMPT_PATHS` as a mismatch allowlist.
- Repo-wide bundle parity is strict again for Claude, Codex, and Gemini with no Phase 6 exceptions.
- The final regression file asserts checked-in content for exactly the same 12 Phase 6 prompt files refreshed in 06-06 through 06-08.
- `npm run test:workflow-runtime` passes after the temporary allowlist is removed.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "rules.tdd.mode|TDD Exemption:|completed TDD steps|diff summary|PHASE6_TDD_PROMPT_PATHS|parity\\.mismatched" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Phase 6 closes with strict checked-in command parity restored and no temporary prompt drift allowances.
- The runtime suite protects both generated source output and checked-in TDD-light prompts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temporary prompt allowlist -> final repo state | a lingering allowlist can hide stale checked-in prompt content |
| checked-in TDD-light prompts -> workflow users | stale prompt text can contradict runtime checkpoint behavior after Phase 6 ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | `scripts/test-workflow-runtime.js` parity gate | mitigate | Remove the temporary Phase 6 prompt allowlist entirely and restore strict `parity.mismatched === []` assertions. |
| T-06-11 | Information Integrity | checked-in Phase 6 prompts | mitigate | Assert the exact 12 checked-in prompt files contain the shipped `rules.tdd.mode` / `TDD Exemption:` / `completed TDD steps` wording. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the final regression file for the Phase 6 prompt contract strings and the absence of a temporary mismatch allowlist.
</verification>

<success_criteria>
- Phase 6 ends with strict parity restored and one green regression command.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-09-SUMMARY.md`
</output>
