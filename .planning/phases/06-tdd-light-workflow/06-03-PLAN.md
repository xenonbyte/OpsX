---
phase: 06-tdd-light-workflow
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - lib/workflow.js
  - lib/runtime-guidance.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - TDD-02
  - TDD-03
must_haves:
  truths:
    - "`task-checkpoint` enforces `RED` / `VERIFY` only for required task classes, not for every group."
    - "Explicit `TDD Exemption:` markers and `TDD Class:` markers win over heuristics."
    - "Apply guidance surfaces `rules.tdd.mode` and TDD blocker codes without adding a new checkpoint id or lifecycle stage."
  artifacts:
    - path: "lib/workflow.js"
      provides: "TDD-aware task parsing, classification, and checkpoint findings"
      contains: "tdd-red-missing"
    - path: "lib/runtime-guidance.js"
      provides: "Apply instructions that expose TDD mode and next-group TDD metadata"
      contains: "tddMode"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Mode matrix tests for `off`, `light`, and `strict`"
      contains: "task checkpoint strict mode blocks missing RED and VERIFY for behavior-change groups"
  key_links:
    - from: "skills/opsx/references/artifact-templates*.md"
      to: "lib/workflow.js"
      via: "the runtime recognizes the exact `## Test Plan`, `TDD Class:`, and `TDD Exemption:` markers documented in Phase 6 guidance"
      pattern: "## Test Plan|TDD Class:|TDD Exemption:"
    - from: "lib/workflow.js"
      to: "lib/runtime-guidance.js"
      via: "apply instructions surface checkpoint severity, finding codes, and the selected group's TDD classification"
      pattern: "runTaskCheckpoint|tddMode"
---

<objective>
Teach the existing `task-checkpoint` and apply guidance to understand TDD-light semantics without expanding the workflow surface.

Purpose: implement D-03, D-07, D-09, D-10, D-11, D-13, D-15, and D-16 through the existing Phase 4/5 runtime path.
Output: TDD-aware task classification, exact warning/block finding codes, and apply guidance that surfaces TDD blockers before `apply`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
@.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
@.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-05-SUMMARY.md

<interfaces>
From `lib/workflow.js`:
```javascript
function extractTopLevelTaskGroups(tasksText) {}
function runTaskCheckpoint(options = {}) {}
function buildCheckpointResult(schema, checkpointId, findings = [], extra = {}) {}
```

From `lib/runtime-guidance.js`:
```javascript
function parseTopLevelTaskGroups(tasksText) {}
function buildApplyInstructions(options = {}) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce TDD-light in `task-checkpoint` and surface the result in apply guidance</name>
  <files>
lib/workflow.js
lib/runtime-guidance.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - `strict` blocks required groups that lack `RED` or `VERIFY`.
    - `light` warns on the same missing evidence but does not block.
    - `off` skips TDD-specific findings entirely.
    - `GREEN` remains expected structure but never becomes its own blocking code, and `REFACTOR` stays optional.
  </behavior>
  <read_first>
lib/workflow.js
lib/runtime-guidance.js
scripts/test-workflow-runtime.js
skills/opsx/references/artifact-templates.md
skills/opsx/references/artifact-templates-zh.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
  </read_first>
  <action>Implement D-03, D-07, D-09, D-10, D-11, D-13, D-15, and D-16 without adding a new checkpoint id, new lifecycle stage, markdown-parser dependency, or standalone TDD artifact. In `lib/workflow.js`, add exact helpers named `extractTestPlanSection(tasksText)`, `parseTddGroupMetadata(groupText)`, `classifyTaskGroupTdd(group, evidence, config)`, and `appendTddTaskCheckpointFindings(findings, groups, evidence, config)`. Parse the exact markers `## Test Plan`, `TDD Class: <class>`, and `TDD Exemption: <class> — <reason>`; explicit markers must override heuristics. Honor visible exemptions for `docs-only`, `copy-only`, `config-only`, `migration-only`, and `generated-refresh-only`, even though only the first three are default config exemptions per D-08. Add these exact finding codes: `tdd-test-plan-missing` as a `WARN` for required groups missing `## Test Plan`; `tdd-red-missing` and `tdd-verify-missing` as `WARN` in `light` mode and `BLOCK` in `strict` mode; `tdd-manual-verify-rationale-missing` as `WARN` when the `Verification:` line or `VERIFY:` step is manual-only without a reason after `manual —`. Do not create a blocking code for `GREEN` or `REFACTOR`. In `lib/runtime-guidance.js`, extend `buildApplyInstructions()` so the payload and text output expose `tddMode`, `nextTaskGroupClass`, `nextTaskGroupExempt`, and the TDD-related checkpoint findings before `apply` begins. In `scripts/test-workflow-runtime.js`, add the exact tests `task checkpoint strict mode blocks missing RED and VERIFY for behavior-change groups`, `task checkpoint light mode warns missing RED and VERIFY for behavior-change groups`, `task checkpoint off mode skips TDD findings`, `task checkpoint prefers explicit TDD Exemption over heuristic classification`, and `apply instructions surface tdd mode and blocker codes`.</action>
  <acceptance_criteria>
- `lib/workflow.js` contains `extractTestPlanSection`, `parseTddGroupMetadata`, `classifyTaskGroupTdd`, `appendTddTaskCheckpointFindings`, and the exact finding codes `tdd-test-plan-missing`, `tdd-red-missing`, `tdd-verify-missing`, and `tdd-manual-verify-rationale-missing`.
- `lib/runtime-guidance.js` exposes `tddMode`, `nextTaskGroupClass`, and `nextTaskGroupExempt` from `buildApplyInstructions()`.
- `scripts/test-workflow-runtime.js` contains the exact five test names listed in the action.
- No new checkpoint id, lifecycle stage, parser dependency, or `tdd` artifact file is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "extractTestPlanSection|parseTddGroupMetadata|classifyTaskGroupTdd|appendTddTaskCheckpointFindings|tdd-test-plan-missing|tdd-red-missing|tdd-verify-missing|tdd-manual-verify-rationale-missing|tddMode|nextTaskGroupClass|nextTaskGroupExempt|task checkpoint strict mode blocks missing RED and VERIFY for behavior-change groups|task checkpoint light mode warns missing RED and VERIFY for behavior-change groups|task checkpoint off mode skips TDD findings|task checkpoint prefers explicit TDD Exemption over heuristic classification|apply instructions surface tdd mode and blocker codes" lib/workflow.js lib/runtime-guidance.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- TDD-03 is implemented on the existing `task-checkpoint`.
- Behavior-changing work cannot reach `apply` in `strict` mode without explicit `RED` and `VERIFY`, while exempt work stays visibly exempt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `tasks.md` markers -> checkpoint classification | incorrect precedence can let silent exemptions or missing RED/VERIFY slip through |
| checkpoint findings -> apply guidance | missing TDD findings in apply instructions can let users execute the wrong task group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | `lib/workflow.js` | mitigate | Treat exact `TDD Exemption: <class> — <reason>` markers as authoritative and do not honor silent exemptions. |
| T-06-03 | Spoofing | task parsing / `## Test Plan` | mitigate | Parse `## Test Plan` separately from numbered groups and keep regex-based numbered-group selection unchanged. |
| T-06-01 | Tampering | `rules.tdd.mode` consumption | mitigate | Respect only normalized `off`, `light`, and `strict` values from `normalizeConfig()` and lock the mode matrix in tests. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the workflow, runtime-guidance, and test files for the exact helper names, finding codes, and test names.
</verification>

<success_criteria>
- TDD-03 is satisfied using the existing `task-checkpoint` surface.
- Apply guidance clearly shows why a task group is blocked or warned under TDD-light.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-03-SUMMARY.md`
</output>
