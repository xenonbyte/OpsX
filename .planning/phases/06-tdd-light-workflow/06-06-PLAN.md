---
phase: 06-tdd-light-workflow
plan: 06
type: execute
wave: 5
depends_on:
  - 06-05
files_modified:
  - commands/claude/opsx/apply.md
  - commands/claude/opsx/propose.md
  - commands/claude/opsx/continue.md
  - commands/claude/opsx/ff.md
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - TDD-04
must_haves:
  truths:
    - "The checked-in Claude `apply`, `propose`, `continue`, and `ff` prompts advertise shipped Phase 6 TDD-light behavior."
    - "The Claude TDD-light slice matches current generator output exactly with no hand-edited drift."
  artifacts:
    - path: "commands/claude/opsx/apply.md"
      provides: "Claude apply prompt updated for execution-proof wording"
      contains: "completed TDD steps"
    - path: "commands/claude/opsx/propose.md"
      provides: "Claude planning prompt updated for `rules.tdd.mode` wording"
      contains: "rules.tdd.mode"
  key_links:
    - from: "lib/generator.js"
      to: "commands/claude/opsx/{apply,propose,continue,ff}.md"
      via: "buildPlatformBundle('claude') renders the bounded Phase 6 route copy"
      pattern: "rules\\.tdd\\.mode|completed TDD steps"
---

<objective>
Refresh the bounded Claude Phase 6 TDD-light prompt slice from the updated generator output.

Purpose: ship the new planning and execution checkpoint wording to checked-in Claude prompts without touching unrelated generated files.
Output: regenerated Claude `apply`, `propose`, `continue`, and `ff` prompts.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
@lib/generator.js
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Claude `apply` / `propose` / `continue` / `ff` slice from `buildPlatformBundle('claude')`</name>
  <files>
commands/claude/opsx/apply.md
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
  </files>
  <read_first>
lib/generator.js
commands/claude/opsx/apply.md
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
scripts/test-workflow-runtime.js
.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
  </read_first>
  <action>Refresh exactly these four checked-in Claude files from `buildPlatformBundle('claude')`: `commands/claude/opsx/apply.md`, `propose.md`, `continue.md`, and `ff.md`. Do not hand-edit prompt prose. Do not touch `commands/claude/opsx.md` or any other Claude action file. The refreshed prompts must include the exact Phase 6 source-of-truth wording for `rules.tdd.mode`, `RED`, `VERIFY`, `TDD Exemption:`, and, for `apply`, `completed TDD steps`, `verification command/result`, `diff summary`, and `drift`.</action>
  <acceptance_criteria>
- Only the four plan-listed Claude files are modified.
- `commands/claude/opsx/propose.md`, `continue.md`, and `ff.md` contain `rules.tdd.mode` and `TDD Exemption:`.
- `commands/claude/opsx/apply.md` contains `completed TDD steps` and `diff summary`.
- The four refreshed files match `buildPlatformBundle('claude')` byte-for-byte.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const {buildPlatformBundle}=require('./lib/generator');const bundle=buildPlatformBundle('claude');const files=['opsx/apply.md','opsx/propose.md','opsx/continue.md','opsx/ff.md'];for(const file of files){const actual=fs.readFileSync(path.join('commands','claude',file),'utf8');if(actual!==bundle[file]){throw new Error(file);}}"</automated>
  </verify>
  <done>
- The checked-in Claude TDD-light prompt slice matches source-of-truth generator output exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator source-of-truth -> checked-in Claude prompts | partial or manual refreshes can leave Claude guidance stale even when runtime behavior is correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | Claude `apply` / `propose` / `continue` / `ff` prompts | mitigate | Regenerate only the listed files from `buildPlatformBundle('claude')` and verify byte-for-byte parity. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run the exact Claude parity assertion for the four plan-listed files.
</verification>

<success_criteria>
- Claude checked-in prompts expose shipped Phase 6 TDD-light wording with no manual drift.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-06-SUMMARY.md`
</output>
