---
phase: 06-tdd-light-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/opsx/SKILL.md
  - skills/opsx/references/artifact-templates.md
  - skills/opsx/references/artifact-templates-zh.md
  - skills/opsx/references/action-playbooks.md
  - skills/opsx/references/action-playbooks-zh.md
autonomous: true
requirements:
  - TDD-02
must_haves:
  truths:
    - "Task authors can see a concrete `## Test Plan` shape plus explicit `RED` / `GREEN` / `REFACTOR` / `VERIFY` examples for behavior work."
    - "Exempt work uses visible `TDD Exemption:` markers and still includes a verification action."
    - "Distributed skill guidance no longer says TDD-light is deferred to Phase 6."
  artifacts:
    - path: "skills/opsx/references/artifact-templates.md"
      provides: "English `tasks.md` source-of-truth with machine-readable TDD markers"
      contains: "## Test Plan"
    - path: "skills/opsx/references/artifact-templates-zh.md"
      provides: "Chinese `tasks.md` source-of-truth with the same exact machine-readable markers"
      contains: "TDD Class:"
    - path: "skills/opsx/SKILL.md"
      provides: "Distributed OpsX workflow contract updated for shipped TDD-light rules"
      contains: "rules.tdd.mode"
  key_links:
    - from: "skills/opsx/references/artifact-templates*.md"
      to: "lib/workflow.js"
      via: "runtime parsing will rely on the exact `## Test Plan`, `TDD Class:`, `TDD Exemption:`, `RED`, and `VERIFY` markers described here"
      pattern: "## Test Plan|TDD Class:|TDD Exemption:|RED:|VERIFY:"
    - from: "skills/opsx/SKILL.md"
      to: "skills/opsx/references/action-playbooks*.md"
      via: "distributed workflow guidance and route playbooks describe the same TDD-light semantics"
      pattern: "rules\\.tdd\\.mode|completed TDD steps"
---

<objective>
Define the authored `tasks.md` shape and operator guidance before runtime enforcement starts parsing it.

Purpose: implement D-04 through D-07, D-10, D-13, and D-17 in the source-of-truth docs so execution work has explicit markers instead of vague prose.
Output: updated skill guidance plus English/Chinese task templates and playbooks with visible TDD markers and exemptions.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
@.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
@.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add machine-readable `tasks.md` TDD markers and examples in both artifact templates</name>
  <files>
skills/opsx/references/artifact-templates.md
skills/opsx/references/artifact-templates-zh.md
  </files>
  <read_first>
skills/opsx/references/artifact-templates.md
skills/opsx/references/artifact-templates-zh.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
  </read_first>
  <action>Implement D-05, D-06, D-07, and D-10 in the authored `tasks.md` examples. In both artifact-template files, replace the old generic tasks guidance with a concrete `## Test Plan` block that uses the exact lines `- Behavior: ...`, `- Requirement/Scenario: ...`, `- Verification: ...`, `- TDD Mode: strict`, and `- Exemption Reason: none`. Then add one behavior-change example that uses the exact machine-readable markers `- TDD Class: behavior-change`, `RED:`, `GREEN:`, optional `REFACTOR:`, and `VERIFY:` inside a numbered top-level task group. Add one exempt example that uses the exact line `- TDD Exemption: docs-only — ...` and still includes a `VERIFY:` checklist item. Keep the Chinese file localized in prose, but preserve the exact English marker tokens `## Test Plan`, `TDD Class:`, `TDD Exemption:`, `RED`, `GREEN`, `REFACTOR`, and `VERIFY` so runtime parsing stays locale-independent. Do not introduce a new artifact such as `tdd.md` or a second task grammar.</action>
  <acceptance_criteria>
- Both artifact-template files contain `## Test Plan`, `TDD Class: behavior-change`, `TDD Exemption: docs-only`, `RED:`, `GREEN:`, `REFACTOR:`, and `VERIFY:`.
- The exempt example still contains a `VERIFY:` checklist item and a visible reason after the em dash in `TDD Exemption: ... — ...`.
- The Chinese file keeps the exact machine-readable marker strings in English while the surrounding explanatory prose is Chinese.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "## Test Plan|TDD Class: behavior-change|TDD Exemption: docs-only|RED:|GREEN:|REFACTOR:|VERIFY:" skills/opsx/references/artifact-templates.md skills/opsx/references/artifact-templates-zh.md</automated>
  </verify>
  <done>
- The authored `tasks.md` examples expose one exact marker format that runtime code can parse deterministically.
- Exempt and behavior-changing task groups are distinguishable without heuristics alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Phase 6 deferral wording with shipped TDD-light guidance in skill and playbooks</name>
  <files>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
  </files>
  <read_first>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
skills/opsx/references/artifact-templates.md
skills/opsx/references/artifact-templates-zh.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
  </read_first>
  <action>Implement D-04, D-07, D-13, and D-17 across distributed guidance. In `skills/opsx/SKILL.md`, remove the exact sentence that says TDD-light is deferred to Phase 6 and replace it with shipped guidance that `task checkpoint` uses `rules.tdd.mode`, enforces `RED` and `VERIFY` for `behavior-change` and `bugfix` groups, allows visible `TDD Exemption:` reasons for exempt work, and keeps `REFACTOR` optional. Update the execution loop so `apply` records completed TDD steps, verification command/result, diff summary, and drift through existing state paths. In both action playbooks, replace the Phase 6 deferral line with explicit route guidance that authored tasks must use `## Test Plan`, `TDD Class:` or `TDD Exemption:`, and `VERIFY:`; manual-only verification is allowed only when the `Verification:` line explains why automated checks are not practical. Keep Phase 7 deferrals intact. Do not add a new route, new lifecycle stage, or a separate TDD review artifact.</action>
  <acceptance_criteria>
- `skills/opsx/SKILL.md` contains `rules.tdd.mode`, `TDD Exemption:`, and `completed TDD steps`.
- Both playbooks contain `## Test Plan`, `TDD Class:`, and `manual-only verification` guidance instead of the old `deferred to Phase 6` wording.
- The exact phrase `deferred to Phase 6` no longer appears in `skills/opsx/SKILL.md` or either action playbook.
- Phase 7 hard-gate deferrals remain present.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "rules.tdd.mode|TDD Exemption:|completed TDD steps|manual-only verification|延后到 Phase 6|deferred to Phase 6|Phase 7" skills/opsx/SKILL.md skills/opsx/references/action-playbooks.md skills/opsx/references/action-playbooks-zh.md</automated>
  </verify>
  <done>
- Distributed skill guidance now matches the Phase 6 contract instead of deferring it.
- Operators are told exactly how exempt work, manual verification, and one-group apply evidence should be written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored task guidance -> runtime parser | ambiguous or localized-only markers can break deterministic TDD classification |
| distributed skill/playbook docs -> operator behavior | stale deferral wording can cause users to omit required RED/VERIFY structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | artifact templates / playbooks | mitigate | Require visible `TDD Exemption:` reasons and keep the exact machine-readable marker strings in both locales. |
| T-06-03 | Spoofing | `## Test Plan` and task-group structure | mitigate | Document one exact marker grammar (`## Test Plan`, `TDD Class:`, `TDD Exemption:`, `RED`, `VERIFY`) so runtime parsing can stay regex-based. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the skill and bilingual guidance files for the exact Phase 6 marker strings and the removal of `deferred to Phase 6`.
</verification>

<success_criteria>
- TDD-02 is satisfied in both English and Chinese guidance.
- The authored task structure exposes explicit TDD markers and visible exemptions without new artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-02-SUMMARY.md`
</output>
