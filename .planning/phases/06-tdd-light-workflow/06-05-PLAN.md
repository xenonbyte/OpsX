---
phase: 06-tdd-light-workflow
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - lib/generator.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - TDD-04
must_haves:
  truths:
    - "Generated route source-of-truth copy for `apply`, `propose`, `continue`, and `ff` explains shipped TDD-light semantics."
    - "The repo stays green during bounded prompt refreshes via a temporary allowlist that covers only the 12 changed checked-in prompts."
    - "No Phase 6 guidance remains source-only; the runtime suite asserts it in generated output before checked-in refreshes land."
  artifacts:
    - path: "lib/generator.js"
      provides: "Action-scoped TDD-light checkpoint notes for planning and apply routes"
      contains: "rules.tdd.mode"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Temporary 12-file Phase 6 parity gate and source-output assertions"
      contains: "PHASE6_TDD_PROMPT_PATHS"
  key_links:
    - from: "lib/generator.js"
      to: "commands/**"
      via: "only `apply`, `propose`, `continue`, and `ff` prompts are refreshed from new TDD-light wording"
      pattern: "rules\\.tdd\\.mode|completed TDD steps"
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/**"
      via: "strict parity remains active for untouched prompts while the 12 planned refresh files are temporarily allowlisted"
      pattern: "PHASE6_TDD_PROMPT_PATHS"
---

<objective>
Update generator source-of-truth copy for shipped TDD-light behavior and stage a bounded parity window for checked-in prompt refreshes.

Purpose: implement D-17 after runtime behavior is real, while keeping prompt refresh scope limited to the 12 action prompts that actually need new TDD-light wording.
Output: updated generator notes plus a temporary Phase 6 parity allowlist and source-output assertions.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
@.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
@.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
@.planning/phases/05-spec-split-checkpoint/05-03-SUMMARY.md
@.planning/phases/05-spec-split-checkpoint/05-07-SUMMARY.md

<interfaces>
From `lib/generator.js`:
```javascript
function getPlanningCheckpointNote(actionId) {}
function buildActionMarkdown(platform, action) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope Phase 6 prompt wording to the 12 TDD-light action prompts and stage a temporary parity gate</name>
  <files>
lib/generator.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - `apply`, `propose`, `continue`, and `ff` advertise shipped TDD-light semantics.
    - Untouched routes keep existing wording so the generated refresh scope stays bounded.
    - Temporary parity drift is limited to exactly 12 checked-in files until the refresh plans land.
  </behavior>
  <read_first>
lib/generator.js
scripts/test-workflow-runtime.js
commands/claude/opsx/apply.md
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
commands/codex/prompts/opsx-apply.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
commands/gemini/opsx/apply.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
  </read_first>
  <action>Implement D-17 with bounded source-output drift. In `lib/generator.js`, keep the existing `getPlanningCheckpointNote(actionId)` helper but make it action-specific: for `propose`, `continue`, and `ff`, the note must mention `spec-split-checkpoint`, `spec checkpoint`, and `task checkpoint`, and must explicitly say `task checkpoint` uses `rules.tdd.mode`, requires `RED` and `VERIFY` for `behavior-change` and `bugfix` groups, and accepts visible `TDD Exemption:` reasons for exempt work. For `apply`, update the planning note to mention `task checkpoint` plus the same `rules.tdd.mode` behavior, and add a new helper `getExecutionCheckpointNote(actionId)` so only `apply` says `execution checkpoint` records completed TDD steps, verification command/result, diff summary, and drift after each top-level task group during `apply`. Leave all other action prompts and index/catalog files unchanged to avoid a repo-wide refresh. In `scripts/test-workflow-runtime.js`, add a temporary allowlist constant named `PHASE6_TDD_PROMPT_PATHS` containing exactly these 12 checked-in files: Claude/Codex/Gemini `apply`, `propose`, `continue`, and `ff`. Keep strict parity for every other generated file. Add source-output assertions that generated content for those 12 routes contains the exact strings `rules.tdd.mode`, `RED`, `VERIFY`, `TDD Exemption:`, and `completed TDD steps` where applicable. Do not hand-edit any checked-in command file in this plan.</action>
  <acceptance_criteria>
- `lib/generator.js` contains `rules.tdd.mode`, `TDD Exemption:`, and `completed TDD steps` in action-scoped note helpers for `apply`, `propose`, `continue`, and `ff`.
- `scripts/test-workflow-runtime.js` defines `PHASE6_TDD_PROMPT_PATHS` with exactly 12 entries spanning Claude/Codex/Gemini `apply`, `propose`, `continue`, and `ff`.
- Strict generated-vs-checked-in parity remains active for all non-listed files.
- No checked-in file under `commands/**` is modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "rules.tdd.mode|TDD Exemption:|completed TDD steps|PHASE6_TDD_PROMPT_PATHS|opsx-apply|opsx-propose|opsx-continue|opsx-ff" lib/generator.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The repo has one bounded source-of-truth path for shipped Phase 6 route copy.
- Generated refresh work can proceed by platform without failing unrelated prompt parity checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator source-of-truth -> checked-in prompts | unbounded note changes can churn unrelated command files and hide stale checked-in content |
| temporary parity gate -> final repo state | an over-broad allowlist can hide drift outside the intended 12 files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | `lib/generator.js` | mitigate | Scope Phase 6 note changes to `apply`, `propose`, `continue`, and `ff` only. |
| T-06-06 | Information Integrity | `scripts/test-workflow-runtime.js` temporary allowlist | mitigate | Keep `PHASE6_TDD_PROMPT_PATHS` limited to the exact 12 plan-listed checked-in prompts and preserve strict parity for all other files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the generator and runtime suite for the exact Phase 6 route-copy strings and the 12-file temporary allowlist constant.
</verification>

<success_criteria>
- Phase 6 route-copy changes are source-of-truth driven and mechanically refreshable.
- The repo can stay green while the 12 changed checked-in prompts are refreshed in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-05-SUMMARY.md`
</output>
