---
phase: 06-tdd-light-workflow
plan: 07
type: execute
wave: 5
depends_on:
  - 06-05
files_modified:
  - commands/codex/prompts/opsx-apply.md
  - commands/codex/prompts/opsx-propose.md
  - commands/codex/prompts/opsx-continue.md
  - commands/codex/prompts/opsx-ff.md
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - TDD-04
must_haves:
  truths:
    - "The checked-in Codex `apply`, `propose`, `continue`, and `ff` prompts advertise shipped Phase 6 TDD-light behavior."
    - "The Codex TDD-light slice matches current generator output exactly with no hand-edited drift."
  artifacts:
    - path: "commands/codex/prompts/opsx-apply.md"
      provides: "Codex apply prompt updated for execution-proof wording"
      contains: "completed TDD steps"
    - path: "commands/codex/prompts/opsx-propose.md"
      provides: "Codex planning prompt updated for `rules.tdd.mode` wording"
      contains: "rules.tdd.mode"
  key_links:
    - from: "lib/generator.js"
      to: "commands/codex/prompts/{opsx-apply,opsx-propose,opsx-continue,opsx-ff}.md"
      via: "buildPlatformBundle('codex') renders the bounded Phase 6 route copy"
      pattern: "rules\\.tdd\\.mode|completed TDD steps"
---

<objective>
Refresh the bounded Codex Phase 6 TDD-light prompt slice from the updated generator output.

Purpose: ship the new planning and execution checkpoint wording to checked-in Codex prompts without touching unrelated generated files.
Output: regenerated Codex `apply`, `propose`, `continue`, and `ff` prompts.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
@lib/generator.js
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Codex `apply` / `propose` / `continue` / `ff` slice from `buildPlatformBundle('codex')`</name>
  <files>
commands/codex/prompts/opsx-apply.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
  </files>
  <read_first>
lib/generator.js
commands/codex/prompts/opsx-apply.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
scripts/test-workflow-runtime.js
.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
  </read_first>
  <action>Refresh exactly these four checked-in Codex files from `buildPlatformBundle('codex')`: `commands/codex/prompts/opsx-apply.md`, `opsx-propose.md`, `opsx-continue.md`, and `opsx-ff.md`. Do not hand-edit prompt prose. Do not touch `commands/codex/prompts/opsx.md` or any other Codex prompt. The refreshed prompts must include the exact Phase 6 source-of-truth wording for `rules.tdd.mode`, `RED`, `VERIFY`, `TDD Exemption:`, and, for `opsx-apply.md`, `completed TDD steps`, `verification command/result`, `diff summary`, and `drift`.</action>
  <acceptance_criteria>
- Only the four plan-listed Codex files are modified.
- `commands/codex/prompts/opsx-propose.md`, `opsx-continue.md`, and `opsx-ff.md` contain `rules.tdd.mode` and `TDD Exemption:`.
- `commands/codex/prompts/opsx-apply.md` contains `completed TDD steps` and `diff summary`.
- The four refreshed files match `buildPlatformBundle('codex')` byte-for-byte.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const {buildPlatformBundle}=require('./lib/generator');const bundle=buildPlatformBundle('codex');const files=['prompts/opsx-apply.md','prompts/opsx-propose.md','prompts/opsx-continue.md','prompts/opsx-ff.md'];for(const file of files){const actual=fs.readFileSync(path.join('commands','codex',file),'utf8');if(actual!==bundle[file]){throw new Error(file);}}"</automated>
  </verify>
  <done>
- The checked-in Codex TDD-light prompt slice matches source-of-truth generator output exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator source-of-truth -> checked-in Codex prompts | partial or manual refreshes can leave Codex guidance stale even when runtime behavior is correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | Codex `apply` / `propose` / `continue` / `ff` prompts | mitigate | Regenerate only the listed files from `buildPlatformBundle('codex')` and verify byte-for-byte parity. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run the exact Codex parity assertion for the four plan-listed files.
</verification>

<success_criteria>
- Codex checked-in prompts expose shipped Phase 6 TDD-light wording with no manual drift.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-07-SUMMARY.md`
</output>
