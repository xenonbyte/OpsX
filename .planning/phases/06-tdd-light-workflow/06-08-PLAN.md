---
phase: 06-tdd-light-workflow
plan: 08
type: execute
wave: 5
depends_on:
  - 06-05
files_modified:
  - commands/gemini/opsx/apply.toml
  - commands/gemini/opsx/propose.toml
  - commands/gemini/opsx/continue.toml
  - commands/gemini/opsx/ff.toml
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - TDD-04
must_haves:
  truths:
    - "The checked-in Gemini `apply`, `propose`, `continue`, and `ff` prompts advertise shipped Phase 6 TDD-light behavior."
    - "The Gemini TDD-light slice matches current generator output exactly with no hand-edited drift."
  artifacts:
    - path: "commands/gemini/opsx/apply.toml"
      provides: "Gemini apply prompt updated for execution-proof wording"
      contains: "completed TDD steps"
    - path: "commands/gemini/opsx/propose.toml"
      provides: "Gemini planning prompt updated for `rules.tdd.mode` wording"
      contains: "rules.tdd.mode"
  key_links:
    - from: "lib/generator.js"
      to: "commands/gemini/opsx/{apply,propose,continue,ff}.toml"
      via: "buildPlatformBundle('gemini') renders the bounded Phase 6 route copy"
      pattern: "rules\\.tdd\\.mode|completed TDD steps"
---

<objective>
Refresh the bounded Gemini Phase 6 TDD-light prompt slice from the updated generator output.

Purpose: ship the new planning and execution checkpoint wording to checked-in Gemini prompts without touching unrelated generated files.
Output: regenerated Gemini `apply`, `propose`, `continue`, and `ff` prompts.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
@lib/generator.js
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Gemini `apply` / `propose` / `continue` / `ff` slice from `buildPlatformBundle('gemini')`</name>
  <files>
commands/gemini/opsx/apply.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
  </files>
  <read_first>
lib/generator.js
commands/gemini/opsx/apply.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
scripts/test-workflow-runtime.js
.planning/phases/06-tdd-light-workflow/06-05-PLAN.md
  </read_first>
  <action>Refresh exactly these four checked-in Gemini files from `buildPlatformBundle('gemini')`: `commands/gemini/opsx/apply.toml`, `propose.toml`, `continue.toml`, and `ff.toml`. Do not hand-edit prompt prose. Do not touch `commands/gemini/opsx.toml` or any other Gemini prompt. The refreshed prompts must include the exact Phase 6 source-of-truth wording for `rules.tdd.mode`, `RED`, `VERIFY`, `TDD Exemption:`, and, for `apply.toml`, `completed TDD steps`, `verification command/result`, `diff summary`, and `drift`.</action>
  <acceptance_criteria>
- Only the four plan-listed Gemini files are modified.
- `commands/gemini/opsx/propose.toml`, `continue.toml`, and `ff.toml` contain `rules.tdd.mode` and `TDD Exemption:`.
- `commands/gemini/opsx/apply.toml` contains `completed TDD steps` and `diff summary`.
- The four refreshed files match `buildPlatformBundle('gemini')` byte-for-byte.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const {buildPlatformBundle}=require('./lib/generator');const bundle=buildPlatformBundle('gemini');const files=['opsx/apply.toml','opsx/propose.toml','opsx/continue.toml','opsx/ff.toml'];for(const file of files){const actual=fs.readFileSync(path.join('commands','gemini',file),'utf8');if(actual!==bundle[file]){throw new Error(file);}}"</automated>
  </verify>
  <done>
- The checked-in Gemini TDD-light prompt slice matches source-of-truth generator output exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator source-of-truth -> checked-in Gemini prompts | partial or manual refreshes can leave Gemini guidance stale even when runtime behavior is correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | Gemini `apply` / `propose` / `continue` / `ff` prompts | mitigate | Regenerate only the listed files from `buildPlatformBundle('gemini')` and verify byte-for-byte parity. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run the exact Gemini parity assertion for the four plan-listed files.
</verification>

<success_criteria>
- Gemini checked-in prompts expose shipped Phase 6 TDD-light wording with no manual drift.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-08-SUMMARY.md`
</output>
