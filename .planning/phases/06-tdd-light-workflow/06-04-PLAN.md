---
phase: 06-tdd-light-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - lib/workflow.js
  - lib/change-store.js
  - lib/change-capsule.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - TDD-04
must_haves:
  truths:
    - "`execution-checkpoint` can describe completed TDD steps and detect thin execution proof after one top-level task group."
    - "Accepted-write persistence stores richer execution proof inside existing `verificationLog`, `context.md`, and `drift.md` paths."
    - "Phase 4 one-top-level-task-group apply behavior remains intact while proof becomes richer."
  artifacts:
    - path: "lib/workflow.js"
      provides: "Execution-evidence normalization for completed TDD steps, diff summary, and drift status"
      contains: "execution-proof-missing"
    - path: "lib/change-store.js"
      provides: "Extended verification-log entry shape for Phase 6 execution proof"
      contains: "completedSteps"
    - path: "lib/change-capsule.js"
      provides: "Context capsule rendering of richer verification evidence"
      contains: "completedSteps:"
  key_links:
    - from: "lib/workflow.js"
      to: "lib/change-store.js"
      via: "normalized execution evidence fields are persisted through `recordTaskGroupExecution()`"
      pattern: "completedSteps|diffSummary|driftStatus"
    - from: "lib/change-store.js"
      to: "lib/change-capsule.js"
      via: "the last verification block in `context.md` mirrors the extended verification-log entry"
      pattern: "renderContextCapsule|summarizeLastVerification"
---

<objective>
Persist the richer execution proof required by Phase 6 without creating a new artifact or changing the apply lifecycle.

Purpose: implement D-12, D-14, and D-16 by extending the existing Phase 4 checkpoint/store/capsule path.
Output: richer execution evidence normalization, persisted verification-log fields, and updated capsule/drift rendering.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
@.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
@.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-04-SUMMARY.md

<interfaces>
From `lib/workflow.js`:
```javascript
function normalizeExecutionEvidence(options = {}) {}
function runExecutionCheckpoint(options = {}) {}
```

From `lib/change-store.js`:
```javascript
function recordTaskGroupExecution(changeDir, payload = {}) {}
```

From `lib/change-capsule.js`:
```javascript
function summarizeLastVerification(entry) {}
function renderContextCapsule(state, options = {}) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record completed TDD steps, diff summary, and drift through the existing execution proof path</name>
  <files>
lib/workflow.js
lib/change-store.js
lib/change-capsule.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Execution proof persists through the existing `verificationLog`, `context.md`, and `drift.md` paths.
    - Missing execution proof becomes a warning, not a new lifecycle branch.
    - One top-level task-group apply flow remains unchanged.
  </behavior>
  <read_first>
lib/workflow.js
lib/change-store.js
lib/change-capsule.js
scripts/test-workflow-runtime.js
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
.planning/phases/06-tdd-light-workflow/06-RESEARCH.md
.planning/phases/06-tdd-light-workflow/06-VALIDATION.md
.planning/phases/06-tdd-light-workflow/06-PATTERNS.md
.planning/phases/04-change-state-machine-and-drift-control/04-04-SUMMARY.md
  </read_first>
  <action>Implement D-12, D-14, and D-16 exactly. In `lib/workflow.js`, extend `normalizeExecutionEvidence()` so it extracts `completedSteps` from completed checklist items labelled `RED:`, `GREEN:`, `REFACTOR:`, and `VERIFY:`, and accepts explicit `executionEvidence.diffSummary`, `executionEvidence.driftStatus`, and `executionEvidence.driftSummary`. Update `runExecutionCheckpoint()` to emit a `WARN` finding with the exact code `execution-proof-missing` when a completed group lacks any of `completedSteps`, `verificationCommand`, `verificationResult`, `diffSummary`, or `driftStatus`; do not add a new checkpoint id or new apply stage. In `lib/change-store.js`, extend each persisted verification-log entry to contain the exact keys `completedSteps`, `diffSummary`, and `driftStatus` alongside the existing `at`, `taskGroup`, `verificationCommand`, `verificationResult`, `changedFiles`, and `checkpointStatus`. Keep the proof in the existing `verificationLog`; do not create `tdd-log.md` or another artifact. Pass `driftSummary` through the existing `drift.md` append path. In `lib/change-capsule.js`, update `summarizeLastVerification()` / `renderContextCapsule()` so `context.md` renders the exact labels `completedSteps:`, `diffSummary:`, and `driftStatus:` when present. In `scripts/test-workflow-runtime.js`, add the exact tests `execution checkpoint records completed tdd steps diff summary and drift status`, `recordTaskGroupExecution persists extended verification evidence`, and `context capsule renders completed tdd steps and diff summary`.</action>
  <acceptance_criteria>
- `lib/workflow.js` contains `execution-proof-missing` and normalizes `completedSteps`, `diffSummary`, and `driftStatus`.
- `lib/change-store.js` persists `completedSteps`, `diffSummary`, and `driftStatus` inside the existing `verificationLog` entry shape.
- `lib/change-capsule.js` renders the exact labels `completedSteps:`, `diffSummary:`, and `driftStatus:` in the last-verification block.
- `scripts/test-workflow-runtime.js` contains the exact three execution-proof test names listed in the action.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "execution-proof-missing|completedSteps|diffSummary|driftStatus|completedSteps:|diffSummary:|driftStatus:|execution checkpoint records completed tdd steps diff summary and drift status|recordTaskGroupExecution persists extended verification evidence|context capsule renders completed tdd steps and diff summary" lib/workflow.js lib/change-store.js lib/change-capsule.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- TDD-04 is satisfied through the existing persistence artifacts.
- Resume, review, and later verify/archive work can inspect richer proof without a new file type.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution checkpoint result -> persisted state | thin or missing proof can make resume and later verification unverifiable |
| persisted verification log -> context/drift sidecars | mismatched rendering can hide the actual executed TDD steps from later runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Repudiation | `lib/change-store.js` / `lib/change-capsule.js` | mitigate | Persist completed TDD steps, diff summary, and drift status through `verificationLog`, then render the same fields into `context.md`. |
| T-06-03 | Information Integrity | `lib/workflow.js` execution evidence | mitigate | Normalize the exact proof fields and emit `execution-proof-missing` warnings when proof is incomplete. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the workflow/store/capsule/test files for the exact Phase 6 execution-proof field names and test names.
</verification>

<success_criteria>
- TDD-04 is implemented without a new execution artifact.
- One-group apply behavior remains intact while durable proof becomes richer.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tdd-light-workflow/06-04-SUMMARY.md`
</output>
