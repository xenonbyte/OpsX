---
phase: 07-verify-sync-archive-and-batch-gates
plan: 08
type: execute
wave: 6
depends_on:
  - 07-05
  - 07-06
  - 07-07
files_modified:
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "Strict repo-wide generated-vs-checked-in parity is restored after the Phase 7 platform refreshes land."
    - "The same 15 Phase 7 route files remain explicitly asserted in both generated output and checked-in files."
    - "No temporary Phase 7 mismatch allowlist or partial-parity escape hatch remains in the runtime suite."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Strict final parity gate plus explicit checked-in assertions for the 15 Phase 7 routes"
      contains: "PHASE7_GATE_PROMPT_PATHS"
  key_links:
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/**"
      via: "the runtime suite enforces strict full-bundle parity while keeping explicit 15-route Phase 7 content assertions"
      pattern: "parity.mismatched"
---

<objective>
Close Phase 7 by removing temporary prompt-drift allowances and restoring strict full-bundle parity.

Purpose: keep the bounded 15-route Phase 7 assertions while re-locking repo-wide generator parity after all three platform refresh plans complete.
Output: one strict runtime suite with no temporary Phase 7 mismatch escape hatch.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-04-SUMMARY.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-05-SUMMARY.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-06-SUMMARY.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-07-SUMMARY.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove temporary Phase 7 prompt allowances and keep explicit 15-route assertions under strict full-bundle parity</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
commands/claude/opsx/verify.md
commands/claude/opsx/sync.md
commands/claude/opsx/archive.md
commands/claude/opsx/batch-apply.md
commands/claude/opsx/bulk-archive.md
commands/codex/prompts/opsx-verify.md
commands/codex/prompts/opsx-sync.md
commands/codex/prompts/opsx-archive.md
commands/codex/prompts/opsx-batch-apply.md
commands/codex/prompts/opsx-bulk-archive.md
commands/gemini/opsx/verify.toml
commands/gemini/opsx/sync.toml
commands/gemini/opsx/archive.toml
commands/gemini/opsx/batch-apply.toml
commands/gemini/opsx/bulk-archive.toml
.planning/phases/07-verify-sync-archive-and-batch-gates/07-04-SUMMARY.md
  </read_first>
  <action>Remove only the temporary Phase 7 mismatch-escape behavior from `scripts/test-workflow-runtime.js` after the Claude/Codex/Gemini refresh plans are complete. Keep `PHASE7_GATE_PROMPT_PATHS` as the exact 15-file assertion scope and add explicit checked-in assertions for those same files so the shipped prompts are validated directly, not only through generated-source assertions. Re-lock repo-wide parity by requiring `parity.mismatched` to be empty for all three platforms again. Keep the bounded content assertions for verify `PASS/WARN/BLOCK`, sync no-partial-write wording, archive internal safe sync plus `.opsx/archive/<change-name>`, and batch isolation with skipped/blocked reporting. Do not broaden assertion scope beyond the 15 plan-listed routes and do not reintroduce any mismatch allowlist naming or logic.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` still defines `PHASE7_GATE_PROMPT_PATHS` with 15 entries.
- Strict repo-wide `parity.mismatched === []` behavior is restored with no Phase 7 mismatch allowlist logic.
- The runtime suite asserts both generated output and checked-in wording for the same 15 Phase 7 routes.
- `npm run test:workflow-runtime` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "PHASE7_GATE_PROMPT_PATHS|parity\\.mismatched|checked-in prompt must mention|PASS|WARN|BLOCK|do not write partial sync|\\.opsx/archive/<change-name>|per-change isolation|skipped|blocked" scripts/test-workflow-runtime.js</automated>
    <automated>rg -n "allowedMismatches|unexpectedMismatches|phase7 mismatch" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Phase 7 closes with strict generated parity and no temporary prompt-drift escape hatch.
- The exact 15 targeted route files remain explicitly asserted in both generated and checked-in form.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refreshed platform bundles -> final runtime suite | leaving temporary mismatch logic in place can hide regressions after the checked-in route refreshes land |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Integrity | `scripts/test-workflow-runtime.js` final parity gate | mitigate | Remove the temporary mismatch escape hatch, keep the exact 15-route assertion scope, and require full-bundle parity to be clean again. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the runtime suite for `PHASE7_GATE_PROMPT_PATHS`, strict `parity.mismatched`, explicit checked-in assertions, and absence of temporary mismatch allowlist terms.
</verification>

<success_criteria>
- Phase 7 ends with strict repo-wide prompt parity restored.
- The final runtime suite directly validates the shipped verify/sync/archive/batch route wording across Claude, Codex, and Gemini.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verify-sync-archive-and-batch-gates/07-08-SUMMARY.md`
</output>
