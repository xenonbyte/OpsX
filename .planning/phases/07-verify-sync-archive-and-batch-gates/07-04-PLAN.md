---
phase: 07-verify-sync-archive-and-batch-gates
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - lib/workflow.js
  - lib/generator.js
  - skills/opsx/SKILL.md
  - skills/opsx/references/action-playbooks.md
  - skills/opsx/references/action-playbooks-zh.md
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "Source-of-truth route metadata describes shipped hard-gate behavior for verify, sync, archive, batch-apply, and bulk-archive."
    - "Skill and bilingual playbooks stop saying verify/archive hard gates are deferred and stop permitting incomplete archive escapes."
    - "The repo stays green while the 15 affected checked-in route files are refreshed through a temporary Phase 7 assertion scope."
  artifacts:
    - path: "lib/workflow.js"
      provides: "Updated route scope and fallback lines for real Phase 7 hard gates"
      contains: "Archive only changes that pass verify and sync preconditions"
    - path: "lib/generator.js"
      provides: "Action-specific Phase 7 planning and execution notes"
      contains: "PASS"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Temporary 15-file Phase 7 prompt assertion scope and source-output checks"
      contains: "PHASE7_GATE_PROMPT_PATHS"
    - path: "skills/opsx/SKILL.md"
      provides: "Shipped workflow guardrails with Phase 7 hard-gate wording"
      contains: "Do not archive incomplete changes"
  key_links:
    - from: "lib/workflow.js"
      to: "lib/generator.js"
      via: "generated route outputs inherit the updated Phase 7 summaries, scopes, and fallback lines"
      pattern: "PASS|WARN|BLOCK"
    - from: "lib/generator.js"
      to: "commands/**"
      via: "only the 15 verify/sync/archive/batch route files are staged for Phase 7 refresh"
      pattern: "PHASE7_GATE_PROMPT_PATHS"
    - from: "skills/opsx/SKILL.md"
      to: "skills/opsx/references/action-playbooks*.md"
      via: "shared guidance uses the same hard-gate semantics in English and Chinese"
      pattern: "deferred to Phase 7"
---

<objective>
Update the source-of-truth route metadata and skill/playbook guidance to match shipped Phase 7 hard gates, then stage a bounded prompt-refresh window.

Purpose: implement D-18 and D-19 only after the real runtime gates exist, while keeping generated refresh work strictly mechanical and limited to the 15 affected route files.
Output: updated workflow metadata, generator notes, skill/playbook guidance, and a temporary 15-file prompt assertion scope in the runtime suite.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-RESEARCH.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-VALIDATION.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-03-SUMMARY.md
@.planning/phases/06-tdd-light-workflow/06-09-SUMMARY.md

<interfaces>
From `lib/workflow.js`:
```javascript
const ACTIONS = [];
function getActionFallbackLines(platform, actionId) {}
```

From `lib/generator.js`:
```javascript
function getPlanningCheckpointNote(actionId) {}
function getExecutionCheckpointNote(actionId) {}
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update workflow metadata and generator notes for real Phase 7 route behavior</name>
  <files>
lib/workflow.js
lib/generator.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Verify routes mention `PASS`, `WARN`, and `BLOCK` plus patch targets and next action.
    - Sync routes promise conservative in-memory planning and no partial writes.
    - Archive routes remove the incomplete-change escape hatch and describe internal safe sync from `VERIFIED`.
    - Batch routes describe per-change isolation, skip/block reporting, and global-failure stop conditions.
  </behavior>
  <read_first>
lib/workflow.js
lib/generator.js
scripts/test-workflow-runtime.js
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-VALIDATION.md
  </read_first>
  <action>Implement D-18 and D-19 in source-of-truth metadata first. In `lib/workflow.js`, update the `ACTIONS` summaries and scopes for `verify`, `sync`, `archive`, `batch-apply`, and `bulk-archive` so they describe shipped behavior, not deferred behavior. Replace the archive scope line `Archive only completed or explicitly user-approved incomplete changes.` with exact hard-gate wording: archive only changes that pass verify and sync preconditions, and if a change is only `VERIFIED`, run the same safe sync check before moving it. Add explicit `ACTION_FALLBACK_LINES` entries for `verify`, `sync`, `archive`, `batch-apply`, and `bulk-archive` covering `PASS/WARN/BLOCK`, no partial sync writes, exact archive destination `.opsx/archive/<change-name>/`, per-change isolation, skip reasons, and global failure stops. In `lib/generator.js`, keep the existing template variables but make `getPlanningCheckpointNote()` / `getExecutionCheckpointNote()` action-specific for these five actions so generated outputs mention the real Phase 7 gate semantics. In `scripts/test-workflow-runtime.js`, add a temporary constant `PHASE7_GATE_PROMPT_PATHS` with exactly these 15 checked-in files: verify/sync/archive/batch-apply/bulk-archive across Claude/Codex/Gemini. Preserve strict parity for all other tracked files, and add generated-source assertions that verify routes mention `PASS`, `WARN`, and `BLOCK`, sync routes mention `do not write partial sync`, archive routes mention `.opsx/archive/<change-name>` and internal safe sync from `VERIFIED`, and batch routes mention per-change isolation plus skip/block counts. Do not hand-edit any `commands/**` file in this plan.</action>
  <acceptance_criteria>
- `lib/workflow.js` no longer contains `Archive only completed or explicitly user-approved incomplete changes.`
- `scripts/test-workflow-runtime.js` defines `PHASE7_GATE_PROMPT_PATHS` with exactly 15 entries for the targeted routes.
- `scripts/test-workflow-runtime.js` contains generated-source assertions for verify/sync/archive/batch Phase 7 wording.
- No checked-in `commands/**` file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "PHASE7_GATE_PROMPT_PATHS|PASS|WARN|BLOCK|do not write partial sync|\\.opsx/archive/<change-name>|VERIFIED|per-change isolation|skip|blocked|Archive only completed or explicitly user-approved incomplete changes\\." lib/workflow.js lib/generator.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Generated Phase 7 route copy has one bounded source-of-truth path and a 15-file assertion scope for later refresh plans.
- The repo remains green while only the targeted checked-in route files are temporarily allowed to drift.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace stale skill and bilingual playbook wording with shipped hard-gate behavior</name>
  <files>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
scripts/test-workflow-runtime.js
  </files>
  <read_first>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
scripts/test-workflow-runtime.js
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
  </read_first>
  <action>Update the shipped guidance files to match the real Phase 7 gate contract. In `skills/opsx/SKILL.md`, remove the lines that say `allowedPaths` / `forbiddenPaths` are only warnings during Phase 4, remove `Hard enforcement for verify / archive path and drift gates is deferred to Phase 7`, and replace `Do not archive incomplete changes unless the user explicitly accepts the risk.` with hard-block wording that matches D-12. In `skills/opsx/references/action-playbooks.md` and `skills/opsx/references/action-playbooks-zh.md`, replace all `deferred to Phase 7` wording, update `verify` to `PASS/WARN/BLOCK`, update `sync` to conservative in-memory planning with no partial writes, update `archive` to `VERIFIED`/`SYNCED` plus internal safe sync and exact archive target, and update `batch-apply` / `bulk-archive` to per-change isolation with clear skipped/blocked reasons. When a shared line mentions Codex `$opsx-*`, keep the Claude/Gemini `/opsx-*` equivalent on the same line. Add runtime assertions that these files no longer contain `deferred to Phase 7` or `explicitly user-approved incomplete changes`.</action>
  <acceptance_criteria>
- `skills/opsx/SKILL.md`, `action-playbooks.md`, and `action-playbooks-zh.md` contain no `deferred to Phase 7` wording.
- These files contain hard-gate wording for verify/sync/archive/batch routes and no incomplete-archive escape hatch.
- `scripts/test-workflow-runtime.js` asserts the stale phrases are absent from the skill/playbook files.
- `npm run test:workflow-runtime` passes after the wording updates.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "deferred to Phase 7|explicitly user-approved incomplete changes|Do not archive incomplete changes unless the user explicitly accepts the risk" skills/opsx/SKILL.md skills/opsx/references/action-playbooks.md skills/opsx/references/action-playbooks-zh.md scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Shipped skill and playbook guidance matches the implemented Phase 7 hard-gate semantics in both languages.
- Later generated refresh plans can be mechanical and doc-clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime gate behavior -> generated route text | stale source-of-truth wording can mislead users into bypassing hard gates even if the libraries are correct |
| shared skill/playbooks -> human execution | deferred wording or incomplete-archive escape hatches can cause operators to approve unsafe flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Integrity | `lib/workflow.js` / `lib/generator.js` / guidance docs | mitigate | Update source-of-truth first, stage the exact 15-file prompt scope, and assert stale wording is removed before any checked-in prompt refresh. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep source-of-truth and guidance files for the new Phase 7 terms and absence of stale deferred wording.
</verification>

<success_criteria>
- No source-of-truth or shipped guidance still describes verify/archive hard gates as deferred.
- The repo is ready for three mechanical platform refresh plans without broad prompt churn.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verify-sync-archive-and-batch-gates/07-04-SUMMARY.md`
</output>
