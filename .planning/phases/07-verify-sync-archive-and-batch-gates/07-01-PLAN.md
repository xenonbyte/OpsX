---
phase: 07-verify-sync-archive-and-batch-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/path-scope.js
  - lib/verify.js
  - lib/runtime-guidance.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "`opsx-verify` returns `PASS`, `WARN`, or `BLOCK` from persisted state, drift, task completion, execution proof, TDD evidence, and changed-file scope instead of relying on prompt-only judgment."
    - "Allowed/forbidden path matching works with the repo's Node `>=14.14.0` engine floor and preserves `lib/**` and `*.pem` semantics."
    - "A successful verify path can advance an implemented change to `VERIFIED` only after hashes are refreshed and blockers are cleared."
  artifacts:
    - path: "package.json"
      provides: "Pinned Node-14-compatible glob matcher dependency"
      contains: "\"picomatch\": \"4.0.4\""
    - path: "lib/path-scope.js"
      provides: "Narrow allowed/forbidden path matching helper"
      contains: "function matchPathScope"
    - path: "lib/verify.js"
      provides: "Verify gate evaluation and accepted-state helper"
      contains: "function evaluateVerifyGate"
    - path: "scripts/test-workflow-runtime.js"
      provides: "QUAL-01 regression coverage for path scope, drift, TDD evidence, and verify acceptance"
      contains: "verify gate blocks forbidden paths unresolved drift and incomplete task groups"
  key_links:
    - from: "package.json"
      to: "lib/path-scope.js"
      via: "dependency import for Node-14-compatible glob matching"
      pattern: "picomatch"
    - from: "lib/path-scope.js"
      to: "lib/verify.js"
      via: "verify gate classifies allowed, forbidden, and explainable extra files"
      pattern: "matchPathScope"
    - from: "lib/verify.js"
      to: "scripts/test-workflow-runtime.js"
      via: "runtime tests lock PASS/WARN/BLOCK findings and verify acceptance"
      pattern: "verify gate|acceptVerifyGate"
---

<objective>
Establish the Node-14-compatible path-scope foundation and ship the real Phase 7 verify gate.

Purpose: implement D-01 through D-06, D-16, and the Phase 7 matcher decision so every later archive/batch path can reuse one deterministic verify result instead of ad hoc prompt reasoning.
Output: pinned matcher dependency, a narrow `lib/path-scope.js` helper, a reusable `lib/verify.js` gate, exported runtime helpers, and green QUAL-01 regression coverage.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-RESEARCH.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-VALIDATION.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/06-tdd-light-workflow/06-CONTEXT.md

<interfaces>
From `lib/change-state.js`:
```javascript
const { MUTATION_EVENTS, applyMutationEvent, resolveContinueAction } = require('./change-state');
```

From `lib/change-store.js`:
```javascript
const { loadChangeState, writeChangeState } = require('./change-store');
```

From `lib/change-artifacts.js`:
```javascript
const { hashTrackedArtifacts, detectArtifactHashDrift } = require('./change-artifacts');
```

From `lib/workflow.js`:
```javascript
const { runTaskCheckpoint, runExecutionCheckpoint } = require('./workflow');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Node-14-compatible path-scope matcher and lock Phase 7 scope fixtures</name>
  <files>
package.json
package-lock.json
lib/path-scope.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - `allowedPaths` and `forbiddenPaths` keep glob semantics such as `lib/**` and `*.pem` under the repo's Node `>=14.14.0` engine floor.
    - Path-scope evaluation returns normalized allow/forbid/out-of-scope findings instead of throwing for ordinary mismatches.
    - QUAL-01 fixtures cover forbidden globs and explainable extra docs/config paths before verify integration lands.
  </behavior>
  <read_first>
package.json
package-lock.json
lib/runtime-guidance.js
scripts/test-workflow-runtime.js
.planning/phases/07-verify-sync-archive-and-batch-gates/07-RESEARCH.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-VALIDATION.md
  </read_first>
  <action>Resolve the research matcher question exactly as locked in the planning context. Add `picomatch@4.0.4` to `package.json` and `package-lock.json`; do not use `path.matchesGlob()` because the package engine stays `>=14.14.0`. Create `lib/path-scope.js` with a narrow exported helper such as `matchPathScope(changedFiles, { allowedPaths, forbiddenPaths })` plus any small normalization helpers it needs. Normalize paths to POSIX separators, treat empty `allowedPaths` as "no positive scope list", classify forbidden matches as blocking candidates, and classify extra docs/config paths separately so verify can downgrade them to `WARN` per D-02. In `scripts/test-workflow-runtime.js`, add exact tests named `matchPathScope uses picomatch globs for allowed and forbidden paths` and `matchPathScope distinguishes forbidden files from explainable docs or config extras`. Keep this helper Phase-7-bounded: no JSON work, no broad path canonicalization API, and no glob-output parsing beyond the allowed/forbidden scope matcher.</action>
  <acceptance_criteria>
- `package.json` and `package-lock.json` contain `picomatch` version `4.0.4`.
- `lib/path-scope.js` exports `matchPathScope` and normalizes Windows separators to `/`.
- `scripts/test-workflow-runtime.js` contains the exact two matcher test names from the action.
- `npm run test:workflow-runtime` passes with the matcher tests enabled.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "\"picomatch\": \"4\\.0\\.4\"|function matchPathScope|replace\\(/\\\\\\\\/g, '/'\\)|matchPathScope uses picomatch globs for allowed and forbidden paths|matchPathScope distinguishes forbidden files from explainable docs or config extras" package.json package-lock.json lib/path-scope.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The repo has a deterministic, Node-14-compatible path-scope matcher ready for verify and archive gates.
- Phase 8 path-hardening work remains out of scope except for this narrow helper.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the hard verify gate and accepted `VERIFIED` transition</name>
  <files>
lib/verify.js
lib/runtime-guidance.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - `opsx-verify` returns `PASS`, `WARN`, or `BLOCK` with severity-classified findings, patch targets, and next action.
    - Forbidden paths, incomplete task groups, missing execution checkpoints, missing strict-mode `RED`/`VERIFY`, unresolved drift blockers, and unresolved scope/requirement drift block by default per D-01, D-04, D-05, and D-06.
    - An accepted verify path can advance `IMPLEMENTED` to `VERIFIED` only after hashes are refreshed and blockers are cleared.
  </behavior>
  <read_first>
lib/change-state.js
lib/change-store.js
lib/change-artifacts.js
lib/runtime-guidance.js
lib/workflow.js
lib/path-scope.js
scripts/test-workflow-runtime.js
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
.planning/phases/06-tdd-light-workflow/06-CONTEXT.md
  </read_first>
  <action>Implement D-01 through D-06 and D-16 as a reusable library module, not prompt-only prose. Create `lib/verify.js` exporting `evaluateVerifyGate(options)` and `acceptVerifyGate(changeDir, gateResult)`. `evaluateVerifyGate()` must load normalized state through `loadChangeState()`, recompute hashes with `hashTrackedArtifacts()`, inspect drift with `detectArtifactHashDrift()`, parse `tasks.md` top-level groups using the same semantics as `runtime-guidance`, reuse `runTaskCheckpoint()` for strict TDD enforcement, reuse `runExecutionCheckpoint()` or equivalent execution-proof parsing for completion evidence, and inspect `drift.md` headings `User approval needed`, `Scope changes detected`, `Requirements discovered during apply`, and `Files changed outside allowed paths`. Use `matchPathScope()` for `allowedPaths` / `forbiddenPaths` and emit stable codes `artifact-hash-drift`, `forbidden-path-change`, `out-of-scope-change`, `task-group-incomplete`, `execution-checkpoint-missing`, `strict-tdd-record-missing`, `drift-approval-pending`, `scope-change-unresolved`, `discovered-requirement-unresolved`, and `manual-verification-rationale-missing`. `acceptVerifyGate()` must reject any `BLOCK` result, otherwise apply `MUTATION_EVENTS.VERIFY_ACCEPTED`, refresh hashes into the persisted state, and write the new `VERIFIED` state with updated warnings/blockers. In `lib/runtime-guidance.js`, export the existing helper(s) `parseTopLevelTaskGroups`, `loadPersistedStateView`, and `inspectReadOnlyHashDrift` for gate reuse; do not add a new CLI workflow runner. In `scripts/test-workflow-runtime.js`, add exact tests named `verify gate blocks forbidden paths unresolved drift and incomplete task groups`, `verify gate warns for manual verification rationale and docs-only extras`, and `acceptVerifyGate advances implemented changes to VERIFIED with refreshed hashes`.</action>
  <acceptance_criteria>
- `lib/verify.js` exports `evaluateVerifyGate` and `acceptVerifyGate`.
- `lib/verify.js` contains the exact finding codes `forbidden-path-change`, `task-group-incomplete`, `execution-checkpoint-missing`, `strict-tdd-record-missing`, `drift-approval-pending`, `scope-change-unresolved`, and `discovered-requirement-unresolved`.
- `lib/runtime-guidance.js` exports `parseTopLevelTaskGroups`, `loadPersistedStateView`, and `inspectReadOnlyHashDrift`.
- `scripts/test-workflow-runtime.js` contains the exact three verify test names from the action and they pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "evaluateVerifyGate|acceptVerifyGate|forbidden-path-change|task-group-incomplete|execution-checkpoint-missing|strict-tdd-record-missing|drift-approval-pending|scope-change-unresolved|discovered-requirement-unresolved|verify gate blocks forbidden paths unresolved drift and incomplete task groups|verify gate warns for manual verification rationale and docs-only extras|acceptVerifyGate advances implemented changes to VERIFIED with refreshed hashes|parseTopLevelTaskGroups|loadPersistedStateView|inspectReadOnlyHashDrift" lib/verify.js lib/runtime-guidance.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- QUAL-01 is enforced by reusable code and not by generated prose alone.
- Later archive and batch flows can reuse one deterministic verify report and accepted-state helper.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| change artifacts and drift ledger -> `lib/verify.js` | stale or out-of-scope files can be misclassified if verify trusts stored state without rehashing and scope matching |
| persisted state -> accepted verify transition | a verify acceptance write can silently bless incomplete work if blockers are not re-evaluated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `lib/verify.js` | mitigate | Recompute tracked hashes, reload normalized state, and block unresolved artifact drift before any accepted `VERIFIED` write. |
| T-07-02 | Tampering | `lib/path-scope.js` / `lib/verify.js` | mitigate | Use `picomatch@4.0.4` under Node `>=14.14.0` and classify forbidden or clearly out-of-scope files as blocking findings. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep dependency, matcher, verify exports, finding codes, and the exact QUAL-01 test names across the modified files.
</verification>

<success_criteria>
- `opsx-verify` is backed by real library code that distinguishes `PASS`, `WARN`, and `BLOCK`.
- Verify acceptance can move a change to `VERIFIED` only after hard gates, drift, and strict TDD/proof requirements are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verify-sync-archive-and-batch-gates/07-01-SUMMARY.md`
</output>
