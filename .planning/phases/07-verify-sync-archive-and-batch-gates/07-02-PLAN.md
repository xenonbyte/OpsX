---
phase: 07-verify-sync-archive-and-batch-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/sync.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - "`opsx-sync` computes a full in-memory sync plan before writing anything into `.opsx/specs/**`."
    - "Duplicate requirement ids, conflicting normative language, omitted canonical requirements, and unsafe partial writes block sync deterministically."
    - "A successful sync path advances `VERIFIED` to `SYNCED` only after atomic writes complete."
  artifacts:
    - path: "lib/sync.js"
      provides: "Conservative delta-spec sync planner and apply helper"
      contains: "function planSync"
    - path: "scripts/test-workflow-runtime.js"
      provides: "QUAL-02 regression coverage for conflict blocking and conflict-free sync acceptance"
      contains: "sync plan blocks duplicate requirement ids and writes nothing"
  key_links:
    - from: "lib/sync.js"
      to: "lib/spec-validator.js"
      via: "delta and canonical specs are parsed through the existing deterministic spec parser"
      pattern: "parseSpecFile|reviewSpecSplitEvidence"
    - from: "lib/sync.js"
      to: "scripts/test-workflow-runtime.js"
      via: "runtime tests lock no-partial-write behavior and accepted `SYNCED` transitions"
      pattern: "sync plan|applySyncPlan"
---

<objective>
Build the conservative in-memory spec-sync planner and accepted `SYNCED` transition.

Purpose: implement D-07 through D-09 and the exact QUAL-02 merge contract without hiding conflicts behind section-level auto-merge.
Output: a reusable `lib/sync.js` planner/apply helper pair plus green sync conflict and no-partial-write runtime coverage.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-RESEARCH.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-VALIDATION.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-02-SUMMARY.md
@.planning/phases/07-verify-sync-archive-and-batch-gates/07-01-SUMMARY.md

<interfaces>
From `lib/spec-validator.js`:
```javascript
const { parseSpecFile, collectSpecSplitEvidence, reviewSpecSplitEvidence } = require('./spec-validator');
```

From `lib/change-state.js`:
```javascript
const { MUTATION_EVENTS, applyMutationEvent } = require('./change-state');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conservative spec sync planning, atomic apply, and `SYNCED` acceptance</name>
  <files>
lib/sync.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - Sync blocks omissions, duplicate requirement ids, conflicting requirements, and any merge that would require a hidden partial write.
    - Sync plans all canonical file writes in memory first and only writes after a conflict-free full plan exists.
    - A conflict-free sync accepts a `VERIFIED` change into `SYNCED` and leaves canonical specs untouched on failure.
  </behavior>
  <read_first>
lib/spec-validator.js
lib/change-state.js
lib/change-store.js
lib/fs-utils.js
scripts/test-workflow-runtime.js
skills/opsx/references/artifact-templates.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-CONTEXT.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-RESEARCH.md
.planning/phases/07-verify-sync-archive-and-batch-gates/07-PATTERNS.md
  </read_first>
  <action>Implement D-07 through D-09 exactly in `lib/sync.js`. Export `planSync(options)`, `applySyncPlan(plan)`, and `acceptSyncPlan(changeDir, plan)` or equivalently named narrow helpers. `planSync()` must enumerate `.opsx/changes/<change>/specs/**/spec.md`, load the matching canonical `.opsx/specs/**/spec.md` file when present, parse both sides with `parseSpecFile()`, and build a single in-memory write plan for the full change. Reuse `reviewSpecSplitEvidence()` findings where it helps, but add explicit Phase 7 blocking checks for omitted canonical requirements that were neither preserved nor explicitly removed, duplicate requirement ids, conflicting normative language, and likely behavior conflicts. `applySyncPlan()` must use `writeTextAtomic()` only after the whole plan is conflict-free and must not leave partial writes behind on any blocking finding. `acceptSyncPlan()` must apply `MUTATION_EVENTS.SYNC_ACCEPTED`, persist the `SYNCED` state through `writeChangeState()`, and refresh hashes only on accepted writes. In `scripts/test-workflow-runtime.js`, add exact tests named `sync plan blocks duplicate requirement ids and writes nothing`, `sync plan blocks omitted canonical requirements and conflicting normative language`, and `applySyncPlan writes full conflict-free capability files and advances VERIFIED to SYNCED`. Do not introduce a second Markdown parser, JSON sync output, or broad path-hardening work.</action>
  <acceptance_criteria>
- `lib/sync.js` exports `planSync` and `applySyncPlan`.
- `lib/sync.js` contains logic for omitted canonical requirements, duplicate ids, conflicting requirements, and atomic writes.
- `scripts/test-workflow-runtime.js` contains the exact three sync test names from the action.
- `npm run test:workflow-runtime` passes with the new sync tests enabled.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "function planSync|function applySyncPlan|duplicate-requirement-id|conflicting-requirements|omitted canonical requirements|writeTextAtomic|sync plan blocks duplicate requirement ids and writes nothing|sync plan blocks omitted canonical requirements and conflicting normative language|applySyncPlan writes full conflict-free capability files and advances VERIFIED to SYNCED" lib/sync.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- QUAL-02 is satisfied by one deterministic sync planner and accepted `SYNCED` path.
- Archive can later reuse the same safe sync checks instead of inventing a second merge path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| change-local delta specs -> canonical `.opsx/specs/**` | malformed or conflicting delta specs can overwrite canonical requirements if the merge plan is not validated in memory first |
| accepted sync writes -> persisted lifecycle state | the state can claim `SYNCED` even when canonical files were only partially updated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | `lib/sync.js` | mitigate | Parse both delta and canonical specs, block duplicate/conflicting/omitted requirements, and write only after a conflict-free full plan exists. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep for the sync exports, atomic-write path, and the exact QUAL-02 runtime test names.
</verification>

<success_criteria>
- `.opsx/specs/**` sync is conservative, deterministic, and leaves no partial state on conflict.
- A successful sync can promote a verified change to `SYNCED` without hiding requirement conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-verify-sync-archive-and-batch-gates/07-02-SUMMARY.md`
</output>
