---
phase: 03-skill-and-command-surface-rewrite
plan: 11
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-09
  - 03-10
files_modified:
  - lib/install.js
  - lib/cli.js
  - scripts/postinstall.js
  - templates/project/rule-file.md.tmpl
  - AGENTS.md
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "CLI help, postinstall output, install-adjacent guidance, project hand-off template, and repo-local `AGENTS.md` all match the explicit-only route contract."
    - "Repo-local `AGENTS.md` keeps `openspec/config.yaml` and `openspec/changes/` authoring guidance, but its stale route bullet is replaced with current OpsX route guidance."
    - "The final Phase 3 verification bundle is green: runtime suite plus the narrowed public-surface gate."
  artifacts:
    - path: "lib/cli.js"
      provides: "CLI help text aligned to explicit `$opsx-*` examples"
      contains: "Codex usage:"
    - path: "templates/project/rule-file.md.tmpl"
      provides: "Project hand-off template with explicit route guidance"
      contains: "$opsx-"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Final repo-wide bundle parity and public-surface contract assertions"
      contains: "checkedInEntries"
    - path: "AGENTS.md"
      provides: "Repo-local hand-off guidance with corrected route bullet and preserved `openspec/` authoring paths"
      contains: "For Codex"
  key_links:
    - from: "lib/cli.js"
      to: "scripts/postinstall.js"
      via: "same public Codex route examples"
      pattern: "\\$opsx-"
    - from: "templates/project/rule-file.md.tmpl"
      to: "AGENTS.md"
      via: "matching hand-off guidance for Codex and Claude/Gemini"
      pattern: "\\$opsx-|/opsx-"
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/**"
      via: "final checked-in parity assertions after all bounded refresh plans land"
      pattern: "buildPlatformBundle|checkedInEntries"
---

<objective>
Finish the remaining help, postinstall, template, and repo hand-off rewrites, then activate the final Phase 3 verification bundle.

Purpose: remove the last stale route bullets that are not regenerated by the command bundles themselves, preserve legitimate repo-local authoring guidance, and close the phase with full runtime plus public-surface verification.
Output: aligned `lib/install.js`/`lib/cli.js`/`scripts/postinstall.js`/`templates/project/rule-file.md.tmpl`/`AGENTS.md` surfaces plus final runtime assertions that compare checked-in bundles and public guidance against the Phase 3 contract.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-09-PLAN.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-10-PLAN.md

<interfaces>
From `lib/cli.js`:
```javascript
function showHelp() {
  return [
    'Codex usage:',
    '  - Prefer `$opsx <request>` for natural-language workflow requests'
  ].join('\n');
}
```

From `scripts/postinstall.js`:
```javascript
console.log(`
2. For Codex, prefer:
   $opsx <request>
`);
```

From `templates/project/rule-file.md.tmpl`:
```markdown
- For Codex, prefer `$opsx <request>`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align help and hand-off surfaces, then activate the final parity/public-surface runtime gate</name>
  <files>
lib/install.js
lib/cli.js
scripts/postinstall.js
templates/project/rule-file.md.tmpl
AGENTS.md
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - CLI help, postinstall, template, and repo hand-off guidance all use explicit `$opsx-*` for Codex and `/opsx-*` for Claude/Gemini.
    - `AGENTS.md` keeps `openspec/config.yaml` and `openspec/changes/` repo-authoring instructions but replaces only the stale route guidance.
    - The runtime suite now enforces repo-wide checked-in `commands/**` parity with `buildPlatformBundle()` and verifies the narrowed public surfaces do not advertise banned routes.
  </behavior>
  <read_first>
lib/install.js
lib/cli.js
scripts/postinstall.js
templates/project/rule-file.md.tmpl
AGENTS.md
scripts/test-workflow-runtime.js
README.md
docs/commands.md
docs/codex.md
skills/opsx/SKILL.md
.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
  </read_first>
  <action>Per D-01 through D-04, D-13 through D-16, and the resolved research decision for `AGENTS.md`, finish the non-generated surface rewrite and turn on the final gate. In `lib/cli.js`, replace the current help line `Prefer \`$opsx <request>\`` with a short explicit-route example list such as `$opsx-onboard`, `$opsx-propose`, `$opsx-status`, and `$opsx-apply`; keep help truthful and do not solve CMD-05 by mutating CLI status behavior. In `scripts/postinstall.js`, rewrite the Codex next-step copy so it shows explicit route examples instead of standalone `$opsx` or `$opsx <request>`. In `templates/project/rule-file.md.tmpl`, replace `For Codex, prefer $opsx <request>` with exact wording that Codex uses explicit `$opsx-*` routes and Claude/Gemini use `/opsx-*`. In `AGENTS.md`, preserve the repo-local `openspec/config.yaml` and `openspec/changes/` authoring-path bullets, but replace the stale route bullet with exact current guidance: `For Codex, use explicit $opsx-* routes; for Claude/Gemini, use /opsx-* routes.` Update `lib/install.js` only as needed so install/check guidance does not reintroduce `$opsx <request>` or `/prompts:*` as public UX while keeping the internal Codex prompt-file install layout intact. Finally, upgrade `scripts/test-workflow-runtime.js` from Wave 1 source-output assertions to the full Phase 3 gate: compare every checked-in `commands/**` file against `buildPlatformBundle()`, verify installed assets reflect the explicit-only route model, and assert public help/template/hand-off surfaces no longer advertise banned route strings.</action>
  <acceptance_criteria>
- `lib/cli.js`, `scripts/postinstall.js`, and `templates/project/rule-file.md.tmpl` contain explicit `$opsx-*` examples and no standalone `$opsx` or `$opsx <request>` guidance.
- `AGENTS.md` still references `openspec/config.yaml` and `openspec/changes/`, but its route bullet no longer contains `$openspec`, `/openspec`, `/opsx:*`, or `/prompts:*`.
- `scripts/test-workflow-runtime.js` performs repo-wide checked-in `commands/**` parity checks and public-surface assertions after all refresh plans land.
- `npm run test:workflow-runtime`, `node scripts/check-phase1-legacy-allowlist.js`, and `node bin/opsx.js --help` all pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node scripts/check-phase1-legacy-allowlist.js</automated>
    <automated>node bin/opsx.js --help</automated>
  </verify>
  <done>
- Every remaining non-generated route/help surface matches the explicit-only contract.
- `AGENTS.md` is corrected only where Phase 3 superseded it.
- The full Phase 3 verification bundle is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/postinstall/template guidance -> user behavior | Stale route copy can send users to removed entrypoints even when generated bundles are correct |
| repo-local AGENTS guidance -> agent behavior in this repository | Incorrect route bullets can override correct public workflow guidance during execution |
| runtime suite -> shipped surface | Without final checked-in parity activation, generated bundles and public guidance can drift silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing / Tampering | `lib/cli.js`, `scripts/postinstall.js`, `templates/project/rule-file.md.tmpl`, `AGENTS.md` | mitigate | Rewrite every stale route bullet/example to explicit `$opsx-*` and `/opsx-*` guidance, then run the full public-surface gate. |
| T-03-04 | Denial of Service / Tampering | `lib/install.js`, `scripts/test-workflow-runtime.js` | mitigate | Preserve internal install layout while enabling final checked-in parity assertions and narrowed public-surface checks. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Run `node scripts/check-phase1-legacy-allowlist.js`.
- Run `node bin/opsx.js --help`.
</verification>

<success_criteria>
- The remaining non-generated route/help surfaces match the explicit-only contract.
- The final runtime and public-surface gates are active and green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-11-SUMMARY.md`
</output>
