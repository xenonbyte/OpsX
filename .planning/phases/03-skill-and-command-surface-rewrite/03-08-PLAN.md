---
phase: 03-skill-and-command-surface-rewrite
plan: 08
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - commands/gemini/opsx/new.toml
  - commands/gemini/opsx/onboard.toml
  - commands/gemini/opsx/propose.toml
  - commands/gemini/opsx/resume.toml
  - commands/gemini/opsx/status.toml
  - commands/gemini/opsx/sync.toml
  - commands/gemini/opsx/verify.toml
autonomous: true
requirements:
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "The second bounded Gemini refresh slice brings the remaining Gemini action leaves, including `onboard`, `resume`, and `status`, into parity with the shared fallback contract."
    - "Gemini empty-state routes describe missing workspace or no active change as guidance, not implicit state creation."
  artifacts:
    - path: "commands/gemini/opsx/onboard.toml"
      provides: "Gemini onboarding fallback guidance from the shared templates"
      contains: "/opsx-onboard"
    - path: "commands/gemini/opsx/status.toml"
      provides: "Gemini status fallback guidance for missing workspace or active change"
      contains: "/opsx-status"
  key_links:
    - from: "lib/generator.js"
      to: "commands/gemini/opsx/status.toml"
      via: "buildPlatformBundle('gemini')"
      pattern: "buildPlatformBundle"
    - from: "templates/commands/action.md.tmpl"
      to: "commands/gemini/opsx/resume.toml"
      via: "shared fallback wording"
      pattern: "resume"
---

<objective>
Finish the Gemini generated refresh in a second bounded slice that includes the empty-state routes.

Purpose: isolate the Gemini files that carry `onboard`/`status`/`resume` fallback text so they stay under the warning threshold while still verifying CMD-05 on the checked-in bundle.
Output: the remaining seven Gemini action files regenerated from `buildPlatformBundle('gemini')`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {
  if (platform === 'gemini') {
    /* returns opsx/onboard.toml, opsx/resume.toml, opsx/status.toml and other action leaves */
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the remaining Gemini action leaves, including the empty-state routes</name>
  <files>
commands/gemini/opsx/new.toml
commands/gemini/opsx/onboard.toml
commands/gemini/opsx/propose.toml
commands/gemini/opsx/resume.toml
commands/gemini/opsx/status.toml
commands/gemini/opsx/sync.toml
commands/gemini/opsx/verify.toml
  </files>
  <read_first>
commands/gemini/opsx/onboard.toml
commands/gemini/opsx/resume.toml
commands/gemini/opsx/status.toml
lib/generator.js
templates/commands/action.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md
  </read_first>
  <action>Per D-09 through D-12 and D-17 through D-22, refresh only the remaining Gemini action files listed here from `buildPlatformBundle('gemini')`. Keep the work mechanical and bounded: no source-template edits, no help/docs/skills edits, and no Claude/Codex outputs. The important outcome is that the checked-in Gemini `onboard`, `resume`, and `status` leaves now carry the exact non-mutating empty-state guidance defined in 03-02.</action>
  <acceptance_criteria>
- All seven listed Gemini files exactly match the current `buildPlatformBundle('gemini')` output.
- `commands/gemini/opsx/onboard.toml`, `resume.toml`, and `status.toml` carry the Phase 3 missing-workspace or no-active-change fallback behavior.
- None of the refreshed files auto-create state or reintroduce banned public entrypoint wording.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const { buildPlatformBundle } = require('./lib/generator');const bundle=buildPlatformBundle('gemini');['opsx/new.toml','opsx/onboard.toml','opsx/propose.toml','opsx/resume.toml','opsx/status.toml','opsx/sync.toml','opsx/verify.toml'].forEach((rel)=>{const actual=fs.readFileSync(path.join('commands','gemini',rel),'utf8');if(actual!==bundle[rel]){throw new Error(rel);}});"</automated>
  </verify>
  <done>
- The Gemini bundle is fully refreshed across two bounded plans, with CMD-05 guidance now present in checked-in Gemini outputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Gemini empty-state prompts | A partial refresh can leave `onboard`/`resume`/`status` inconsistent with the shared fallback contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | `commands/gemini/opsx/onboard.toml`, `resume.toml`, `status.toml` | mitigate | Regenerate the remaining Gemini leaves from `buildPlatformBundle('gemini')` and verify exact parity for the listed files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Compare each listed Gemini file against `buildPlatformBundle('gemini')`.
</verification>

<success_criteria>
- The second bounded Gemini refresh slice is generated, checked in, and parity-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-08-SUMMARY.md`
</output>
