---
phase: 03-skill-and-command-surface-rewrite
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - commands/claude/opsx.md
  - commands/claude/opsx/apply.md
  - commands/claude/opsx/archive.md
  - commands/claude/opsx/batch-apply.md
  - commands/claude/opsx/bulk-archive.md
  - commands/claude/opsx/continue.md
  - commands/claude/opsx/explore.md
  - commands/claude/opsx/ff.md
autonomous: true
requirements:
  - CMD-01
  - CMD-04
must_haves:
  truths:
    - "The Claude index and first bounded refresh set of Claude action leaves are regenerated directly from the updated Phase 3 source of truth."
    - "These refreshed Claude files expose `/opsx-*` routes only and do not reintroduce legacy or Codex-only route wording."
  artifacts:
    - path: "commands/claude/opsx.md"
      provides: "Refreshed Claude index from `buildPlatformBundle('claude')`"
      contains: "/opsx-<action>"
    - path: "commands/claude/opsx/explore.md"
      provides: "Representative first-half Claude action prompt generated from shared templates"
      contains: "/opsx-explore"
  key_links:
    - from: "lib/generator.js"
      to: "commands/claude/opsx.md"
      via: "buildPlatformBundle('claude')"
      pattern: "buildPlatformBundle"
    - from: "templates/commands/action.md.tmpl"
      to: "commands/claude/opsx/*.md"
      via: "shared prompt text expansion"
      pattern: "\\.opsx/config\\.yaml"
---

<objective>
Refresh the first bounded slice of checked-in Claude command files from the new generator contract.

Purpose: keep generated refresh work under the file-budget warning threshold while starting the checked-in bundle migration in parallelizable slices.
Output: the Claude index plus the first seven Claude action files regenerated from `buildPlatformBundle('claude')`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {
  if (platform === 'claude') {
    files['opsx.md'] = buildIndexMarkdown('claude', 'OpsX Workflow');
    actions.forEach((action) => {
      files[`opsx/${action.id}.md`] = buildActionMarkdown('claude', action);
    });
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Claude index and first-half action leaves from `buildPlatformBundle('claude')`</name>
  <files>
commands/claude/opsx.md
commands/claude/opsx/apply.md
commands/claude/opsx/archive.md
commands/claude/opsx/batch-apply.md
commands/claude/opsx/bulk-archive.md
commands/claude/opsx/continue.md
commands/claude/opsx/explore.md
commands/claude/opsx/ff.md
  </files>
  <read_first>
commands/claude/opsx.md
commands/claude/opsx/apply.md
commands/claude/opsx/explore.md
lib/generator.js
templates/commands/action.md.tmpl
templates/commands/index.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md
  </read_first>
  <action>Per D-09, D-10, D-11, and D-12, refresh only the bounded Claude slice listed in this plan from `buildPlatformBundle('claude')`. Do not hand-edit prompt prose, and do not modify Codex or Gemini outputs here. The goal is a mechanical checked-in refresh that brings the Claude index and the first seven Claude action leaves in line with the explicit route and strict preflight contract already locked in 03-02.</action>
  <acceptance_criteria>
- All eight listed Claude files exactly match the current `buildPlatformBundle('claude')` output.
- None of the refreshed files contain `$opsx <request>`, standalone `$opsx`, `/openspec`, `$openspec`, `/prompts:openspec`, `/opsx:*`, or `/prompts:opsx-*`.
- The refreshed Claude index still lists the expected `/opsx-*` route inventory.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const { buildPlatformBundle } = require('./lib/generator');const bundle=buildPlatformBundle('claude');['opsx.md','opsx/apply.md','opsx/archive.md','opsx/batch-apply.md','opsx/bulk-archive.md','opsx/continue.md','opsx/explore.md','opsx/ff.md'].forEach((rel)=>{const actual=fs.readFileSync(path.join('commands','claude',rel),'utf8');if(actual!==bundle[rel]){throw new Error(rel);}});"</automated>
  </verify>
  <done>
- The first bounded Claude refresh slice is complete and matches the Phase 3 source of truth.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Claude files | Hand edits or partial refreshes can drift Claude outputs away from the new contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `commands/claude/opsx*.md` | mitigate | Regenerate only from `buildPlatformBundle('claude')` and verify byte-for-byte parity for the listed files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Compare each listed Claude file against `buildPlatformBundle('claude')`.
</verification>

<success_criteria>
- The first bounded Claude refresh slice is generated, checked in, and parity-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-03-SUMMARY.md`
</output>
