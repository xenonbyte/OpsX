---
phase: 03-skill-and-command-surface-rewrite
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - lib/workflow.js
  - lib/generator.js
  - templates/commands/action.md.tmpl
  - templates/commands/index.md.tmpl
  - templates/commands/codex-entry.md.tmpl
  - templates/commands/shared-entry.md.tmpl
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "Route syntax, strict preflight wording, and empty-state fallback behavior come from shared workflow metadata/templates instead of generated leaves."
    - "Codex source output treats `commands/codex/prompts/opsx.md` as an internal generated route catalog only and never advertises standalone `$opsx` or `$opsx <request>` as public UX."
    - "Wave 1 tests prove the new source-of-truth bundle contract and keep the suite green before checked-in bundle refresh plans land."
  artifacts:
    - path: "lib/workflow.js"
      provides: "Central route syntax and per-action fallback metadata"
      contains: "getPrimaryWorkflowSyntax"
    - path: "lib/generator.js"
      provides: "Shared render variables for explicit-only Codex guidance and strict preflight copy"
      contains: "buildPlatformBundle"
    - path: "templates/commands/action.md.tmpl"
      provides: "Strict Phase 3 preflight wording with honest missing-file fallback boundaries"
      contains: ".opsx/config.yaml"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Wave 1 source-output assertions for explicit-only routes and empty-state fallback text"
      contains: "buildPlatformBundle('codex')"
  key_links:
    - from: "lib/workflow.js"
      to: "lib/generator.js"
      via: "route syntax and fallback metadata"
      pattern: "getActionSyntax|getPrimaryWorkflowSyntax"
    - from: "lib/generator.js"
      to: "templates/commands/*.tmpl"
      via: "renderTemplate inputs for route/preflight/fallback copy"
      pattern: "renderTemplate"
    - from: "scripts/test-workflow-runtime.js"
      to: "lib/generator.js"
      via: "source-output assertions that must stay green before checked-in parity activates"
      pattern: "buildPlatformBundle"
---

<objective>
Rewrite the Phase 3 source of truth before any checked-in generated bundles are refreshed.

Purpose: separate workflow metadata/template changes from generated-asset refresh so the plan stays inside the file budget while still locking the Phase 3 route and preflight contract early.
Output: updated workflow/generator/template sources plus Wave 1 runtime assertions that validate the new bundle contract without requiring checked-in command parity yet.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-VALIDATION.md

<interfaces>
From `lib/workflow.js`:
```javascript
function getActionSyntax(platform, actionId) {
  if (platform === 'claude') return `/opsx-${actionId}`;
  if (platform === 'gemini') return `/opsx-${actionId}`;
  if (platform === 'codex') return `$opsx-${actionId}`;
}

function getPrimaryWorkflowSyntax(platform) {
  if (platform === 'codex') return '$opsx <request>';
  if (platform === 'claude' || platform === 'gemini') return '/opsx-<action>';
}
```

From `lib/generator.js`:
```javascript
function buildActionMarkdown(platform, action) {
  return renderTemplate(template, {
    primary_workflow_syntax: getPrimaryWorkflowSyntax(platform),
    action_syntax: getActionSyntax(platform, action.id)
  });
}

function buildPlatformBundle(platform) { /* returns commands/** file map */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move explicit-only routing and strict preflight into shared workflow metadata and templates</name>
  <files>
lib/workflow.js
lib/generator.js
templates/commands/action.md.tmpl
templates/commands/index.md.tmpl
templates/commands/codex-entry.md.tmpl
templates/commands/shared-entry.md.tmpl
  </files>
  <read_first>
lib/workflow.js
lib/generator.js
templates/commands/action.md.tmpl
templates/commands/index.md.tmpl
templates/commands/codex-entry.md.tmpl
templates/commands/shared-entry.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
  </read_first>
  <behavior>
    - Codex source output advertises only explicit `$opsx-*` routes; it never emits standalone `$opsx` or `$opsx <request>` as the public primary entry model.
    - Every generated action prompt requires reading `.opsx/config.yaml`, `.opsx/active.yaml`, active `state.yaml`, `context.md`, and current artifacts when present before acting.
    - `onboard`, `status`, and `resume` define concrete non-mutating fallback guidance, while mutation-heavy routes stop or redirect when required artifacts are absent.
    - No template claims Phase 4 hash comparison, stage-transition enforcement, allowed/forbidden path enforcement, or durable apply-state mutation already exists.
  </behavior>
  <action>Per D-01 through D-12 and D-17 through D-22, rewrite the generator source of truth before touching checked-in bundles. In `lib/workflow.js`, keep `getActionSyntax('codex', id)` as `$opsx-${id}` but replace `getPrimaryWorkflowSyntax('codex')` with explicit-only route guidance and add action-level fallback metadata for `onboard`, `status`, `resume`, plus a blocking or redirect fallback for routes that require an initialized workspace or active change. In `lib/generator.js` and the shared templates, replace the current “planned for later phases” copy with exact Phase 3 preflight bullets that read `.opsx/config.yaml` and `.opsx/active.yaml` if present, then read the active change `state.yaml`, `context.md`, and current artifacts before acting when an active change exists. Keep `commands/codex/prompts/opsx.md` in the generated contract only as an internal route catalog: it may list the 14 `$opsx-*` routes, but it must not contain `Preferred:`, `Primary workflow entry:`, standalone `$opsx`, `$opsx <request>`, or `/prompts:*` public-route wording. Do not refresh any checked-in `commands/**` file in this plan.</action>
  <acceptance_criteria>
- `lib/workflow.js` no longer returns `$opsx <request>` from `getPrimaryWorkflowSyntax('codex')`.
- `templates/commands/action.md.tmpl` names `.opsx/config.yaml`, `.opsx/active.yaml`, active `state.yaml`, `context.md`, and current artifacts in the Phase 3 preflight block.
- Shared templates describe `onboard`, `status`, and `resume` fallback behavior without claiming Phase 4 enforcement.
- No shared template contains `$opsx <request>`, standalone `$opsx`, `/openspec`, `$openspec`, `/prompts:openspec`, `/opsx:*`, or `/prompts:opsx-*` as current public guidance.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "\\$opsx <request>|/openspec|\\$openspec|/prompts:openspec|/opsx:\\*|/prompts:opsx-\\*|artifact hash comparison|stage-transition enforcement|allowed/forbidden path enforcement|durable apply-state mutation|\\.opsx/config\\.yaml|\\.opsx/active\\.yaml|state\\.yaml|context\\.md" lib/workflow.js lib/generator.js templates/commands</automated>
  </verify>
  <done>
- Shared metadata and templates define the Phase 3 route/preflight contract.
- The source-of-truth layer is ready for bounded generated-bundle refresh plans.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Wave 1 runtime assertions to validate source output without requiring checked-in bundle parity yet</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
lib/workflow.js
lib/generator.js
templates/commands/action.md.tmpl
templates/commands/index.md.tmpl
templates/commands/codex-entry.md.tmpl
templates/commands/shared-entry.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-VALIDATION.md
  </read_first>
  <action>Per D-09, D-10, D-11, D-12, and the resolved research decision to keep checked-in parity activation for the final verification wave, rewrite `scripts/test-workflow-runtime.js` so Wave 1 locks the source-output contract only. Assert that `buildPlatformBundle('claude')`, `buildPlatformBundle('codex')`, and `buildPlatformBundle('gemini')` emit the new explicit-only routing, strict preflight copy, and honest `onboard`/`status`/`resume` fallback text. Do not make the suite compare every checked-in `commands/**` file yet; the later generated-refresh plans must still be able to land in bounded parallel waves without turning Wave 1 red. Add helper inventories and exact string checks now so 03-03 through 03-08 only need to refresh checked-in bundle files to match the already-verified source output.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` asserts the generated Codex source output contains explicit `$opsx-*` guidance and no standalone `$opsx` or `$opsx <request>`.
- `scripts/test-workflow-runtime.js` asserts generated `onboard`, `status`, and `resume` prompts contain the exact Phase 3 fallback semantics.
- Wave 1 does not yet require repo-wide checked-in `commands/**` parity; that activation is reserved for the final verification plan.
- `npm run test:workflow-runtime` passes after the source-of-truth changes without refreshing checked-in bundles.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "buildPlatformBundle\\('claude'\\)|buildPlatformBundle\\('codex'\\)|buildPlatformBundle\\('gemini'\\)|onboard|resume|status|\\$opsx-" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Wave 1 has passing automated coverage for the new source-output contract.
- Later bundle-refresh plans can stay small and focus on checked-in generated assets only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow metadata/templates -> generated bundles | Bad shared route or preflight copy fans out to every platform bundle |
| source-output assertions -> later bundle refresh plans | If Wave 1 overreaches into checked-in parity too early, every bounded refresh plan starts from a failing baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing / Tampering | `lib/workflow.js`, `lib/generator.js`, `templates/commands/*` | mitigate | Centralize explicit-only Codex route text and internal-index-only rules in shared metadata/templates before refreshing generated leaves. |
| T-03-02 | Repudiation / Tampering | `templates/commands/action.md.tmpl`, `buildActionMarkdown()` | mitigate | Replace “planned for later phases” wording with exact read-if-present preflight plus honest missing-file fallback boundaries. |
| T-03-03 | Tampering | `scripts/test-workflow-runtime.js` | mitigate | Lock the new source-output contract in Wave 1 while deferring checked-in bundle parity activation to the final verification plan. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep workflow/generator/template/test sources for banned route strings and required preflight file references.
</verification>

<success_criteria>
- Shared workflow metadata/templates emit the Phase 3 contract from one source of truth.
- Wave 1 tests validate source output and stay green before checked-in bundle refresh plans begin.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-02-SUMMARY.md`
</output>
