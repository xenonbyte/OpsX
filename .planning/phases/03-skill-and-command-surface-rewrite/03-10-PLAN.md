---
phase: 03-skill-and-command-surface-rewrite
plan: 10
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-04
  - 03-05
  - 03-06
  - 03-07
  - 03-08
files_modified:
  - README.md
  - README-zh.md
  - docs/commands.md
  - docs/codex.md
  - docs/supported-tools.md
  - docs/runtime-guidance.md
autonomous: true
requirements:
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "README and docs present the hard clean break and explicit `$opsx-*` route model as the current public UX."
    - "Public docs no longer present legacy OpenSpec routes or paths, `/prompts:*`, `/opsx:*`, standalone `$opsx`, or `$opsx <request>` as current usage."
  artifacts:
    - path: "docs/codex.md"
      provides: "Explicit-only Codex public usage guide"
      contains: "$opsx-propose"
    - path: "docs/runtime-guidance.md"
      provides: "Current runtime input guidance centered on `.opsx` artifacts only"
      contains: ".opsx/config.yaml"
  key_links:
    - from: "README.md"
      to: "docs/commands.md"
      via: "same explicit route inventory"
      pattern: "\\$opsx-"
    - from: "docs/codex.md"
      to: "commands/codex/prompts/opsx.md"
      via: "matching explicit-only Codex route model"
      pattern: "\\$opsx-"
---

<objective>
Rewrite README and docs to the hard clean break and current runtime guidance.

Purpose: split README/docs work away from skill/playbook edits so the plan stays inside the preferred context budget while aligning every public written surface to the final generated contract.
Output: updated `README*.md` and `docs/*.md` files that advertise only the current OpsX route and path model.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-05-PLAN.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-06-PLAN.md

<interfaces>
From `docs/commands.md`:
```markdown
## Agent Entrypoints
- Claude/Gemini: `/opsx-<action>`
- Codex (recommended): `$opsx <request>`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README and docs to the explicit route surface and hard clean break</name>
  <files>
README.md
README-zh.md
docs/commands.md
docs/codex.md
docs/supported-tools.md
docs/runtime-guidance.md
  </files>
  <read_first>
README.md
README-zh.md
docs/commands.md
docs/codex.md
docs/supported-tools.md
docs/runtime-guidance.md
commands/codex/prompts/opsx.md
commands/codex/prompts/opsx-status.md
.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
  </read_first>
  <action>Per D-01 through D-04 and D-13 through D-16, remove the remaining public-route drift from README and docs. In `README.md`, `README-zh.md`, `docs/commands.md`, `docs/codex.md`, and `docs/supported-tools.md`, replace the current “preferred `$opsx <request>`” model with an explicit `$opsx-*` route inventory and concrete examples such as `$opsx-onboard`, `$opsx-propose`, `$opsx-status`, and `$opsx-apply`. Remove the `OpsX was originally adapted from Fission-AI/OpenSpec.` sentence from both READMEs. In `docs/runtime-guidance.md`, rewrite the compatibility section so it says runtime reads current `.opsx` artifacts only; do not present `openspec/`, `.openspec`, or `~/.openspec` as current user guidance even though migration internals may still understand them. None of these docs may mention standalone `$opsx`, `$opsx <request>`, `/openspec`, `$openspec`, `/prompts:openspec`, `/opsx:*`, `/prompts:opsx-*`, `openspec/`, `.openspec`, or `~/.openspec` as current user-facing routes or paths.</action>
  <acceptance_criteria>
- `README.md`, `README-zh.md`, `docs/commands.md`, `docs/codex.md`, and `docs/supported-tools.md` contain explicit `$opsx-*` examples and no standalone `$opsx` or `$opsx <request>` guidance.
- `README.md` and `README-zh.md` no longer contain `Fission-AI/OpenSpec`.
- `docs/runtime-guidance.md` describes current `.opsx` runtime inputs without presenting legacy paths as current user guidance.
- The rewritten docs do not contain `/openspec`, `$openspec`, `/prompts:openspec`, `/opsx:*`, `/prompts:opsx-*`, `openspec/`, `.openspec`, or `~/.openspec` as current public UX.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "\\$opsx-(onboard|propose|status|apply)" README.md README-zh.md docs/commands.md docs/codex.md docs/supported-tools.md</automated>
    <automated>node -e "const fs=require('fs');const files=['README.md','README-zh.md','docs/commands.md','docs/codex.md','docs/supported-tools.md','docs/runtime-guidance.md'];const banned=['$opsx <request>','/openspec','$openspec','/prompts:openspec','/opsx:*','/prompts:opsx-*','openspec/','.openspec','~/.openspec','Fission-AI/OpenSpec'];for(const file of files){const text=fs.readFileSync(file,'utf8');for(const token of banned){if(text.includes(token)){throw new Error(file+' contains '+token);}}}"</automated>
  </verify>
  <done>
- User-facing README/docs surfaces match the explicit-only route model and hard clean break.
- Public docs no longer preserve OpenSpec lineage or legacy path guidance as current UX.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated route contract -> public docs | Stale docs can send users to removed entrypoints or paths even when generated prompts are correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing / Tampering | `README*.md`, `docs/*.md` | mitigate | Rewrite every public route and path reference to the explicit-only current OpsX surface and remove all banned legacy/public-route tokens. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep README/docs for expected `$opsx-*` examples and scan them for banned public-route/path tokens.
</verification>

<success_criteria>
- README and docs match the explicit-only route model and hard clean break.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-10-SUMMARY.md`
</output>
