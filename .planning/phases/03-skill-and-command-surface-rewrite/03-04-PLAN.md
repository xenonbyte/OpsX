---
phase: 03-skill-and-command-surface-rewrite
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - commands/claude/opsx/new.md
  - commands/claude/opsx/onboard.md
  - commands/claude/opsx/propose.md
  - commands/claude/opsx/resume.md
  - commands/claude/opsx/status.md
  - commands/claude/opsx/sync.md
  - commands/claude/opsx/verify.md
autonomous: true
requirements:
  - CMD-01
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "The second bounded Claude refresh slice brings the remaining Claude action leaves, including `onboard`, `resume`, and `status`, into parity with the new contract."
    - "Claude empty-state routes describe missing workspace or no active change as guidance, not implicit state creation."
  artifacts:
    - path: "commands/claude/opsx/onboard.md"
      provides: "Claude onboarding fallback guidance from the shared templates"
      contains: "/opsx-onboard"
    - path: "commands/claude/opsx/status.md"
      provides: "Claude status fallback guidance for missing workspace or active change"
      contains: "/opsx-status"
  key_links:
    - from: "lib/generator.js"
      to: "commands/claude/opsx/onboard.md"
      via: "buildPlatformBundle('claude')"
      pattern: "buildPlatformBundle"
    - from: "templates/commands/action.md.tmpl"
      to: "commands/claude/opsx/resume.md"
      via: "shared fallback wording"
      pattern: "resume"
---

<objective>
Finish the Claude generated refresh in a second bounded slice that includes the empty-state routes.

Purpose: isolate the Claude files that carry `onboard`/`status`/`resume` fallback text so they stay under the warning threshold while still verifying CMD-05 on the checked-in bundle.
Output: the remaining seven Claude action files regenerated from `buildPlatformBundle('claude')`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {
  /* returns opsx/onboard.md, opsx/resume.md, opsx/status.md and other action leaves */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the remaining Claude action leaves, including the empty-state routes</name>
  <files>
commands/claude/opsx/new.md
commands/claude/opsx/onboard.md
commands/claude/opsx/propose.md
commands/claude/opsx/resume.md
commands/claude/opsx/status.md
commands/claude/opsx/sync.md
commands/claude/opsx/verify.md
  </files>
  <read_first>
commands/claude/opsx/onboard.md
commands/claude/opsx/resume.md
commands/claude/opsx/status.md
lib/generator.js
templates/commands/action.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md
  </read_first>
  <action>Per D-09 through D-12 and D-17 through D-22, refresh only the remaining Claude action files listed here from `buildPlatformBundle('claude')`. Keep this plan mechanical: no source-template edits, no Codex or Gemini files, and no manual prose changes. The important outcome is that the checked-in Claude `onboard`, `resume`, and `status` leaves now carry the exact non-mutating empty-state guidance defined in 03-02.</action>
  <acceptance_criteria>
- All seven listed Claude files exactly match the current `buildPlatformBundle('claude')` output.
- `commands/claude/opsx/onboard.md`, `resume.md`, and `status.md` carry the Phase 3 missing-workspace or no-active-change fallback behavior.
- None of the refreshed files auto-create state or reintroduce banned public entrypoint wording.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const { buildPlatformBundle } = require('./lib/generator');const bundle=buildPlatformBundle('claude');['opsx/new.md','opsx/onboard.md','opsx/propose.md','opsx/resume.md','opsx/status.md','opsx/sync.md','opsx/verify.md'].forEach((rel)=>{const actual=fs.readFileSync(path.join('commands','claude',rel),'utf8');if(actual!==bundle[rel]){throw new Error(rel);}});"</automated>
  </verify>
  <done>
- The Claude bundle is fully refreshed across two bounded plans, with CMD-05 guidance now present in checked-in Claude outputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Claude empty-state prompts | A partial refresh can leave `onboard`/`resume`/`status` inconsistent with the shared fallback contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | `commands/claude/opsx/onboard.md`, `resume.md`, `status.md` | mitigate | Regenerate the remaining Claude leaves from `buildPlatformBundle('claude')` and verify exact parity for the listed files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Compare each listed Claude file against `buildPlatformBundle('claude')`.
</verification>

<success_criteria>
- The second bounded Claude refresh slice is generated, checked in, and parity-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-04-SUMMARY.md`
</output>
