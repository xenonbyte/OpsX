---
phase: 03-skill-and-command-surface-rewrite
plan: 07
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - commands/gemini/opsx.toml
  - commands/gemini/opsx/apply.toml
  - commands/gemini/opsx/archive.toml
  - commands/gemini/opsx/batch-apply.toml
  - commands/gemini/opsx/bulk-archive.toml
  - commands/gemini/opsx/continue.toml
  - commands/gemini/opsx/explore.toml
  - commands/gemini/opsx/ff.toml
autonomous: true
requirements:
  - CMD-04
must_haves:
  truths:
    - "The Gemini index and first bounded Gemini action slice are regenerated directly from the updated Phase 3 source of truth."
    - "These refreshed Gemini files keep `/opsx-*` guidance aligned with Claude and do not leak banned legacy or Codex-only route wording."
  artifacts:
    - path: "commands/gemini/opsx.toml"
      provides: "Refreshed Gemini index from `buildPlatformBundle('gemini')`"
      contains: "/opsx-<action>"
    - path: "commands/gemini/opsx/explore.toml"
      provides: "Representative first-half Gemini action prompt regenerated from shared templates"
      contains: "/opsx-explore"
  key_links:
    - from: "lib/generator.js"
      to: "commands/gemini/opsx.toml"
      via: "buildPlatformBundle('gemini')"
      pattern: "buildPlatformBundle"
    - from: "templates/commands/gemini-index.toml.tmpl"
      to: "commands/gemini/opsx.toml"
      via: "wrapper prompt generation"
      pattern: "prompt ="
---

<objective>
Refresh the first bounded slice of checked-in Gemini command files from the new generator contract.

Purpose: keep Gemini generated refresh work inside the warning-safe budget while maintaining three-platform parity with the shared Phase 3 source of truth.
Output: the Gemini index plus the first seven Gemini action files regenerated from `buildPlatformBundle('gemini')`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-RESEARCH.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {
  if (platform === 'gemini') {
    files['opsx.toml'] = buildGeminiIndexToml('OpsX Workflow');
    actions.forEach((action) => {
      files[`opsx/${action.id}.toml`] = buildGeminiActionToml(action);
    });
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Gemini index and first-half action leaves from `buildPlatformBundle('gemini')`</name>
  <files>
commands/gemini/opsx.toml
commands/gemini/opsx/apply.toml
commands/gemini/opsx/archive.toml
commands/gemini/opsx/batch-apply.toml
commands/gemini/opsx/bulk-archive.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/explore.toml
commands/gemini/opsx/ff.toml
  </files>
  <read_first>
commands/gemini/opsx.toml
commands/gemini/opsx/explore.toml
lib/generator.js
templates/commands/gemini-index.toml.tmpl
templates/commands/gemini-action.toml.tmpl
templates/commands/action.md.tmpl
.planning/phases/03-skill-and-command-surface-rewrite/03-02-PLAN.md
  </read_first>
  <action>Per D-09 through D-12, refresh only the bounded Gemini slice listed here from `buildPlatformBundle('gemini')`. Do not edit shared templates or other platforms in this plan. The required outcome is a mechanical checked-in refresh that keeps Gemini aligned with the new shared route and preflight contract while staying under the warning threshold.</action>
  <acceptance_criteria>
- All eight listed Gemini files exactly match the current `buildPlatformBundle('gemini')` output.
- None of the refreshed files contain `$opsx <request>`, standalone `$opsx`, `/openspec`, `$openspec`, `/prompts:openspec`, `/opsx:*`, or `/prompts:opsx-*`.
- The refreshed Gemini index still lists the expected `/opsx-*` route inventory.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>node -e "const fs=require('fs');const path=require('path');const { buildPlatformBundle } = require('./lib/generator');const bundle=buildPlatformBundle('gemini');['opsx.toml','opsx/apply.toml','opsx/archive.toml','opsx/batch-apply.toml','opsx/bulk-archive.toml','opsx/continue.toml','opsx/explore.toml','opsx/ff.toml'].forEach((rel)=>{const actual=fs.readFileSync(path.join('commands','gemini',rel),'utf8');if(actual!==bundle[rel]){throw new Error(rel);}});"</automated>
  </verify>
  <done>
- The first bounded Gemini refresh slice is generated, checked in, and parity-verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Gemini files | Partial refreshes can leave Gemini outputs inconsistent with the shared contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `commands/gemini/opsx*.toml` | mitigate | Regenerate only from `buildPlatformBundle('gemini')` and verify byte-for-byte parity for the listed files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Compare each listed Gemini file against `buildPlatformBundle('gemini')`.
</verification>

<success_criteria>
- The first bounded Gemini refresh slice is generated, checked in, and parity-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-and-command-surface-rewrite/03-07-SUMMARY.md`
</output>
