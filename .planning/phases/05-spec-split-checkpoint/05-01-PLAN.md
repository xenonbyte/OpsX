---
phase: 05-spec-split-checkpoint
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - schemas/spec-driven/schema.json
  - lib/workflow.js
  - lib/change-store.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - SPEC-01
must_haves:
  truths:
    - "Schema and workflow validation recognize `spec-split-checkpoint` as the planning gate that runs after `specs` and before `design`."
    - "Persisted change state keeps a dedicated split-spec checkpoint slot without dropping `spec`, `task`, or `execution`."
    - "Phase 5 checkpoint catalogs and alias mapping survive normalize/write/read cycles through the existing Phase 4 store."
  artifacts:
    - path: "schemas/spec-driven/schema.json"
      provides: "Canonical `spec-split-checkpoint` definition"
      contains: "\"id\": \"spec-split-checkpoint\""
    - path: "lib/workflow.js"
      provides: "Workflow checkpoint catalog and next-step semantics"
      contains: "DEFAULT_CHECKPOINT_IDS"
    - path: "lib/change-store.js"
      provides: "Canonical checkpoint-id to persisted-key normalization"
      contains: "recordCheckpointResult"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Wave 0 schema/state regression coverage for the new checkpoint"
      contains: "spec-split-checkpoint schema is inserted after specs and before design"
  key_links:
    - from: "schemas/spec-driven/schema.json"
      to: "lib/workflow.js"
      via: "validateCheckpointContracts() checks the exact checkpoint id, trigger, insertion order, and states"
      pattern: "spec-split-checkpoint|after-specs-before-design"
    - from: "lib/workflow.js"
      to: "lib/change-store.js"
      via: "canonical checkpoint ids are persisted through a stable alias map instead of a new artifact type"
      pattern: "spec-split-checkpoint|specSplit"
---

<objective>
Lock the Phase 5 checkpoint catalog and persistence contract before validator or guidance work begins.

Purpose: satisfy SPEC-01 and D-01 through D-03 first so later plans can rely on a stable schema id, workflow catalog, and state-store slot.
Output: updated schema/workflow catalogs, a persisted `specSplit` checkpoint slot, and green Wave 0 regression coverage.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
@.planning/phases/05-spec-split-checkpoint/05-VALIDATION.md
@.planning/phases/05-spec-split-checkpoint/05-PATTERNS.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md

<interfaces>
From `schemas/spec-driven/schema.json`:
```json
{
  "id": "spec-checkpoint",
  "phase": "planning",
  "trigger": "after-design-before-tasks",
  "insertion": {
    "after": ["design"],
    "before": ["tasks"]
  },
  "states": ["PASS", "WARN", "BLOCK"]
}
```

From `lib/workflow.js`:
```javascript
const CHECKPOINT_STATES = ['PASS', 'WARN', 'BLOCK'];
const DEFAULT_CHECKPOINT_IDS = ['spec-checkpoint', 'task-checkpoint', 'execution-checkpoint'];

function buildCheckpointResult(schema, checkpointId, findings = [], extra = {}) {}
function buildCheckpointNextStep(checkpointId, status, patchTargets) {}
```

From `lib/change-store.js`:
```javascript
const DEFAULT_CHECKPOINTS = Object.freeze({
  spec: Object.freeze({ status: 'PENDING', updatedAt: null }),
  task: Object.freeze({ status: 'PENDING', updatedAt: null }),
  execution: Object.freeze({ status: 'PENDING', updatedAt: null })
});

function normalizeChangeState(raw, options = {}) {}
function recordCheckpointResult(changeDir, checkpointId, result, currentHashes) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pre-design checkpoint catalog and persisted alias mapping</name>
  <files>
schemas/spec-driven/schema.json
lib/workflow.js
lib/change-store.js
  </files>
  <behavior>
    - The canonical schema id is `spec-split-checkpoint` with trigger `after-specs-before-design`.
    - Public checkpoint output remains canonical while state persistence uses the existing Phase 4 store.
    - Existing `spec`, `task`, and `execution` checkpoint slots remain readable and unchanged.
  </behavior>
  <read_first>
schemas/spec-driven/schema.json
lib/workflow.js
lib/change-store.js
lib/change-state.js
.planning/REQUIREMENTS.md
.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
.planning/phases/05-spec-split-checkpoint/05-PATTERNS.md
.planning/phases/05-spec-split-checkpoint/05-VALIDATION.md
  </read_first>
  <action>Implement D-01, D-02, and D-03 exactly. In `schemas/spec-driven/schema.json`, insert a new checkpoint object immediately after `specs` and before `design` with `id: "spec-split-checkpoint"`, `phase: "planning"`, `trigger: "after-specs-before-design"`, `insertion.after: ["specs"]`, `insertion.before: ["design"]`, and `states: ["PASS", "WARN", "BLOCK"]`. In `lib/workflow.js`, add `spec-split-checkpoint` to `DEFAULT_CHECKPOINT_IDS` and teach `buildCheckpointNextStep()` that a passing split-spec checkpoint proceeds to `design` while the existing `spec-checkpoint` remains the later design-before-tasks gate per D-02. Do not add `runSpecSplitCheckpoint()` in this plan. In `lib/change-store.js`, extend the persisted checkpoint shape with a dedicated `specSplit` slot and add an explicit alias map that accepts `spec-split-checkpoint`, `spec-split`, and `specSplit` while still normalizing `spec-checkpoint`, `task-checkpoint`, and `execution-checkpoint` to the existing Phase 4 slots. `recordCheckpointResult()` must write the normalized persisted key, while public-facing output remains the canonical checkpoint id. Do not introduce any new artifact file, new dependency, or alternate checkpoint document.</action>
  <acceptance_criteria>
- `schemas/spec-driven/schema.json` contains the exact strings `"id": "spec-split-checkpoint"` and `"trigger": "after-specs-before-design"`.
- `lib/workflow.js` contains `DEFAULT_CHECKPOINT_IDS` with `spec-split-checkpoint` included and `buildCheckpointNextStep()` includes a `Proceed to design.` branch for this checkpoint.
- `lib/change-store.js` contains a dedicated `specSplit` checkpoint slot and a canonical alias map for `spec-split-checkpoint`.
- `lib/change-store.js` still preserves `spec`, `task`, and `execution` checkpoint slots.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint|after-specs-before-design|Proceed to design\\.|specSplit" schemas/spec-driven/schema.json lib/workflow.js lib/change-store.js</automated>
  </verify>
  <done>
- The repo has one canonical split-spec checkpoint id and one normalized persisted state slot.
- Later plans can rely on the schema/workflow/state catalog without inventing compatibility shims.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Wave 0 regression coverage for checkpoint ordering and persistence</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
schemas/spec-driven/schema.json
lib/workflow.js
lib/change-store.js
.planning/phases/05-spec-split-checkpoint/05-VALIDATION.md
.planning/phases/05-spec-split-checkpoint/05-PATTERNS.md
  </read_first>
  <action>Add the exact Wave 0 contract tests called for by 05-VALIDATION. In `scripts/test-workflow-runtime.js`, create tests named `spec-split-checkpoint schema is inserted after specs and before design`, `change-store preserves specSplit checkpoint alias round-trip`, and `checkpoint contract validators remain green with spec-split-checkpoint`. The schema-order test must assert the new checkpoint appears before the existing `spec-checkpoint` and exposes `PASS`, `WARN`, and `BLOCK`. The round-trip test must seed `recordCheckpointResult(changeDir, 'spec-split-checkpoint', ...)`, reload state, and assert the persisted slot is `checkpoints.specSplit` while the existing `spec`, `task`, and `execution` slots remain present. The validator test must keep `validateCheckpointContracts()` green after the new id is added. Keep the quick/full command `npm run test:workflow-runtime` green at the end of the plan.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` contains the exact test names `spec-split-checkpoint schema is inserted after specs and before design`, `change-store preserves specSplit checkpoint alias round-trip`, and `checkpoint contract validators remain green with spec-split-checkpoint`.
- The round-trip test asserts `state.checkpoints.specSplit.status` after a persisted write using the canonical checkpoint id.
- The validator test still exercises the existing checkpoint catalog rather than replacing it with a Phase 5-only fixture.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint schema is inserted after specs and before design|change-store preserves specSplit checkpoint alias round-trip|checkpoint contract validators remain green with spec-split-checkpoint" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Wave 0 locks the schema/state contract before the validator and guidance layers change.
- Future plans can detect catalog drift and state-slot regressions automatically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema catalog -> runtime validators | a wrong checkpoint id or insertion order can silently move the gate to the wrong stage |
| canonical checkpoint id -> persisted `state.yaml` slot | mismatched ids can drop checkpoint status on disk or overwrite the wrong slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `schemas/spec-driven/schema.json` / `lib/workflow.js` | mitigate | Test the exact `spec-split-checkpoint` id, trigger, insertion order, and `PASS/WARN/BLOCK` states together. |
| T-05-02 | Tampering | `lib/change-store.js` | mitigate | Normalize canonical ids to an explicit `specSplit` slot and add a round-trip regression using `recordCheckpointResult()`. |
| T-05-03 | Denial of Service | checkpoint contract validation | mitigate | Keep `validateCheckpointContracts()` green so missing checkpoint definitions fail immediately. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the schema/workflow/state files for the canonical checkpoint strings and alias slot.
</verification>

<success_criteria>
- SPEC-01 is anchored in the schema, workflow catalog, and persisted state store.
- The Phase 4 checkpoint infrastructure accepts the new checkpoint without creating a new artifact type.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-split-checkpoint/05-01-SUMMARY.md`
</output>
