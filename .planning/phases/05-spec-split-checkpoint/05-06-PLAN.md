---
phase: 05-spec-split-checkpoint
plan: 06
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - commands/gemini/opsx/propose.toml
  - commands/gemini/opsx/continue.toml
  - commands/gemini/opsx/ff.toml
autonomous: true
requirements:
  - SPEC-03
must_haves:
  truths:
    - "The checked-in Gemini `propose`, `continue`, and `ff` prompts mention `spec-split-checkpoint` before `design`."
    - "The Gemini planning-route slice matches current generator output with no hand-edited drift."
  artifacts:
    - path: "commands/gemini/opsx/propose.toml"
      provides: "Gemini propose prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
    - path: "commands/gemini/opsx/continue.toml"
      provides: "Gemini continue prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
    - path: "commands/gemini/opsx/ff.toml"
      provides: "Gemini fast-forward prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
  key_links:
    - from: "lib/generator.js"
      to: "commands/gemini/opsx/*.toml"
      via: "buildPlatformBundle('gemini') renders the bounded split-spec planning note"
      pattern: "spec-split-checkpoint"
---

<objective>
Refresh the bounded Gemini planning-route slice from the updated Phase 5 source of truth.

Purpose: ship the new split-spec guidance to checked-in Gemini prompts without widening the generated refresh scope.
Output: regenerated `commands/gemini/opsx/propose.toml`, `continue.toml`, and `ff.toml`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
@lib/generator.js
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Gemini `propose` / `continue` / `ff` slice from `buildPlatformBundle('gemini')`</name>
  <files>
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
  </files>
  <read_first>
lib/generator.js
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
scripts/test-workflow-runtime.js
.planning/phases/05-spec-split-checkpoint/05-03-PLAN.md
  </read_first>
  <action>Refresh only the bounded Gemini planning-route files from generator output: `commands/gemini/opsx/propose.toml`, `commands/gemini/opsx/continue.toml`, and `commands/gemini/opsx/ff.toml`. Generate content from `buildPlatformBundle('gemini')` and replace the checked-in files byte-for-byte; do not hand edit prose. Do not touch `commands/gemini/opsx.toml` or any non-listed Gemini prompt. Each refreshed file must contain the exact Phase 5 planning note that mentions `spec-split-checkpoint` after `specs` and before `design` while preserving the later `spec checkpoint` and `task checkpoint` note.</action>
  <acceptance_criteria>
- Only `commands/gemini/opsx/propose.toml`, `commands/gemini/opsx/continue.toml`, and `commands/gemini/opsx/ff.toml` are modified by this plan.
- All three files contain the exact string `spec-split-checkpoint`.
- `npm run test:workflow-runtime` passes with the temporary Phase 5 parity exemption still active for the remaining platforms.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint" commands/gemini/opsx/propose.toml commands/gemini/opsx/continue.toml commands/gemini/opsx/ff.toml</automated>
  </verify>
  <done>
- The checked-in Gemini planning prompts match the new Phase 5 source-of-truth note exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Gemini prompts | manual edits or partial refreshes can leave public guidance inconsistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | `commands/gemini/opsx/{propose,continue,ff}.toml` | mitigate | Regenerate only the bounded 3-file slice from `buildPlatformBundle('gemini')` and assert the exact `spec-split-checkpoint` string. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the three Gemini files for `spec-split-checkpoint`.
</verification>

<success_criteria>
- The Gemini planning-route slice is parity-aligned with Phase 5 source-of-truth guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-split-checkpoint/05-06-SUMMARY.md`
</output>
