---
phase: 05-spec-split-checkpoint
plan: 07
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-05
  - 05-06
files_modified:
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - SPEC-03
  - SPEC-04
must_haves:
  truths:
    - "Temporary Phase 5 parity exemptions are removed once all 9 planning-route prompts are refreshed."
    - "The runtime suite re-locks full checked-in command parity while preserving spec-split checkpoint contract coverage."
    - "Phase-wide regression coverage proves the split-spec checkpoint note exists in generated output and checked-in planning prompts."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Final parity and regression gate for Phase 5"
      contains: "spec-split-checkpoint"
  key_links:
    - from: "lib/generator.js"
      to: "commands/**"
      via: "full bundle parity is restored after the bounded 9-file refresh wave"
      pattern: "buildPlatformBundle"
    - from: "lib/workflow.js"
      to: "scripts/test-workflow-runtime.js"
      via: "split-spec checkpoint contract assertions remain active after temporary exemptions are removed"
      pattern: "runSpecSplitCheckpoint"
---

<objective>
Restore strict repo-wide generated parity and close Phase 5 with one final regression gate.

Purpose: remove the temporary bounded-refresh escape hatch and prove the split-spec checkpoint contract is stable across workflow, source output, and checked-in prompts.
Output: a single test file with no Phase 5 parity exemptions left and green phase-wide runtime regression coverage.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
@scripts/test-workflow-runtime.js
@lib/generator.js
@lib/workflow.js

<interfaces>
From `scripts/test-workflow-runtime.js`:
```javascript
function collectBundleParity(platform, generatedBundle) {}
const PLATFORM_BUNDLE_TARGETS = Object.freeze({ /* checked-in roots */ });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove temporary Phase 5 parity exemptions and re-lock full regression coverage</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
lib/generator.js
lib/workflow.js
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
.planning/phases/05-spec-split-checkpoint/05-03-PLAN.md
  </read_first>
  <action>Remove the temporary Phase 5 parity exemption added in 05-03 and restore strict repo-wide checked-in parity against `buildPlatformBundle()` output. Keep the split-spec checkpoint source-output assertions active for the 9 planning-route prompts and add explicit checked-in assertions that those same 9 files contain `spec-split-checkpoint` and do not contain `spec-review.md`. Keep the runner-contract tests for `runSpecSplitCheckpoint()` active so SPEC-04 remains verified after the exemption disappears. Do not add any new exemption path or broaden the scope of checked-in refresh files beyond the 9 prompts already refreshed in 05-04 through 05-06.</action>
  <acceptance_criteria>
- `scripts/test-workflow-runtime.js` no longer contains a non-empty Phase 5 parity exemption list.
- Repo-wide bundle parity assertions are strict again for Claude, Codex, and Gemini with no Phase 5 exceptions.
- The final regression gate asserts the 9 refreshed checked-in planning prompts contain `spec-split-checkpoint` and omit `spec-review.md`.
- `npm run test:workflow-runtime` passes after the exemption is removed.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint|spec-review.md|bundle parity|runSpecSplitCheckpoint" scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- Phase 5 ends with strict checked-in command parity restored and no temporary test escape hatches.
- SPEC-03 and SPEC-04 stay covered by one repeatable final regression command.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temporary parity waiver -> final repo state | a lingering exemption can hide generated drift in shipped prompts |
| checked-in planning prompts -> workflow users | stale checked-in prompt content can contradict the source-of-truth split-spec contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | `scripts/test-workflow-runtime.js` parity gate | mitigate | Remove the temporary Phase 5 exemption entirely and restore strict repo-wide parity assertions. |
| T-05-13 | Information Integrity | checked-in planning prompts | mitigate | Add explicit checked-in prompt assertions for `spec-split-checkpoint` and `spec-review.md` absence across the 9 refreshed files. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the final regression file for the split-spec contract assertions and absence of lingering parity exemptions.
</verification>

<success_criteria>
- Phase 5 closes with strict parity restored and one green phase-wide regression command.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-split-checkpoint/05-07-SUMMARY.md`
</output>
