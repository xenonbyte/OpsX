---
phase: 05-spec-split-checkpoint
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - commands/claude/opsx/propose.md
  - commands/claude/opsx/continue.md
  - commands/claude/opsx/ff.md
autonomous: true
requirements:
  - SPEC-03
must_haves:
  truths:
    - "The checked-in Claude `propose`, `continue`, and `ff` prompts mention `spec-split-checkpoint` before `design`."
    - "The Claude planning-route slice matches current generator output with no hand-edited drift."
  artifacts:
    - path: "commands/claude/opsx/propose.md"
      provides: "Claude propose prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
    - path: "commands/claude/opsx/continue.md"
      provides: "Claude continue prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
    - path: "commands/claude/opsx/ff.md"
      provides: "Claude fast-forward prompt updated for split-spec review"
      contains: "spec-split-checkpoint"
  key_links:
    - from: "lib/generator.js"
      to: "commands/claude/opsx/*.md"
      via: "buildPlatformBundle('claude') renders the bounded split-spec planning note"
      pattern: "spec-split-checkpoint"
---

<objective>
Refresh the bounded Claude planning-route slice from the updated Phase 5 source of truth.

Purpose: ship the new split-spec guidance to checked-in Claude prompts without touching unrelated generated files.
Output: regenerated `commands/claude/opsx/propose.md`, `continue.md`, and `ff.md`.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
@lib/generator.js
@scripts/test-workflow-runtime.js

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the Claude `propose` / `continue` / `ff` slice from `buildPlatformBundle('claude')`</name>
  <files>
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
  </files>
  <read_first>
lib/generator.js
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
scripts/test-workflow-runtime.js
.planning/phases/05-spec-split-checkpoint/05-03-PLAN.md
  </read_first>
  <action>Refresh only the bounded Claude planning-route files from generator output: `commands/claude/opsx/propose.md`, `commands/claude/opsx/continue.md`, and `commands/claude/opsx/ff.md`. Generate content from `buildPlatformBundle('claude')` and replace the checked-in files byte-for-byte; do not hand edit prose. Do not touch `commands/claude/opsx.md` or any non-listed Claude action file. Each refreshed file must contain the exact Phase 5 planning note that mentions `spec-split-checkpoint` after `specs` and before `design` while preserving the later `spec checkpoint` and `task checkpoint` note.</action>
  <acceptance_criteria>
- Only `commands/claude/opsx/propose.md`, `commands/claude/opsx/continue.md`, and `commands/claude/opsx/ff.md` are modified by this plan.
- All three files contain the exact string `spec-split-checkpoint`.
- `npm run test:workflow-runtime` passes with the temporary Phase 5 parity exemption still active for the remaining platforms.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint" commands/claude/opsx/propose.md commands/claude/opsx/continue.md commands/claude/opsx/ff.md</automated>
  </verify>
  <done>
- The checked-in Claude planning prompts match the new Phase 5 source-of-truth note exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-of-truth generator -> checked-in Claude prompts | manual edits or partial refreshes can leave public guidance inconsistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | `commands/claude/opsx/{propose,continue,ff}.md` | mitigate | Regenerate only the bounded 3-file slice from `buildPlatformBundle('claude')` and assert the exact `spec-split-checkpoint` string. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the three Claude files for `spec-split-checkpoint`.
</verification>

<success_criteria>
- The Claude planning-route slice is parity-aligned with Phase 5 source-of-truth guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-split-checkpoint/05-04-SUMMARY.md`
</output>
