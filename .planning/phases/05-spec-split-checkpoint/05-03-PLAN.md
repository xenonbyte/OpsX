---
phase: 05-spec-split-checkpoint
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - lib/workflow.js
  - lib/generator.js
  - skills/opsx/SKILL.md
  - skills/opsx/references/action-playbooks.md
  - skills/opsx/references/action-playbooks-zh.md
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - SPEC-02
  - SPEC-03
  - SPEC-04
must_haves:
  truths:
    - "`runSpecSplitCheckpoint()` returns the canonical checkpoint contract for clean, invalid, and escalated review cases."
    - "Simple single-spec changes can be reviewed inline, while risky spec sets surface a read-only reviewer recommendation without a new route or artifact."
    - "Source-of-truth generator and skill guidance place `spec-split-checkpoint` between `specs` and `design` while deferring TDD-light and verify/archive hard gates."
  artifacts:
    - path: "lib/workflow.js"
      provides: "Split-spec checkpoint facade and canonical next-step behavior"
      contains: "function runSpecSplitCheckpoint"
    - path: "lib/generator.js"
      provides: "Bounded planning checkpoint note helper for propose/continue/ff only"
      contains: "getPlanningCheckpointNote"
    - path: "skills/opsx/SKILL.md"
      provides: "Distributed skill contract updated for spec-split review behavior"
      contains: "spec-split-checkpoint"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Runner contract tests plus temporary bounded parity exemption for the 9 planning-route prompts"
      contains: "read-only reviewer"
  key_links:
    - from: "lib/spec-validator.js"
      to: "lib/workflow.js"
      via: "runSpecSplitCheckpoint() consumes structured evidence and stable findings"
      pattern: "collectSpecSplitEvidence|reviewSpecSplitEvidence"
    - from: "lib/generator.js"
      to: "commands/**"
      via: "buildPlatformBundle() renders the split-spec planning note into propose/continue/ff prompts"
      pattern: "spec-split-checkpoint"
    - from: "skills/opsx/SKILL.md"
      to: "skills/opsx/references/action-playbooks*.md"
      via: "skill contract and bilingual playbooks describe the same inline-vs-read-only review behavior"
      pattern: "spec-split-checkpoint|spec-review.md"
---

<objective>
Wire the reusable validator into the workflow facade and align source-of-truth guidance before the bounded generated refresh wave.

Purpose: satisfy SPEC-03 and SPEC-04 while keeping Phase 5 bounded to existing routes, existing artifacts, and the existing checkpoint result contract.
Output: `runSpecSplitCheckpoint()`, bounded source-of-truth updates for `propose`/`continue`/`ff`, aligned skill/playbooks, and a temporary 9-file parity exemption until checked-in command slices are refreshed.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
@.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
@.planning/phases/05-spec-split-checkpoint/05-VALIDATION.md
@.planning/phases/05-spec-split-checkpoint/05-PATTERNS.md
@.planning/phases/03-skill-and-command-surface-rewrite/03-CONTEXT.md
@.planning/phases/04-change-state-machine-and-drift-control/04-CONTEXT.md

<interfaces>
From `lib/spec-validator.js`:
```javascript
function collectSpecSplitEvidence(options = {}) {}
function parseSpecFile(filePath, text) {}
function reviewSpecSplitEvidence(evidence, options = {}) {}
```

From `lib/workflow.js`:
```javascript
function buildCheckpointResult(schema, checkpointId, findings = [], extra = {}) {}
function resolveSecurityReviewState(options = {}) {}
```

From `lib/generator.js`:
```javascript
function buildActionMarkdown(platform, action) {
  return renderTemplate(template, {
    planning_checkpoint_note: '...'
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `runSpecSplitCheckpoint()` and bound the source-of-truth prompt drift to 9 planning prompts</name>
  <files>
lib/workflow.js
lib/generator.js
scripts/test-workflow-runtime.js
  </files>
  <behavior>
    - The workflow facade returns the canonical checkpoint contract without adding a new route or a `spec-review.md` artifact.
    - Risky spec sets surface a read-only reviewer recommendation before design.
    - Source-of-truth changes are limited to `propose`, `continue`, and `ff` so the generated refresh wave stays bounded.
  </behavior>
  <read_first>
lib/spec-validator.js
lib/workflow.js
lib/generator.js
scripts/test-workflow-runtime.js
commands/claude/opsx/propose.md
commands/claude/opsx/continue.md
commands/claude/opsx/ff.md
commands/codex/prompts/opsx-propose.md
commands/codex/prompts/opsx-continue.md
commands/codex/prompts/opsx-ff.md
commands/gemini/opsx/propose.toml
commands/gemini/opsx/continue.toml
commands/gemini/opsx/ff.toml
.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
.planning/phases/05-spec-split-checkpoint/05-PATTERNS.md
.planning/phases/05-spec-split-checkpoint/05-VALIDATION.md
  </read_first>
  <action>Implement D-05 through D-10 and D-11 in the workflow/generator layer. In `lib/workflow.js`, require `collectSpecSplitEvidence()` and `reviewSpecSplitEvidence()` from `./spec-validator`, then export a new `runSpecSplitCheckpoint(options = {})`. The new facade must accept either `options.specFiles` (preferred array of `{ path, text }`) or a single inline fallback derived from `options.sources.specs` at virtual path `specs/inline/spec.md` so simple single-spec reviews still work per D-08. It must call `buildCheckpointResult(schema, 'spec-split-checkpoint', findings, ...)` and keep the required output fields `checkpoint`, `phase`, `status`, `findings`, `patchTargets`, and `nextStep`. When the validator finds no issues, the split-spec checkpoint passes and `nextStep` is `Proceed to design.`. When escalation is recommended, add a single `WARN` finding with code `read-only-reviewer-recommended`, no new artifact target, and an explicit `nextStep` that says a read-only reviewer should inspect the existing `proposal`/`specs` before design, must not write files directly, and must not create `spec-review.md`. Trigger escalation when any of these are true: more than one spec file, more than one unique capability directory, `resolveSecurityReviewState(options)` returns `required` or `recommended`, or the parsed requirement count is `>= 7`. In `lib/generator.js`, replace the single global planning checkpoint string with a helper such as `getPlanningCheckpointNote(actionId)` so only `propose`, `continue`, and `ff` mention the exact Phase 5 path: ``spec-split-checkpoint` runs after `specs` and before `design`; `spec checkpoint` runs after `design` and before `tasks`; `task checkpoint` runs after `tasks` and before `apply`.`` Keep other action prompts and all platform index/catalog files unchanged so the checked-in drift remains bounded to 9 planning-route files. In `scripts/test-workflow-runtime.js`, add contract tests for PASS, BLOCK, and escalation-WARN cases plus a temporary Phase 5 parity exemption list covering only the 9 checked-in `propose`, `continue`, and `ff` action files across Claude/Codex/Gemini. Keep source-output assertions active by checking `buildPlatformBundle()` output for `spec-split-checkpoint` in those 9 prompts.</action>
  <acceptance_criteria>
- `lib/workflow.js` exports `runSpecSplitCheckpoint`.
- `runSpecSplitCheckpoint()` returns the canonical checkpoint id `spec-split-checkpoint` and does not add `createsArtifacts` entries.
- `lib/workflow.js` contains the exact warning code `read-only-reviewer-recommended`.
- `lib/generator.js` contains a helper that scopes the new planning checkpoint note to `propose`, `continue`, and `ff` only.
- `scripts/test-workflow-runtime.js` contains a temporary Phase 5 parity exemption list for exactly 9 generated prompt files and source-output assertions for `spec-split-checkpoint`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "runSpecSplitCheckpoint|read-only-reviewer-recommended|spec-split-checkpoint|propose|continue|ff" lib/workflow.js lib/generator.js scripts/test-workflow-runtime.js</automated>
  </verify>
  <done>
- The runtime has a reusable split-spec checkpoint facade with deterministic inline and escalated behavior.
- Checked-in command drift is intentionally bounded to 9 files until the refresh wave lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align skill and bilingual playbooks to the spec-split-before-design contract</name>
  <files>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
  </files>
  <read_first>
skills/opsx/SKILL.md
skills/opsx/references/action-playbooks.md
skills/opsx/references/action-playbooks-zh.md
lib/workflow.js
lib/generator.js
.planning/phases/05-spec-split-checkpoint/05-CONTEXT.md
.planning/phases/05-spec-split-checkpoint/05-RESEARCH.md
  </read_first>
  <action>Implement D-09 through D-13 in the distributed skill guidance. In `skills/opsx/SKILL.md`, update the checkpoint list and default execution loop so `spec-split-checkpoint` is explicitly listed after `specs` and before `design`, simple single-spec changes can be reviewed inline, and risky changes may require a read-only reviewer pass before design. State explicitly that the reviewer may read artifacts and report findings but must not write files directly or create `spec-review.md`. In both `skills/opsx/references/action-playbooks.md` and `skills/opsx/references/action-playbooks-zh.md`, update `Common setup`, `propose`, `continue`, and `ff` so they mention the split-spec checkpoint before design, the existing `spec checkpoint` still after design, checkpoint findings patch existing artifacts only, and no new public `$opsx-spec-split-*` or `/opsx-spec-split-*` route exists. Also state explicitly that TDD-light belongs to Phase 6 and verify/archive hard gates belong to Phase 7, so this phase does not add RED/GREEN/REFACTOR/VERIFY rules or hard archive/verify blocking.</action>
  <acceptance_criteria>
- `skills/opsx/SKILL.md` contains `spec-split-checkpoint` in the checkpoints section and default execution loop.
- Both playbook files contain `spec-split-checkpoint` and mention that the reviewer is read-only and must not create `spec-review.md`.
- Neither playbook introduces a new public `opsx-spec-split` route.
- The docs explicitly defer TDD-light to Phase 6 and hard verify/archive gates to Phase 7.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workflow-runtime</automated>
    <automated>rg -n "spec-split-checkpoint|spec-review.md|read-only reviewer|只读审阅|Phase 6|Phase 7" skills/opsx/SKILL.md skills/opsx/references/action-playbooks.md skills/opsx/references/action-playbooks-zh.md</automated>
  </verify>
  <done>
- Source-of-truth prompt generation and distributed skill docs now describe the same Phase 5 behavior.
- Phase 5 stays bounded to existing routes, existing artifacts, and later-phase deferrals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator findings -> workflow checkpoint result | incorrect shaping can break the canonical checkpoint contract or suggest illegal artifacts/routes |
| source-of-truth guidance -> generated/public surfaces | unbounded note changes can churn unrelated commands or reintroduce route drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | `runSpecSplitCheckpoint()` result shaping | mitigate | Reuse `buildCheckpointResult()` and lock PASS/BLOCK/WARN contract tests with the canonical required fields. |
| T-05-07 | Elevation of Scope | reviewer escalation guidance | mitigate | Encode escalation as read-only reviewer guidance with no new route and no `spec-review.md` artifact. |
| T-05-08 | Tampering | `lib/generator.js` planning note | mitigate | Scope the new text to `propose`, `continue`, and `ff` only and keep a temporary 9-file parity exemption until refresh plans land. |
</threat_model>

<verification>
- Run `npm run test:workflow-runtime`.
- Grep the workflow, generator, and skill docs for the exact split-spec checkpoint strings and reviewer constraints.
</verification>

<success_criteria>
- SPEC-03 and SPEC-04 are implemented in the workflow facade and source-of-truth guidance.
- The repo is ready for a bounded 9-file generated refresh wave instead of a repo-wide command rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-split-checkpoint/05-03-SUMMARY.md`
</output>
