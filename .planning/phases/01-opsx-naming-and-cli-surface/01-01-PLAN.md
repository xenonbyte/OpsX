---
phase: 01-opsx-naming-and-cli-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/opsx.js
  - bin/openspec.js
  - install.sh
  - uninstall.sh
  - scripts/postinstall.js
  - lib/constants.js
  - lib/cli.js
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - NAME-01
  - NAME-02
must_haves:
  truths:
    - "User can install `@xenonbyte/opsx` at version `3.0.0` and the published bin name is only `opsx`."
    - "Running `node bin/opsx.js --help` and `node bin/opsx.js --version` prints OpsX branding and the Phase 1 command surface."
    - "`opsx migrate` and `opsx status` are callable and return truthful Phase 1 output without claiming Phase 2 or Phase 4 behavior already exists."
  artifacts:
    - path: "package.json"
      provides: "OpsX npm package identity and bin mapping"
      contains: "\"name\": \"@xenonbyte/opsx\""
    - path: "bin/opsx.js"
      provides: "Published OpsX CLI wrapper"
      contains: "runCli(process.argv.slice(2))"
    - path: "lib/cli.js"
      provides: "OpsX help, version, and Phase 1 command dispatch"
      contains: "case 'migrate'"
    - path: "scripts/test-workflow-runtime.js"
      provides: "Regression coverage for renamed binary/help output"
      contains: "opsx"
  key_links:
    - from: "package.json"
      to: "bin/opsx.js"
      via: "bin map"
      pattern: "\"opsx\": \"bin/opsx.js\""
    - from: "bin/opsx.js"
      to: "lib/cli.js"
      via: "require('../lib/cli')"
      pattern: "require\\('\\.\\./lib/cli'\\)"
    - from: "lib/cli.js"
      to: "lib/install.js"
      via: "install/uninstall/check/doc/language dispatch"
      pattern: "require\\('\\./install'\\)"
---

<objective>
Deliver the public package and CLI identity rename for Phase 1.

Purpose: make `@xenonbyte/opsx` and `opsx` the only published npm/binary surface per D-02, D-03, D-05, and D-06 while keeping the existing install/check/doc/language behavior intact per D-08.
Output: updated package metadata, a new `bin/opsx.js` entrypoint, renamed shell/postinstall wrappers, OpsX-branded help/version text, and runtime tests updated for the renamed public surface.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-PATTERNS.md

<interfaces>
From `bin/openspec.js`:
```javascript
const { runCli } = require('../lib/cli');
runCli(process.argv.slice(2)).catch((error) => {
  const message = error && error.message ? error.message : String(error);
  console.error(message);
  process.exit(1);
});
```

From `lib/cli.js`:
```javascript
async function runCli(argv) { /* help/version/install/uninstall/check/doc/language dispatch */ }
module.exports = { runCli };
```

From `lib/install.js`:
```javascript
module.exports = {
  install,
  uninstall,
  runCheck,
  showDoc,
  setLanguage
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package metadata and published executable wrappers</name>
  <files>
package.json
bin/opsx.js
bin/openspec.js
install.sh
uninstall.sh
scripts/postinstall.js
  </files>
  <read_first>
package.json
bin/openspec.js
install.sh
uninstall.sh
scripts/postinstall.js
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Apply D-02, D-03, D-04, D-05, and D-06 exactly: set `package.json` `"name"` to `@xenonbyte/opsx`, `"version"` to `3.0.0`, `"description"` to `AI-native operational spec execution workflow for Claude, Codex, and Gemini`, and repository/bugs/homepage URLs to `xenonbyte/opsx`. Change the `bin` map to a single entry `"opsx": "bin/opsx.js"`, add `bin/opsx.js` by copying the current thin wrapper pattern, and remove `bin/openspec.js` instead of leaving an alias. Repoint `install.sh` and `uninstall.sh` to `bin/opsx.js`. Rewrite `scripts/postinstall.js` so the visible product name is `OpsX`, the recommended install command is `opsx install --platform claude,codex,gemini`, and the guidance does not mention `openspec`, `$openspec`, or `/prompts:openspec`. Do not add any compatibility shim, proxy binary, or duplicate `openspec` publish path.</action>
  <verify>
`node -e "const p=require('./package.json'); if (p.name !== '@xenonbyte/opsx') process.exit(1); if (p.version !== '3.0.0') process.exit(1); if (!p.bin || p.bin.opsx !== 'bin/opsx.js') process.exit(1); if (Object.prototype.hasOwnProperty.call(p.bin,'openspec')) process.exit(1);"`
`test -f bin/opsx.js`
`test ! -f bin/openspec.js`
`rg -n "@xenonbyte/opsx|bin/opsx.js|xenonbyte/opsx" package.json install.sh uninstall.sh scripts/postinstall.js`
  </verify>
  <done>
- `package.json` contains `"name": "@xenonbyte/opsx"`, `"version": "3.0.0"`, and only `"opsx": "bin/opsx.js"` under `bin`.
- `bin/opsx.js` exists and contains `runCli(process.argv.slice(2))`.
- `bin/openspec.js` no longer exists in the working tree.
- `install.sh` and `uninstall.sh` invoke `node "$SCRIPT_DIR/bin/opsx.js"`.
- `scripts/postinstall.js` contains `OpsX v${PACKAGE_VERSION}` and `opsx install --platform`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand CLI help/version and add truthful Phase 1 `migrate` and `status` commands</name>
  <files>
lib/constants.js
lib/cli.js
scripts/test-workflow-runtime.js
  </files>
  <read_first>
lib/constants.js
lib/cli.js
lib/install.js
lib/runtime-guidance.js
scripts/test-workflow-runtime.js
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
  </read_first>
  <action>In `lib/constants.js`, add explicit public identity constants for Phase 1 such as `PRODUCT_NAME = 'OpsX'`, `PRODUCT_SHORT_NAME = 'opsx'`, and `PRODUCT_LONG_NAME = 'Operational Spec eXecution'`, while keeping deferred workspace/home constants unchanged because Phase 2 owns `.opsx/` and `~/.opsx/` migration. In `lib/cli.js`, change all help, version, and unknown-command strings to `OpsX`/`opsx`; make the intended command surface match D-07 exactly by showing `opsx install`, `opsx uninstall`, `opsx check`, `opsx doc`, `opsx language`, `opsx migrate`, `opsx status`, `opsx --help`, and `opsx --version` as the primary UX; and keep `opsx --check`, `opsx --doc`, and `opsx --language` only as secondary compatibility aliases so D-08 remains true. Implement first-class `check`, `doc`, and `language` subcommand dispatch in addition to the legacy flags, and change the Codex guidance to `$opsx <request>` with no `/prompts:*` primary UX. Add `case 'migrate'` and `case 'status'` that exit successfully with explicit Phase 1 messaging: `migrate` must say the real workspace migration lands in Phase 2, and `status` must say durable change-state reporting lands in Phase 4 while showing only current package/version/phase notice. Update `scripts/test-workflow-runtime.js` to assert the renamed help/version strings, the new primary `opsx check|doc|language` surface, continued compatibility for `--check|--doc|--language`, and the placeholder outputs for `migrate` and `status`, but do not change fixture paths like `openspec/changes`, `.openspec.yaml`, or `.openspec` because those are deferred to Phase 2.</action>
  <verify>
`node scripts/test-workflow-runtime.js`
`node bin/opsx.js --version`
`node bin/opsx.js --help`
`node bin/opsx.js migrate`
`node bin/opsx.js status`
  </verify>
  <done>
- `node bin/opsx.js --version` prints exactly `OpsX v3.0.0`.
- `node bin/opsx.js --help` presents `opsx check`, `opsx doc`, and `opsx language <en|zh>` as the primary syntax, while `--check`, `--doc`, and `--language` appear at most as secondary compatibility aliases.
- `node bin/opsx.js --help` includes `opsx install --platform <claude|codex|gemini[,...]>`, `opsx migrate`, and `opsx status`.
- `node bin/opsx.js --help` does not include ``openspec``, ``$openspec``, or ``/prompts:openspec``.
- `node bin/opsx.js migrate` exits `0` and includes the phrase `Phase 2`.
- `node bin/opsx.js status` exits `0` and includes the phrase `Phase 4`.
- `node scripts/test-workflow-runtime.js` passes after the public-surface rename.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata -> published npm artifact | Wrong name/bin metadata would ship a stale or misleading install surface |
| CLI help/version -> operator expectations | Misleading output could imply old commands or later-phase behavior exists |

Auth/session/data threats: N/A for this plan because no authentication, session state, secrets, or user data flows are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `package.json`, `bin/*` | mitigate | Remove the `openspec` bin alias entirely and publish only `opsx` per D-06 so users cannot invoke a stale binary name accidentally. |
| T-01-02 | Tampering | `install.sh`, `uninstall.sh`, `scripts/postinstall.js` | mitigate | Point every wrapper and install message at `bin/opsx.js` and `@xenonbyte/opsx` so shell entrypoints cannot drift away from package metadata. |
| T-01-03 | Information Disclosure / Misleading Output | `lib/cli.js` | mitigate | Make `migrate` and `status` print explicit Phase 1 placeholders instead of claiming `.opsx` migration or state-machine support already exists. |
| T-01-04 | Elevation / auth/session/data | Phase 1 CLI rename | N/A | No auth, session, permission, or data boundary exists in this naming-only surface plan. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-runtime.js`.
- Run `node bin/opsx.js --version`.
- Run `node bin/opsx.js --help`.
- Run `node bin/opsx.js migrate`.
- Run `node bin/opsx.js status`.
</verification>

<success_criteria>
- `@xenonbyte/opsx` version `3.0.0` is the only package identity in `package.json`.
- `bin/opsx.js` is the only published CLI wrapper and `bin/openspec.js` is removed.
- Help/version/error output uses OpsX branding only.
- `migrate` and `status` are callable, truthful Phase 1 commands rather than missing or misleading routes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-01-SUMMARY.md`
</output>
