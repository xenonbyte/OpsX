---
phase: 01-opsx-naming-and-cli-surface
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - README.md
  - README-zh.md
  - docs/*.md
  - templates/project/*.tmpl
autonomous: true
requirements:
  - NAME-03
  - NAME-04
must_haves:
  truths:
    - "README, docs, and shipped project templates present OpsX as the primary package, CLI, skill, and command identity."
    - "Legacy tokens are removed from active docs/template guidance, except the single lineage sentence allowed in `README.md` and `README-zh.md`."
  artifacts:
    - path: "README.md"
      provides: "Primary OpsX install and usage guidance"
      contains: "@xenonbyte/opsx"
    - path: "docs/commands.md"
      provides: "OpsX command reference"
      contains: "opsx check"
    - path: "templates/project/config.yaml.tmpl"
      provides: "OpsX-first project template guidance"
      contains: "OpsX"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "install and version guidance"
      pattern: "@xenonbyte/opsx"
    - from: "docs/commands.md"
      to: "commands/claude/opsx.md"
      via: "shared public command surface"
      pattern: "opsx"
    - from: "templates/project/*.tmpl"
      to: "skills/opsx/"
      via: "generated project instructions"
      pattern: "opsx"
---

<objective>
Rewrite the README/docs/template guidance to the Phase 1 OpsX public surface.

Purpose: satisfy D-04, D-09, and D-11 by making public and templated guidance consistent with the renamed package, CLI, skill, and generated command assets.
Output: OpsX-first README files, rewritten docs, and rewritten `templates/project/*.tmpl` without active legacy public tokens.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README and docs to the OpsX public surface</name>
  <files>
README.md
README-zh.md
docs/*.md
  </files>
  <read_first>
README.md
README-zh.md
docs/commands.md
docs/codex.md
docs/customization.md
docs/runtime-guidance.md
docs/supported-tools.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
  </read_first>
  <action>Rewrite the user-facing README and `docs/*.md` surfaces so `OpsX`, `opsx`, and `@xenonbyte/opsx` are the primary terms everywhere per D-04 and D-09. Document `opsx install`, `opsx uninstall`, `opsx check`, `opsx doc`, `opsx language`, `opsx migrate`, `opsx status`, `opsx --help`, and `opsx --version` as the intended Phase 1 surface; if compatibility aliases `--check`, `--doc`, or `--language` appear at all, present them only as secondary aliases. Remove `$openspec`, `/openspec`, `/prompts:openspec`, `skills/openspec`, `@xenonbyte/openspec`, `openspec/config.yaml`, and `~/.openspec` from active how-to guidance. The only allowed lineage sentence is `OpsX was originally adapted from Fission-AI/OpenSpec.`, and it may appear only in `README.md` and `README-zh.md`.</action>
  <verify>
`rg -n "@xenonbyte/opsx|OpsX|opsx (install|uninstall|check|doc|language|migrate|status)" README.md README-zh.md docs`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" docs`
  </verify>
  <done>
- `README.md` and `README-zh.md` show `npm install -g @xenonbyte/opsx` and `opsx install --platform ...`.
- `docs/commands.md` presents `opsx check`, `opsx doc`, and `opsx language` as the primary syntax.
- No file under `docs/` contains the legacy public tokens scanned by the Phase 1 gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite shipped project templates to OpsX-only guidance</name>
  <files>
templates/project/*.tmpl
  </files>
  <read_first>
templates/project/change-metadata.yaml.tmpl
templates/project/config.yaml.tmpl
templates/project/rule-file.md.tmpl
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
  </read_first>
  <action>Rewrite `templates/project/*.tmpl` so newly generated project instructions prefer `OpsX`, `opsx`, and `@xenonbyte/opsx` only. Do not leave `openspec`, `.openspec`, `~/.openspec`, `openspec/config.yaml`, or `skills/openspec` in any shipped project template. If deferred Phase 2 migration context must be acknowledged, do it without naming legacy paths inside these templates.</action>
  <verify>
`rg -n "OpsX|@xenonbyte/opsx|opsx " templates/project`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" templates/project`
  </verify>
  <done>
- `templates/project/*.tmpl` use OpsX naming only.
- No shipped project template contains the legacy-name tokens scanned by the Phase 1 gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/templates -> operator behavior | Outdated instructions would tell users to install or invoke the wrong product surface |

Auth/session/data threats: N/A for this plan because it edits documentation and templates only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Information Disclosure / Misleading Output | README/docs/templates | mitigate | Rewrite public and templated guidance to OpsX-only active instructions, with the lineage sentence restricted to the README files only. |
| T-01-15 | Tampering | `templates/project/*.tmpl` | mitigate | Remove legacy public tokens from shipped templates so newly generated projects cannot reintroduce the old surface. |
| T-01-16 | Elevation / auth/session/data | docs/template rewrite | N/A | No auth, session, privilege, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `rg -n "@xenonbyte/opsx|OpsX|opsx (install|uninstall|check|doc|language|migrate|status)" README.md README-zh.md docs`.
- Run `! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" docs templates/project`.
</verification>

<success_criteria>
- Public docs and project templates all describe the OpsX surface as primary.
- Legacy tokens are removed from active docs/template guidance except the allowed README lineage sentence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-05-SUMMARY.md`
</output>
