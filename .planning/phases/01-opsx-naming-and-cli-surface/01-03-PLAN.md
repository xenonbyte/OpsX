---
phase: 01-opsx-naming-and-cli-surface
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/generator.js
  - lib/workflow.js
  - templates/commands/*.tmpl
  - commands/openspec.md
  - commands/claude/**
  - commands/codex/prompts/**
  - commands/gemini/**
autonomous: true
requirements:
  - NAME-03
must_haves:
  truths:
    - "Generated Claude, Codex, and Gemini command assets use `OpsX`, `/opsx-*`, and `$opsx <request>` as the public surface."
    - "Checked-in command bundles do not ship legacy `openspec` top-level entry assets."
    - "Phase 1 generated guidance stays honest about deferred `.opsx` migration, state, spec-split, and TDD-light behavior."
  artifacts:
    - path: "lib/generator.js"
      provides: "OpsX command-bundle generation rules"
      contains: "opsx"
    - path: "lib/workflow.js"
      provides: "Action syntax catalog for OpsX surfaces"
      contains: "opsx"
    - path: "commands/claude/opsx.md"
      provides: "Claude-facing OpsX entry"
      contains: "OpsX"
  key_links:
    - from: "lib/generator.js"
      to: "templates/commands/*.tmpl"
      via: "loadTemplate()"
      pattern: "templates', 'commands'"
    - from: "lib/workflow.js"
      to: "commands/claude/opsx/*.md"
      via: "action syntax generation"
      pattern: "opsx"
    - from: "templates/commands/*.tmpl"
      to: "commands/**"
      via: "regenerated checked-in assets"
      pattern: "OpsX"
---

<objective>
Rewrite command generation and checked-in command assets to the OpsX public surface only.

Purpose: satisfy D-09, D-13, and D-14 by aligning generator code, templates, and emitted command bundles with the OpsX naming surface now, while keeping deeper command semantics and state-machine behavior deferred to Phase 3+.
Output: updated `lib/generator.js` and `lib/workflow.js`, rewritten `templates/commands/*.tmpl`, removed legacy `commands/*/openspec*` entry assets, and regenerated OpsX command bundles.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-PATTERNS.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-01-PLAN.md

<interfaces>
From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) { /* returns generated files per platform */ }
module.exports = { buildPlatformBundle };
```

From `lib/workflow.js`:
```javascript
function getAllActions() { /* action catalog */ }
function getActionSyntax(platform, actionId) { /* platform-specific syntax */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite workflow syntax and command templates to OpsX naming</name>
  <files>
lib/generator.js
lib/workflow.js
templates/commands/*.tmpl
  </files>
  <read_first>
lib/generator.js
lib/workflow.js
templates/commands/action.md.tmpl
templates/commands/codex-entry.md.tmpl
templates/commands/index.md.tmpl
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Rewrite the public command-generation surface so emitted guidance uses `OpsX`, `/opsx-*`, and `$opsx <request>` per D-14. When command examples appear, show `opsx check`, `opsx doc`, and `opsx language` as the primary CLI syntax from D-07. Remove `/openspec`, `$openspec`, `/prompts:openspec`, and `skills/openspec` from the generated public text. Keep Phase 1 claims truthful per D-13: do not claim `.opsx/active.yaml`, `state.yaml`, `spec-split-checkpoint`, TDD-light, full `opsx migrate`, or full `opsx status` behavior already exist.</action>
  <verify>
`rg -n "OpsX|\\$opsx <request>|/opsx-|opsx check|opsx doc|opsx language" lib/generator.js lib/workflow.js templates/commands`
`! rg -n "/openspec|/prompts:openspec|\\$openspec|skills/openspec|@xenonbyte/openspec" templates/commands`
  </verify>
  <done>
- `lib/generator.js` and `lib/workflow.js` generate OpsX-first command syntax.
- `templates/commands/*.tmpl` no longer present legacy public entrypoint guidance.
- Template text stays phase-accurate about deferred functionality.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate checked-in command assets and delete legacy top-level entry files</name>
  <files>
commands/openspec.md
commands/claude/**
commands/codex/prompts/**
commands/gemini/**
  </files>
  <read_first>
commands/claude/opsx.md
commands/codex/prompts/opsx.md
commands/gemini/opsx.toml
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Regenerate the checked-in command assets from the updated generator/templates so all shipped command bundles match the OpsX public surface. Remove `commands/openspec.md`, `commands/claude/openspec.md`, `commands/codex/prompts/openspec.md`, and `commands/gemini/openspec.toml` rather than keeping dual entrypoints. Update `commands/claude/opsx.md`, all `commands/claude/opsx/*.md` action files, `commands/codex/prompts/opsx.md`, all `commands/codex/prompts/opsx-*.md` action files, `commands/gemini/opsx.toml`, and all `commands/gemini/opsx/*.toml` action files so the committed assets match the new template output exactly.</action>
  <verify>
`rg -n "OpsX|\\$opsx <request>|/opsx-|opsx check|opsx doc|opsx language" commands/claude commands/codex/prompts commands/gemini`
`! rg -n "OpenSpec|/openspec|/prompts:openspec|\\$openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" commands`
`test ! -e commands/openspec.md`
`test ! -e commands/claude/openspec.md`
`test ! -e commands/codex/prompts/openspec.md`
`test ! -e commands/gemini/openspec.toml`
  </verify>
  <done>
- No checked-in `commands/*/openspec*` top-level entry asset remains.
- `commands/claude/opsx.md`, `commands/codex/prompts/opsx.md`, and `commands/gemini/opsx.toml` use OpsX-first language.
- All regenerated action files align with the updated templates and do not overclaim later-phase behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/generator -> checked-in command assets | Drift here ships contradictory command guidance |
| checked-in command assets -> operator workflow entrypoints | Stale `openspec` routes would mislead users about the supported surface |

Auth/session/data threats: N/A for this plan because it rewrites command text and generated assets only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | `lib/generator.js`, `templates/commands/*.tmpl` | mitigate | Update generator code and templates in one plan so emitted command assets cannot drift from the source rules. |
| T-01-09 | Information Disclosure / Misleading Output | `commands/**` | mitigate | Remove legacy `openspec` entry files and regenerate all shipped command assets with OpsX-first text only. |
| T-01-10 | Elevation / auth/session/data | command asset rewrite | N/A | No auth, session, privilege, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `rg -n "OpsX|\\$opsx <request>|/opsx-|opsx check|opsx doc|opsx language" lib/generator.js lib/workflow.js templates/commands`.
- Run `rg -n "OpsX|\\$opsx <request>|/opsx-|opsx check|opsx doc|opsx language" commands/claude commands/codex/prompts commands/gemini`.
- Confirm the legacy `commands/*/openspec*` entry files are gone.
</verification>

<success_criteria>
- Generator code, templates, and checked-in command assets all speak the OpsX surface.
- No shipped command entry asset keeps `openspec` as the active top-level route.
- Phase 1 command text remains honest about deferred runtime/state features.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-03-SUMMARY.md`
</output>
