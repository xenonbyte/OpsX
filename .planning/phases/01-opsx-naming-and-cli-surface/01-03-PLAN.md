---
phase: 01-opsx-naming-and-cli-surface
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
  - README-zh.md
  - CHANGELOG.md
  - docs/commands.md
  - docs/codex.md
  - docs/customization.md
  - docs/runtime-guidance.md
  - docs/supported-tools.md
  - templates/project/change-metadata.yaml.tmpl
  - templates/project/config.yaml.tmpl
  - templates/project/rule-file.md.tmpl
  - scripts/check-phase1-legacy-allowlist.js
autonomous: true
requirements:
  - NAME-04
  - NAME-05
must_haves:
  truths:
    - "README, docs, templates, and release notes present OpsX as the primary product identity."
    - "Historical OpenSpec references remain only in explicit lineage, changelog, or deferred-internal allowlist contexts."
    - "Phase 1 has an executable grep gate that fails if legacy names reappear on active public surfaces."
  artifacts:
    - path: "README.md"
      provides: "Primary OpsX installation and usage guidance"
      contains: "@xenonbyte/opsx"
    - path: "CHANGELOG.md"
      provides: "Breaking `3.0.0` release note"
      contains: "3.0.0"
    - path: "scripts/check-phase1-legacy-allowlist.js"
      provides: "Reproducible legacy-name gate with explicit allowlist"
      contains: "allowlist"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "install/version/docs guidance"
      pattern: "@xenonbyte/opsx"
    - from: "scripts/check-phase1-legacy-allowlist.js"
      to: "README.md, docs/, commands/, skills/opsx/"
      via: "legacy-name scan"
      pattern: "OpenSpec\\|openspec"
    - from: "CHANGELOG.md"
      to: "Phase 1 release messaging"
      via: "3.0.0 entry"
      pattern: "3\\.0\\.0"
---

<objective>
Rewrite the public documentation and release metadata to the OpsX surface, then lock the Phase 1 legacy-name gate behind an explicit allowlist.

Purpose: satisfy D-04, D-09, D-10, D-11, D-15, and D-16 without broadening into Phase 2 migration work by documenting the new public identity and encoding which remaining `openspec` references are allowed because they are history or deferred runtime internals.
Output: OpsX-first README/docs/templates, a `3.0.0` breaking-release changelog entry, and a repeatable naming gate script that enforces the Phase 1 allowlist.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README and docs to the Phase 1 OpsX public surface</name>
  <files>
README.md
README-zh.md
docs/commands.md
docs/codex.md
docs/customization.md
docs/runtime-guidance.md
docs/supported-tools.md
  </files>
  <read_first>
README.md
README-zh.md
docs/commands.md
docs/codex.md
docs/customization.md
docs/runtime-guidance.md
docs/supported-tools.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Rewrite the public README and `docs/*.md` surfaces so `OpsX`, `opsx`, and `@xenonbyte/opsx` are the primary terms everywhere per D-04 and D-09. Document the working Phase 1 commands with the intended syntax from D-07: `opsx install`, `opsx uninstall`, `opsx check`, `opsx doc`, `opsx language`, `opsx migrate`, `opsx status`, `opsx --help`, and `opsx --version`. If compatibility aliases `opsx --check|--doc|--language` are mentioned at all, list them only as secondary aliases. Remove `$openspec`, `/openspec`, `/prompts:openspec`, `skills/openspec`, `@xenonbyte/openspec`, `openspec/config.yaml`, and `~/.openspec` from active how-to guidance. Keep the docs phase-accurate by saying migration/state behavior is deferred, but do not preserve legacy path explanations inside shipped docs. The only allowed lineage sentence in user-facing docs is `OpsX was originally adapted from Fission-AI/OpenSpec.`, and it may appear only in `README.md` and `README-zh.md`.</action>
  <verify>
`rg -n "@xenonbyte/opsx|OpsX|opsx (install|uninstall|check|doc|language|migrate|status)" README.md README-zh.md docs`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" docs`
  </verify>
  <done>
- `README.md` and `README-zh.md` show `npm install -g @xenonbyte/opsx` and `opsx install --platform ...`.
- `docs/commands.md` presents `opsx check`, `opsx doc`, and `opsx language` as the primary syntax and does not present `/openspec`, `$openspec`, or `/prompts:openspec` as primary workflow entrypoints.
- `docs/codex.md` prefers `$opsx <request>` instead of `$openspec <request>`.
- No file under `docs/` contains the legacy-name tokens scanned by the Phase 1 gate.
- `README.md` and `README-zh.md` contain at most the exact lineage sentence `OpsX was originally adapted from Fission-AI/OpenSpec.` as their only remaining legacy reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite shipped project templates to OpsX-only guidance</name>
  <files>
templates/project/change-metadata.yaml.tmpl
templates/project/config.yaml.tmpl
templates/project/rule-file.md.tmpl
  </files>
  <read_first>
templates/project/change-metadata.yaml.tmpl
templates/project/config.yaml.tmpl
templates/project/rule-file.md.tmpl
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Rewrite the shipped project templates so new generated instructions prefer `OpsX`, `opsx`, and `@xenonbyte/opsx` only. Do not leave `openspec`, `.openspec`, `~/.openspec`, `openspec/config.yaml`, or `skills/openspec` in any shipped template. If Phase 2 migration context must be acknowledged, do it without naming legacy paths inside these templates; reserve any temporary internal-path explanation for gate-allowed internal files such as `AGENTS.md`, not `templates/project/`.</action>
  <verify>
`rg -n "OpsX|@xenonbyte/opsx|opsx " templates/project`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" templates/project`
  </verify>
  <done>
- `templates/project/rule-file.md.tmpl`, `config.yaml.tmpl`, and `change-metadata.yaml.tmpl` use OpsX naming only.
- No file under `templates/project/` contains the legacy-name tokens scanned by the Phase 1 gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the `3.0.0` release note and encode the executable Phase 1 legacy-name gate</name>
  <files>
CHANGELOG.md
scripts/check-phase1-legacy-allowlist.js
  </files>
  <read_first>
CHANGELOG.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
  </read_first>
  <action>Add a new `3.0.0` changelog entry that explicitly says this release renames the package to `@xenonbyte/opsx`, renames the CLI to `opsx`, renames the distributed skill bundle to `skills/opsx`, removes the `openspec` binary alias per D-06, and defers real workspace migration plus state-machine behavior to later phases instead of claiming they shipped here. Create `scripts/check-phase1-legacy-allowlist.js` as the reproducible Phase 1 grep gate. The script must scan only the shipped/runtime surfaces that matter for Phase 1 release readiness: `README.md`, `README-zh.md`, `CHANGELOG.md`, `package.json`, `bin/`, `lib/`, `scripts/` except `scripts/check-phase1-legacy-allowlist.js`, `commands/`, `skills/opsx/`, `templates/`, `docs/`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, and `AGENTS.md`. Hardcode the allowlist policy rather than deriving it from gitignore or comments: allow deferred-internal/runtime files `lib/runtime-guidance.js`, `scripts/test-workflow-runtime.js`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, and `AGENTS.md`; allow historical lineage in `CHANGELOG.md`; allow exactly one source-lineage sentence in `README.md` and `README-zh.md`; reject any remaining hit inside `package.json`, `bin/`, `lib/cli.js`, `commands/`, `skills/opsx/`, `docs/`, or `templates/`. By construction, the gate must not scan `.planning/` artifacts or the gate script itself. Keep the script output operator-friendly by printing unexpected matches with file and line context.</action>
  <verify>
`node scripts/check-phase1-legacy-allowlist.js`
`npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run`
`rg -n "3\\.0\\.0|@xenonbyte/opsx|opsx|skills/opsx" CHANGELOG.md package.json`
  </verify>
  <done>
- `CHANGELOG.md` contains a `3.0.0` entry that mentions `@xenonbyte/opsx`, `opsx`, `skills/opsx`, and no `openspec` binary alias.
- `scripts/check-phase1-legacy-allowlist.js` contains an explicit allowlist including `lib/runtime-guidance.js`, `scripts/test-workflow-runtime.js`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, `AGENTS.md`, `CHANGELOG.md`, `README.md`, and `README-zh.md`.
- Running `node scripts/check-phase1-legacy-allowlist.js` passes only when legacy references are limited to the explicit allowlist, the exact README lineage sentence, and no forbidden shipped/docs/template surface reintroduces them.
- The gate scans only the Phase 1 shipped/runtime surfaces and excludes `.planning/` plus `scripts/check-phase1-legacy-allowlist.js` itself by construction.
- `npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run` completes without shipping contradictory OpenSpec-vs-OpsX naming on the public package surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/templates -> operator decisions | Outdated instructions can cause users to install or invoke the wrong package or command family |
| legacy-name gate -> release readiness | A weak allowlist can let active `openspec` references slip back into public surfaces |

Auth/session/data threats: N/A for this plan because it edits documentation, release metadata, and a local verification script only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure / Misleading Output | README/docs/templates | mitigate | Rewrite public docs to use OpsX-first installation and command guidance, and explicitly label deferred `openspec/` internals instead of presenting them as recommended setup. |
| T-01-10 | Tampering | `scripts/check-phase1-legacy-allowlist.js` | mitigate | Hardcode the allowlist so only history, lineage, and deferred runtime internals are permitted; reject active legacy references in docs/assets/templates. |
| T-01-11 | Repudiation | `CHANGELOG.md` | mitigate | Add a `3.0.0` entry that states exactly what Phase 1 renamed and what Phase 2/4 work remains deferred. |
| T-01-12 | Elevation / auth/session/data | documentation and gate script | N/A | No auth, session, permission, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `node scripts/check-phase1-legacy-allowlist.js`.
- Run `npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run`.
- Spot-check README/docs/template outputs for OpsX-first language and the single allowed lineage sentence.
</verification>

<success_criteria>
- README/docs/templates describe OpsX as the primary product surface and stay honest about deferred Phase 2/4 behavior.
- Historical or internal `openspec` references are restricted to the explicit allowlist.
- `CHANGELOG.md` communicates `3.0.0` as a breaking rename and workflow-state-upgrade milestone without overclaiming unfinished phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-03-SUMMARY.md`
</output>
