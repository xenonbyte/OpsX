---
phase: 01-opsx-naming-and-cli-surface
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - CHANGELOG.md
  - scripts/check-phase1-legacy-allowlist.js
autonomous: true
requirements:
  - NAME-04
  - NAME-05
must_haves:
  truths:
    - "The `3.0.0` changelog entry explicitly communicates the breaking OpsX rename and deferred follow-up phases."
    - "An executable Phase 1 allowlist gate fails if legacy public tokens reappear on shipped/runtime surfaces."
    - "Final package and regression gates pass on the renamed public surface."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Breaking `3.0.0` release note"
      contains: "3.0.0"
    - path: "scripts/check-phase1-legacy-allowlist.js"
      provides: "Executable allowlist gate for legacy-name scans"
      contains: "allowlist"
  key_links:
    - from: "scripts/check-phase1-legacy-allowlist.js"
      to: "README.md, README-zh.md, docs/, commands/, skills/opsx/, templates/"
      via: "explicit scan target list"
      pattern: "allowlist"
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "3.0.0 rename messaging"
      pattern: "@xenonbyte/opsx"
---

<objective>
Finish Phase 1 release messaging and enforce the final legacy-name/package gates.

Purpose: satisfy D-10, D-15, and D-16 by turning the rename policy into a repeatable allowlist gate and by documenting `3.0.0` as the breaking OpsX release without overclaiming later-phase work.
Output: a `3.0.0` changelog entry plus `scripts/check-phase1-legacy-allowlist.js` that gates shipped/runtime surfaces and final Phase 1 verification.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-04-PLAN.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `3.0.0` breaking-release note for the OpsX rename</name>
  <files>
CHANGELOG.md
  </files>
  <read_first>
CHANGELOG.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Add a new `3.0.0` changelog entry that explicitly states the package rename to `@xenonbyte/opsx`, the CLI rename to `opsx`, the distributed skill rename to `skills/opsx`, and the removal of any `openspec` binary alias per D-06. Keep the message phase-accurate: call out that real workspace migration belongs to Phase 2 and durable state-machine behavior belongs to later phases instead of claiming those features already shipped here.</action>
  <verify>
`rg -n "3\\.0\\.0|@xenonbyte/opsx|skills/opsx|opsx" CHANGELOG.md`
  </verify>
  <done>
- `CHANGELOG.md` contains a `3.0.0` entry describing the breaking OpsX rename.
- The changelog does not imply that Phase 2 or Phase 4 behaviors were completed in Phase 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Encode the explicit allowlist gate and run the final Phase 1 verification bundle</name>
  <files>
scripts/check-phase1-legacy-allowlist.js
  </files>
  <read_first>
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
  </read_first>
  <action>Create `scripts/check-phase1-legacy-allowlist.js` as the reproducible Phase 1 legacy-name gate. Hardcode the scan target list rather than deriving it from gitignore or comments: `README.md`, `README-zh.md`, `CHANGELOG.md`, `package.json`, `bin/`, `lib/`, `scripts/` except `scripts/check-phase1-legacy-allowlist.js`, `commands/`, `skills/opsx/`, `templates/`, `docs/`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, and `AGENTS.md`. Allow deferred-internal/runtime references only in `lib/config.js`, `lib/constants.js`, `lib/install.js`, `lib/runtime-guidance.js`, `scripts/test-workflow-runtime.js`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, and `AGENTS.md`, because Phase 2 owns the actual `.opsx/` workspace/global-directory migration; allow history in `CHANGELOG.md`; allow exactly the lineage sentence `OpsX was originally adapted from Fission-AI/OpenSpec.` in `README.md` and `README-zh.md`; reject any remaining hit inside `package.json`, `bin/`, `lib/cli.js`, `commands/`, `skills/opsx/`, `docs/`, or `templates/`. By construction, the gate must not scan `.planning/` artifacts or the allowlist script itself. Print unexpected matches with file and line context, then run the final verification bundle from D-15: `node scripts/test-workflow-runtime.js`, `node scripts/check-phase1-legacy-allowlist.js`, and `npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run`.</action>
  <verify>
`node scripts/test-workflow-runtime.js`
`node scripts/check-phase1-legacy-allowlist.js`
`npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run`
  </verify>
  <done>
- `scripts/check-phase1-legacy-allowlist.js` scans only the intended shipped/runtime surfaces and excludes `.planning/` plus the gate script itself.
- The allowlist is explicit and limited to approved deferred-internal files (`lib/config.js`, `lib/constants.js`, `lib/install.js`, `lib/runtime-guidance.js`, `scripts/test-workflow-runtime.js`, `openspec/config.yaml`, `schemas/spec-driven/schema.json`, `AGENTS.md`), `CHANGELOG.md`, and the single README lineage sentence.
- Final runtime, grep, and package dry-run gates all pass on the renamed public surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy-name gate -> release readiness | A weak scan policy would allow stale OpenSpec naming to ship |
| changelog -> operator expectations | Overclaiming later-phase behavior would mislead upgrade decisions |

Auth/session/data threats: N/A for this plan because it edits release notes and a local verification script only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Information Disclosure / Misleading Output | `CHANGELOG.md` | mitigate | Describe the `3.0.0` rename accurately and explicitly defer migration/state-machine work to later phases. |
| T-01-18 | Tampering | `scripts/check-phase1-legacy-allowlist.js` | mitigate | Hardcode the scan surface and allowlist so forbidden public legacy references fail with actionable output. |
| T-01-19 | Repudiation | final verification bundle | mitigate | Run runtime tests, the allowlist script, and `npm pack --dry-run` together so release readiness is reproducible. |
| T-01-20 | Elevation / auth/session/data | changelog/gate work | N/A | No auth, session, privilege, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-runtime.js`.
- Run `node scripts/check-phase1-legacy-allowlist.js`.
- Run `npm_config_cache=/tmp/opsx-npm-cache npm pack --dry-run`.
</verification>

<success_criteria>
- The changelog frames `3.0.0` as the breaking OpsX rename release.
- The allowlist gate rejects unexpected legacy public tokens and ignores `.planning/` plus the gate script itself.
- Final Phase 1 verification covers runtime, search, and package surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-06-SUMMARY.md`
</output>
