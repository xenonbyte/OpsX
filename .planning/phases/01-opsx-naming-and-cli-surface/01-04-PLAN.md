---
phase: 01-opsx-naming-and-cli-surface
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - NAME-02
  - NAME-03
must_haves:
  truths:
    - "The runtime regression suite fails if OpsX help/check/doc/language, skill-bundle install targets, or generated command bundles drift back to legacy public naming."
    - "Phase 2 fixture paths such as `openspec/changes` and `.openspec.yaml` remain explicitly covered as deferred internals instead of being renamed by accident."
  artifacts:
    - path: "scripts/test-workflow-runtime.js"
      provides: "Regression coverage for renamed CLI, skill bundle, and generated assets"
      contains: "opsx"
  key_links:
    - from: "scripts/test-workflow-runtime.js"
      to: "lib/install.js"
      via: "check/doc/install assertions"
      pattern: "OpsX Installation Check"
    - from: "scripts/test-workflow-runtime.js"
      to: "commands/claude/opsx.md"
      via: "generated bundle assertions"
      pattern: "opsx"
---

<objective>
Extend the runtime regression suite to cover the split Phase 1 naming surfaces.

Purpose: lock the renamed CLI, shipped skill bundle, and generated command assets behind one fast automated check after the split plans land.
Output: an updated `scripts/test-workflow-runtime.js` that asserts OpsX public surfaces while preserving deferred Phase 2 fixtures.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-02-PLAN.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `scripts/test-workflow-runtime.js` for the renamed skill and command bundle surfaces</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
  </read_first>
  <action>After 01-01, 01-02, and 01-03 land, update `scripts/test-workflow-runtime.js` so it asserts the renamed public surfaces without tolerating both identities. Keep the existing help/version and compatibility coverage from 01-01, then add explicit assertions for `skills/opsx` install targets, `OpsX Installation Check`, OpsX-branded generated bundle content, and the absence of checked-in `commands/*/openspec*` entry files. Preserve the current Phase 2 fixture roots and deferred-internal path assertions for `openspec/changes`, `.openspec.yaml`, and shared-home behavior; this plan must guard those fixtures against accidental early migration rather than renaming them.</action>
  <verify>
`node scripts/test-workflow-runtime.js`
  </verify>
  <done>
- `node scripts/test-workflow-runtime.js` fails if the CLI, skill bundle, or generated command assets regress to legacy public naming.
- The same test suite still exercises deferred internal path fixtures without renaming them prematurely.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime regression suite -> release confidence | Missing assertions would let stale public naming ship unnoticed |

Auth/session/data threats: N/A for this plan because it modifies test coverage only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Repudiation | `scripts/test-workflow-runtime.js` | mitigate | Add assertions for renamed public surfaces and removed legacy assets so future regressions are caught automatically. |
| T-01-12 | Tampering | deferred path fixtures | mitigate | Keep `openspec/changes` and `.openspec.yaml` fixture assertions intact so later plans cannot silently front-run Phase 2 migration scope. |
| T-01-13 | Elevation / auth/session/data | regression suite | N/A | No auth, session, privilege, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-runtime.js`.
</verification>

<success_criteria>
- One automated runtime script covers the split Phase 1 rename surfaces end to end.
- Deferred Phase 2 path fixtures remain intentionally unchanged and verified as such.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-04-SUMMARY.md`
</output>
