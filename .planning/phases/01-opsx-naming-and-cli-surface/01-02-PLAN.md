---
phase: 01-opsx-naming-and-cli-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/config.js
  - lib/install.js
  - lib/generator.js
  - lib/workflow.js
  - templates/commands/*.tmpl
  - commands/openspec.md
  - commands/claude/openspec.md
  - commands/claude/opsx.md
  - commands/claude/opsx/*.md
  - commands/codex/prompts/openspec.md
  - commands/codex/prompts/opsx.md
  - commands/codex/prompts/opsx-*.md
  - commands/gemini/openspec.toml
  - commands/gemini/opsx.toml
  - commands/gemini/opsx/*.toml
  - skills/opsx/SKILL.md
  - skills/opsx/GUIDE-en.md
  - skills/opsx/GUIDE-zh.md
  - skills/opsx/references/*.md
  - skills/openspec/SKILL.md
  - skills/openspec/GUIDE-en.md
  - skills/openspec/GUIDE-zh.md
  - skills/openspec/references/*.md
  - scripts/test-workflow-runtime.js
autonomous: true
requirements:
  - NAME-03
must_haves:
  truths:
    - "Installed guides and command bundles use `OpsX`, `opsx`, and `skills/opsx` as the public identity."
    - "Generated command files no longer ship `openspec` entry files or present `$openspec` and `/openspec` as primary routes."
    - "Phase 1 keeps internal workspace/runtime semantics deferred without pretending `.opsx` workflow-state behavior already exists."
  artifacts:
    - path: "lib/install.js"
      provides: "Installed skill and command bundle paths for OpsX"
      contains: "skills', 'opsx'"
    - path: "lib/generator.js"
      provides: "Generated OpsX command bundle definitions"
      contains: "opsx"
    - path: "skills/opsx/SKILL.md"
      provides: "Distributed skill identity renamed to OpsX"
      contains: "name: opsx"
    - path: "commands/claude/opsx.md"
      provides: "Claude-facing OpsX index"
      contains: "OpsX"
  key_links:
    - from: "lib/config.js"
      to: "skills/opsx/"
      via: "getRepoSkillDir()"
      pattern: "skills', 'opsx'"
    - from: "lib/install.js"
      to: "commands/"
      via: "buildPlatformBundle(platform)"
      pattern: "buildPlatformBundle\\(platform\\)"
    - from: "lib/generator.js"
      to: "templates/commands/"
      via: "loadTemplate()"
      pattern: "templates', 'commands'"
---

<objective>
Rename the installed skill bundle and generated command assets to the OpsX public identity.

Purpose: satisfy D-09, D-12, D-13, and D-14 by making shipped skills/commands coherent with `opsx` now, while explicitly keeping workspace migration and state-machine semantics deferred to later phases.
Output: renamed repo skill bundle under `skills/opsx`, updated install/config plumbing, regenerated command assets/templates with OpsX branding, and regression coverage for install/check/doc/generated bundles.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-PATTERNS.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-01-PLAN.md

<interfaces>
From `lib/config.js`:
```javascript
function getSharedHome(homeDir = process.env.HOME) { return path.join(homeDir, SHARED_HOME_NAME); }
function getGlobalConfigPath(homeDir = process.env.HOME) { return path.join(getSharedHome(homeDir), GLOBAL_CONFIG_NAME); }
function getRepoSkillDir() { return path.join(REPO_ROOT, 'skills', 'openspec'); }
```

From `lib/install.js`:
```javascript
function installPlatform(platform, options) { /* copies skill bundle, writes manifests, writes generated commands */ }
function runCheck(options = {}) { /* prints installation check report */ }
function showDoc(options = {}) { /* reads GUIDE-en.md or GUIDE-zh.md */ }
```

From `lib/generator.js`:
```javascript
function buildPlatformBundle(platform) { /* returns generated files per platform */ }
module.exports = { buildPlatformBundle };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the distributed skill entry bundle to `skills/opsx` and repoint install/config plumbing</name>
  <files>
lib/config.js
lib/install.js
skills/opsx/SKILL.md
skills/opsx/GUIDE-en.md
skills/opsx/GUIDE-zh.md
skills/openspec/SKILL.md
skills/openspec/GUIDE-en.md
skills/openspec/GUIDE-zh.md
  </files>
  <read_first>
lib/config.js
lib/install.js
skills/openspec/SKILL.md
skills/openspec/GUIDE-en.md
skills/openspec/GUIDE-zh.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
  </read_first>
  <action>Apply D-12 and D-13 without broadening into Phase 2: move the repo skill entry bundle from `skills/openspec/` to `skills/opsx/`, update `getRepoSkillDir()` to return `skills/opsx`, change install copy targets and installed guide lookup paths in `lib/install.js` from `skills/openspec` to `skills/opsx`, and change the check heading to `## OpsX Installation Check`. Rewrite `skills/opsx/SKILL.md`, `GUIDE-en.md`, and `GUIDE-zh.md` so the visible product name is `OpsX`, the public entrypoints are `opsx`, `$opsx <request>`, and `/opsx-*`, and no `openspec`, `$openspec`, `/openspec`, `/prompts:openspec`, or `skills/openspec` guidance remains in the shipped bundle. Keep `SHARED_HOME_NAME = '.openspec'`, `openspec/config.yaml`, `.openspec.yaml`, and `~/.openspec/.opsx-config.yaml` runtime semantics untouched in code because Phase 2 owns that migration, but do not explain those deferred internals inside the shipped skill bundle. Remove the published `skills/openspec/` entry files instead of shipping duplicate identities.</action>
  <verify>
`rg -n "skills/opsx|name: opsx|OpsX Installation Check" lib/config.js lib/install.js skills/opsx`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" skills/opsx`
`test ! -f skills/openspec/SKILL.md`
  </verify>
  <done>
- `lib/config.js` returns `skills/opsx` from `getRepoSkillDir()`.
- `lib/install.js` copies and reads guides from `skills/opsx`, and `runCheck()` starts with `## OpsX Installation Check`.
- `skills/opsx/SKILL.md` contains `name: opsx` and no legacy public entrypoint guidance.
- The shipped skill entry files under `skills/openspec/` are removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate command templates and checked-in command assets with OpsX-only public entrypoints</name>
  <files>
lib/generator.js
lib/workflow.js
templates/commands/*.tmpl
commands/openspec.md
commands/claude/openspec.md
commands/claude/opsx.md
commands/claude/opsx/*.md
commands/codex/prompts/openspec.md
commands/codex/prompts/opsx.md
commands/codex/prompts/opsx-*.md
commands/gemini/openspec.toml
commands/gemini/opsx.toml
commands/gemini/opsx/*.toml
  </files>
  <read_first>
lib/generator.js
lib/workflow.js
templates/commands/action.md.tmpl
templates/commands/codex-entry.md.tmpl
templates/commands/gemini-action.toml.tmpl
templates/commands/gemini-index.toml.tmpl
templates/commands/index.md.tmpl
templates/commands/shared-entry.md.tmpl
commands/claude/opsx.md
commands/codex/prompts/opsx.md
commands/gemini/opsx.toml
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Rewrite generator output so the shipped command bundle is OpsX-only on the public surface. Update `lib/workflow.js`, `lib/generator.js`, and every file under `templates/commands/*.tmpl` so generated files talk about `OpsX`, not `OpenSpec`; the public entrypoints are `/opsx-<action>` for Claude/Gemini actions and `$opsx <request>` for Codex natural-language entry; and `opsx check`, `opsx doc`, and `opsx language` are the primary command spellings when command examples appear. Remove the legacy generated entry files `commands/openspec.md`, `commands/claude/openspec.md`, `commands/codex/prompts/openspec.md`, and `commands/gemini/openspec.toml` instead of keeping dual entrypoints. Regenerate `commands/claude/opsx.md`, every existing `commands/claude/opsx/*.md` action file, `commands/codex/prompts/opsx.md`, every existing `commands/codex/prompts/opsx-*.md` action file, `commands/gemini/opsx.toml`, and every existing `commands/gemini/opsx/*.toml` action file so the checked-in assets match the updated templates. Keep the action text phase-accurate: do not claim `.opsx/active.yaml`, `state.yaml`, `spec-split-checkpoint`, TDD-light, archive gates, or full `opsx migrate` behavior already exist in Phase 1.</action>
  <verify>
`rg -n "Primary workflow entry: \\$opsx <request>|Use the opsx skill|OpsX|opsx check|opsx doc|opsx language" commands templates/commands lib/generator.js lib/workflow.js`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" commands templates/commands`
`test ! -f commands/openspec.md`
`test ! -f commands/claude/openspec.md`
`test ! -f commands/codex/prompts/openspec.md`
`test ! -f commands/gemini/openspec.toml`
  </verify>
  <done>
- `lib/generator.js` and `lib/workflow.js` no longer generate `openspec` top-level entry files.
- `commands/openspec.md`, `commands/claude/openspec.md`, `commands/codex/prompts/openspec.md`, and `commands/gemini/openspec.toml` are removed.
- `commands/claude/opsx.md`, `commands/codex/prompts/opsx.md`, and `commands/gemini/opsx.toml` use `OpsX` branding and no primary `/openspec`, `$openspec`, or `/prompts:openspec` guidance.
- Generated action files under `commands/claude/opsx/`, `commands/codex/prompts/`, and `commands/gemini/opsx/` are regenerated from the updated templates and stay phase-accurate about deferred functionality.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend runtime regression checks for the renamed skill and generated bundle surfaces</name>
  <files>
scripts/test-workflow-runtime.js
  </files>
  <read_first>
scripts/test-workflow-runtime.js
.planning/phases/01-opsx-naming-and-cli-surface/01-VALIDATION.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
  </read_first>
  <action>Update `scripts/test-workflow-runtime.js` after Tasks 1 and 2 land so the existing smoke suite now asserts `skills/opsx` install targets, `OpsX Installation Check`, OpsX-branded generated bundle content, the absence of shipped `commands/*/openspec*` entry files, and the renamed `opsx` public routes. Keep the current fixture roots and runtime-path assertions for `openspec/changes`, `.openspec.yaml`, and shared-home behavior intact because those remain Phase 2 concerns. The runtime script should verify the renamed surfaces without silently tolerating both identities.</action>
  <verify>
`node scripts/test-workflow-runtime.js`
  </verify>
  <done>
- `node scripts/test-workflow-runtime.js` passes with explicit assertions for `skills/opsx`, OpsX-branded generated assets, and removed legacy entry files.
- The test suite still exercises the deferred runtime-path fixtures without renaming them prematurely.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo skill bundle -> installed home directories | Wrong skill path or stale bundle contents would install contradictory public guidance |
| templates/generator -> checked-in command assets | Generated outputs can drift and ship stale `openspec` routes if not regenerated coherently |

Auth/session/data threats: N/A for this plan because it renames install assets and generated docs only; no authentication, session, or user-data behavior is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `lib/install.js`, `lib/config.js` | mitigate | Move every installed skill path from `skills/openspec` to `skills/opsx` in one pass and delete the old bundle so users cannot install mixed identities. |
| T-01-06 | Repudiation / Stale Manifests | installed command bundle | mitigate | Keep manifest writing and cleanup behavior intact while updating recorded paths, then cover the new paths in `scripts/test-workflow-runtime.js`. |
| T-01-07 | Information Disclosure / Misleading Output | generated command files and skill text | mitigate | Rewrite templates and generated assets so public guidance uses OpsX-only entrypoints and explicitly avoids claiming later-phase `.opsx` state behavior exists now. |
| T-01-08 | Elevation / auth/session/data | Phase 1 bundle rename | N/A | No auth, session, privilege, or user-data boundary is present in these install/generator changes. |
</threat_model>

<verification>
- Run `node scripts/test-workflow-runtime.js`.
- Confirm `skills/openspec/` and legacy top-level `openspec` command assets are removed.
- Confirm generated command text uses `OpsX`, `opsx`, and `opsx skill`.
</verification>

<success_criteria>
- Installed guides and checked-in command assets use `skills/opsx` and OpsX branding.
- No shipped command bundle keeps `openspec` as a primary public entry file.
- The plan preserves deferred Phase 2 path semantics instead of partially migrating workspace/runtime behavior early.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-02-SUMMARY.md`
</output>
