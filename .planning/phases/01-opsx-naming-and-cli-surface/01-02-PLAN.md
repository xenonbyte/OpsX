---
phase: 01-opsx-naming-and-cli-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/config.js
  - lib/install.js
  - skills/opsx/**
  - skills/openspec/**
autonomous: true
requirements:
  - NAME-03
must_haves:
  truths:
    - "Installed skill guides and copied assets use `skills/opsx` with OpsX branding as the only shipped identity."
    - "`opsx check` and `opsx doc` resolve the renamed skill bundle without silently changing Phase 2 workspace-path semantics."
  artifacts:
    - path: "lib/config.js"
      provides: "Repo skill bundle lookup for OpsX"
      contains: "skills', 'opsx'"
    - path: "lib/install.js"
      provides: "Install/check/doc plumbing pointed at `skills/opsx`"
      contains: "OpsX Installation Check"
    - path: "skills/opsx/SKILL.md"
      provides: "Distributed OpsX skill entry bundle"
      contains: "name: opsx"
  key_links:
    - from: "lib/config.js"
      to: "skills/opsx/"
      via: "getRepoSkillDir()"
      pattern: "skills', 'opsx'"
    - from: "lib/install.js"
      to: "skills/opsx/"
      via: "installPlatform(), runCheck(), showDoc()"
      pattern: "skills/opsx"
---

<objective>
Move the shipped skill bundle to `skills/opsx` and repoint install/config plumbing to that bundle only.

Purpose: satisfy D-09, D-12, D-13, and D-14 by making the distributed skill identity coherent with OpsX now, while explicitly leaving `.opsx/` workspace migration and durable state semantics for later phases.
Output: renamed skill bundle content under `skills/opsx`, removed shipped `skills/openspec` entry files, and `lib/config.js` / `lib/install.js` pointed at the new bundle.
</objective>

<execution_context>
@/Users/xubo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xubo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-PATTERNS.md
@.planning/phases/01-opsx-naming-and-cli-surface/01-01-PLAN.md

<interfaces>
From `lib/config.js`:
```javascript
function getRepoSkillDir() { return path.join(REPO_ROOT, 'skills', 'openspec'); }
```

From `lib/install.js`:
```javascript
function installPlatform(platform, options) { /* copies skill bundle and generated commands */ }
function runCheck(options = {}) { /* prints installation report */ }
function showDoc(options = {}) { /* reads GUIDE-en.md or GUIDE-zh.md */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the distributed skill bundle as `skills/opsx` only</name>
  <files>
skills/opsx/**
skills/openspec/**
  </files>
  <read_first>
skills/openspec/SKILL.md
skills/openspec/GUIDE-en.md
skills/openspec/GUIDE-zh.md
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Apply D-09, D-12, D-13, and D-14 exactly. Move the shipped skill entry bundle from `skills/openspec/` to `skills/opsx/`, including the guides and reference files needed by the distributed bundle. Rewrite the visible guidance so the public entrypoints are `opsx`, `$opsx <request>`, and `/opsx-*`; remove `openspec`, `$openspec`, `/openspec`, `/prompts:openspec`, `skills/openspec`, and `@xenonbyte/openspec` from the shipped skill bundle. Keep Phase 2 internals out of this task: do not rewrite runtime fixtures, `.openspec` shared-home semantics, or workspace migration behavior here. Remove the shipped `skills/openspec/` entry files instead of leaving duplicate public identities.</action>
  <verify>
`rg -n "name: opsx|OpsX|\\$opsx|/opsx-" skills/opsx`
`! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" skills/opsx`
`test ! -e skills/openspec/SKILL.md`
  </verify>
  <done>
- `skills/opsx/SKILL.md` contains `name: opsx`.
- The shipped guides under `skills/opsx/` use OpsX-only public entrypoints.
- The old shipped `skills/openspec/` entry files are removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repoint repo skill lookup and install/check/doc plumbing to `skills/opsx`</name>
  <files>
lib/config.js
lib/install.js
  </files>
  <read_first>
lib/config.js
lib/install.js
.planning/phases/01-opsx-naming-and-cli-surface/01-CONTEXT.md
.planning/phases/01-opsx-naming-and-cli-surface/01-RESEARCH.md
  </read_first>
  <action>Update `lib/config.js` and `lib/install.js` so the repo skill directory, installed guide lookup, copied asset paths, and installation-check headings point at `skills/opsx` per D-12. `getRepoSkillDir()` must return `skills/opsx`; `runCheck()` must print `## OpsX Installation Check`; guide reads and copied skill assets must come from `skills/opsx`. Preserve the deferred runtime semantics from D-13: keep `.openspec` shared-home paths, `openspec/config.yaml`, `.openspec.yaml`, and existing Phase 2 migration concerns unchanged in code here rather than partially migrating them early.</action>
  <verify>
`rg -n "skills', 'opsx'|skills/opsx|OpsX Installation Check" lib/config.js lib/install.js`
`node bin/opsx.js check`
  </verify>
  <done>
- `lib/config.js` resolves the repo skill bundle as `skills/opsx`.
- `lib/install.js` copies and reads guides from `skills/opsx`.
- `node bin/opsx.js check` reports `OpsX Installation Check` without requiring any `skills/openspec` bundle.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo skill bundle -> installed home directories | Wrong source paths would install contradictory public guidance |
| install/check/doc plumbing -> operator-facing output | A mixed `openspec`/`opsx` bundle would mislead users about the supported identity |

Auth/session/data threats: N/A for this plan because it renames shipped skill assets and local install plumbing only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `skills/opsx/**`, `skills/openspec/**` | mitigate | Remove the shipped `skills/openspec` entry bundle and keep only `skills/opsx` as the public identity per D-12. |
| T-01-06 | Information Disclosure / Misleading Output | `lib/install.js` | mitigate | Point check/doc/install output at `skills/opsx` and `OpsX Installation Check` so operator guidance cannot drift from the shipped bundle. |
| T-01-07 | Elevation / auth/session/data | skill/plumbing rename | N/A | No auth, session, privilege, or user-data boundary is introduced in this plan. |
</threat_model>

<verification>
- Run `rg -n "name: opsx|OpsX|\\$opsx|/opsx-" skills/opsx`.
- Run `! rg -n "OpenSpec|openspec|\\.openspec|\\$openspec|/openspec|/prompts:openspec|skills/openspec|@xenonbyte/openspec|~/.openspec" skills/opsx`.
- Run `node bin/opsx.js check`.
</verification>

<success_criteria>
- The shipped skill bundle lives under `skills/opsx` only.
- Install/check/doc plumbing resolves `skills/opsx` consistently.
- Phase 2 path migration semantics remain deferred instead of being half-implemented here.
</success_criteria>

<output>
After completion, create `.planning/phases/01-opsx-naming-and-cli-surface/01-opsx-naming-and-cli-surface-02-SUMMARY.md`
</output>
